# CatchUp Today 화면 디자인 명세

## 최종 UX 보완 (2026-08-06)

- 상태바를 고려해 화면 최상단과 날짜 선택기 사이에 안전 여백을 둔다.
- 주간 계획이 없으면 `계획 생성하기`로 AI Mate를 열고 생성 문구를 입력만 한다.
- 할 일 카드 본문을 누르면 추천 이유·할 일 조정·날짜별 추가 Prompt Chips를 표시한다.
- 할 일이 없는 날짜에는 `계획 조정`을 표시하고 날짜 기반 요청을 입력만 한다.
- 예정 일정의 `추가` 버튼과 카드 선택 편집은 Upload 추출 일정을 포함한 모든 출처에 적용한다.
- 브리핑의 `4주 일정을 고려해 중요한 일부터 정리했어요.`는 `요.`만 단독 줄바꿈되지 않게 표시한다.

## 1. 문서 목적

이 문서는 첨부된 Today 화면 목업을 개발 구현 기준으로 변환한 화면 전용 디자인 명세다. 개발자는 원본 목업 이미지를 다시 보지 않아도 이 문서만으로 Today 탭의 정상 상태와 MVP에 필요한 파생 상태를 구현할 수 있어야 한다.

이 문서의 범위는 다음과 같다.

- Today 탭의 모바일 우선 레이아웃과 반응형 규칙
- 헤더, 주간 날짜 선택기, AI Mate 브리핑, 할 일 목록, 예정 일정 요약의 시각 명세
- 날짜 선택, 완료 체크, 상세 정보 확인, AI Mate 진입 동작
- 빈 상태, 로딩, 오류, 완료, 제한 상태
- 접근성, 스크롤, 고정 요소, 데이터 바인딩 규칙
- 목업과 제품 기준 문서가 충돌하는 부분의 구현 결정

이 문서는 `PRD.md`, `README.md`, `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`를 제품 기준으로 사용하고 `DESIGN_SYSTEM.md`의 공통 토큰을 따른다. 충돌 시 우선순위는 `PRD.md` → `README.md` → `IA.md` → `USER_FLOW.md` → `SCREEN_SPEC.md` → 목업 → 본 문서의 보완 결정 순이다.

## 2. 제품 범위 확인

### 2.1 PRD와 README 정합성

Today 화면과 관련된 `PRD.md`와 `README.md`의 핵심 범위는 서로 충돌하지 않는다.

- Google Calendar와 CatchUp 직접 입력 개인 일정을 계획에 반영한다.
- 주간 계획은 사용자가 설정한 요일과 시간에 주 1회만 생성한다.
- 한 주는 월요일부터 일요일까지다.
- 계획 생성 시 현재 상태와 앞으로 4주 일정을 참고한다.
- 완료 체크는 허용하지만 체크 직후의 자동 즉시 재조정은 하지 않는다.
- 일일·주간 계획 변경은 AI Mate를 통해서만 처리한다.
- AI Mate 계획 조정은 하루 최대 10회다.
- 사용자는 계획 항목의 시간, 날짜, 우선순위, 내용을 직접 수정할 수 없다.
- 수치형 완료 확률이나 학업 성과 예측은 표시하지 않는다.

### 2.2 목업과 제품 기준의 차이

| 항목 | 목업 관찰 | 제품 기준 | 구현 결정 |
| --- | --- | --- | --- |
| 주간 날짜 순서 | 일요일부터 토요일 | 월요일부터 일요일 | 제품 기준에 따라 월→일 순서로 구현 |
| 할 일 `추가` 버튼 | 직접 추가처럼 보임 | 계획 항목 직접 추가·수정 제외 | 누르면 AI Mate를 열고 `할 일 추가 요청` 문맥을 전달 |
| 카드 연필 아이콘 | 직접 수정처럼 보임 | 계획 항목 직접 수정 제외 | 누르면 AI Mate를 열고 해당 항목 조정 문맥을 전달 |
| 알림 벨 | 헤더 우측에 보임 | 알림 기능 정의 없음 | MVP 기본 구현에서는 제외. 향후 알림 기능 확정 시 44px 슬롯에 추가 |
| 예정 일정 요약 | 현재 캡처 영역에 없음 | Today 필수 섹션 | 할 일 목록 아래 스크롤 영역에 구현 |
| 예상 소요시간·추천 근거 | 접힌 카드에는 보이지 않음 | 할 일 기본 정보 | 카드 상세 펼침 영역에서 제공 |
| 마감일 칩 | 카드에 표시 | 할 일과 예정 일정의 개념 구분 필요 | 할 일의 원본 과제 마감 정보로 표시하되 예정 일정 자체와 동일시하지 않음 |
| 날짜와 요일 조합 | 주간 선택기와 마감 칩이 하나의 실제 연도로 일치하지 않음 | 날짜가 데이터 기준이어야 함 | 요일 문자열을 하드코딩하지 않고 ISO 날짜에서 계산 |

목업의 시각 언어는 유지하되, 직접 편집처럼 보이는 액션은 반드시 AI Mate 흐름으로 연결한다.

## 3. 기준과 판정 방법

### 3.1 값의 신뢰도

| 표기 | 의미 |
| --- | --- |
| `확정` | 목업 또는 제품 문서에서 텍스트, 존재 여부, 순서, 정책을 직접 확인할 수 있는 값 |
| `추정` | 목업 픽셀 비율을 390px 모바일 뷰포트로 환산한 권장값 |
| `구현 결정` | 정적 목업만으로 알 수 없지만 일관된 동작과 접근성을 위해 이 문서에서 정한 규칙 |

첨부 PNG는 `626 × 1292px`이며 iPhone 기기 프레임, Dynamic Island, 상태바, 홈 인디케이터를 포함한다. 실제 웹앱 DOM은 기기 프레임과 운영체제 UI를 그리지 않는다.

아래 수치는 내부 앱 화면을 `390px` 너비의 모바일 웹 뷰포트로 환산한 값이다. 렌더링, 원근감, 안티앨리어싱 차이에 따라 위치와 크기는 약 `±2px`, 색상은 유사 토큰 범위의 오차를 허용한다.

### 3.2 목업이 보여주는 상태

| 항목 | 현재 상태 | 판정 |
| --- | --- | --- |
| 현재 탭 | `Today` 활성 | 확정 |
| 선택 날짜 | 10일 | 확정 |
| 주간 날짜 범위 | 7일~13일 | 확정 |
| AI 브리핑 | 정상 응답 표시 | 확정 |
| 할 일 개수 | 3개 | 확정 |
| 완료 상태 | 3개 모두 미완료 | 확정 |
| 할 일 우선순위 | 높음 2개, 보통 1개 | 확정 |
| AI Mate | 패널 닫힘, 플로팅 버튼 표시 | 확정 |
| 예정 일정 요약 | 화면 아래에 있거나 미표현 | 구현 결정 |
| 스크롤 위치 | 화면 최상단 | 추정 |
| 오류·로딩 | 표시되지 않음 | 확정 |

## 4. 화면 정보 구조

### 4.1 위에서 아래로의 계층

```text
[상단 Safe Area]

[이번 주 날짜 선택기]
  월 화 수 목 금 토 일
  날짜 원형 버튼 7개

[AI Mate 브리핑 카드]
  스파클 + AI Mate
  선택 날짜 요약과 추천 근거
  AI Mate 캐릭터

[선택 날짜의 할 일 섹션]
  섹션 제목 + 개수 배지 + AI Mate 추가 요청 버튼
  할 일 카드 1
  할 일 카드 2
  할 일 카드 3

[선택 날짜의 예정 일정 섹션]
  섹션 제목 + 개수 배지
  예정 일정 카드 또는 빈 상태

[본문 하단 여백]

[AI Mate 플로팅 버튼]

[고정 하단 탭바]
  Today / Month / Upload

[하단 Safe Area]
```

### 4.2 DOM 권장 구조

```text
AppShell
├─ main.TodayPage
│  ├─ TodayHeader
│  ├─ WeekDatePicker
│  ├─ AiBriefingCard
│  ├─ TodoSection
│  │  └─ TodoCard[]
│  └─ ScheduleSection
│     └─ ScheduleCard[]
├─ AiMateFloatingButton
└─ BottomTabBar
```

- `main`만 세로 스크롤한다. `구현 결정`
- 플로팅 버튼과 하단 탭바는 뷰포트에 고정한다. `확정`
- 날짜 선택기와 헤더는 기본 상태에서 스크롤 콘텐츠에 포함한다. sticky로 만들지 않는다. `구현 결정`
- 모바일 화면에서 가로 페이지 스와이프는 사용하지 않는다. `확정`

## 5. 전체 레이아웃

### 5.1 모바일 기준

기준 뷰포트는 `390 × 844px`이다.

| 요소 | 권장값 | 판정 |
| --- | ---: | --- |
| 앱 최대 너비 | 모바일 `100%`, 넓은 화면 `430px` | 구현 결정 |
| 본문 좌우 패딩 | `20px` | 디자인 시스템 |
| 콘텐츠 유효 너비 | `350px` | 계산값 |
| 상단 여백 | `max(20px, env(safe-area-inset-top))` 이후 `24px` | 추정 |
| 하단 탭바 콘텐츠 높이 | `76px` | 디자인 시스템 |
| 본문 하단 패딩 | `calc(176px + env(safe-area-inset-bottom))` 이상 | 디자인 시스템 |
| 플로팅 버튼 | `70 × 70px` | 디자인 시스템 |
| 플로팅 버튼 우측 | `20px` | 디자인 시스템 |
| 플로팅 버튼 하단 | `calc(94px + env(safe-area-inset-bottom))` | 디자인 시스템 |

### 5.2 데스크톱과 태블릿

- `768px` 이상에서는 앱 콘텐츠를 너비 `430px`의 단일 열로 중앙 정렬한다. `구현 결정`
- 바깥 배경은 `color.canvas`를 유지한다.
- 하단 탭바와 플로팅 버튼은 중앙 앱 열을 기준으로 고정한다. 브라우저 전체 우측에 붙이지 않는다.
- 데스크톱에서도 Today를 다열 대시보드로 확장하지 않는다. 10주 MVP에서는 모바일과 동일한 정보 구조를 유지한다.
- 포인터 환경에서도 모든 핵심 액션은 키보드로 접근 가능해야 한다.

## 6. 디자인 토큰

공통 값은 `DESIGN_SYSTEM.md`를 재사용한다. 이 화면에서 사용하는 토큰은 다음과 같다.

### 6.1 색상

| 역할 | 토큰 / 값 | 용도 |
| --- | --- | --- |
| 앱 배경 | `color.canvas` / `#F8F7FB` | 전체 배경 |
| 카드 배경 | `color.surface` / `#FFFFFF` | 할 일·예정 일정 카드 |
| 주요 텍스트 | `color.ink` / `#111014` | 헤더, 제목 |
| 보조 텍스트 | `color.muted` / `#747786` | 우선순위, 메타 |
| 비활성 아이콘 | `color.icon.muted` / `#687181` | 체크박스, 비활성 탭 |
| 경계선 | `color.line` / `#ECEAF0` | 카드, 탭바 |
| 주요 보라 | `color.purple` / `#806CF2` | AI 라벨, 활성 탭, 강조 |
| 진한 보라 | `color.purple.dark` / `#6F4BE9` | pressed, focus |
| 연보라 | `color.purple.soft` / `#E7DEFF` | 개수 배지, 약한 강조 |
| AI 카드 배경 | `color.ai.surface` / `#FBF9FF` | 브리핑 카드 |
| AI 카드 테두리 | `color.ai.border` / `#D9C9FF` | 브리핑 카드 |
| 선택 날짜 배경 | `#151419` | 선택 날짜 원 |
| 완료 색상 | `color.success` / `#29B84A` | 체크 완료 |
| 오류 색상 | `color.error` / `#C4566B` | 오류 메시지 |

보라색은 AI와 주요 인터랙션에 사용하고, 우선순위 의미를 색상만으로 전달하지 않는다.

### 6.2 타이포그래피

기본 글꼴은 `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`다.

| 용도 | 크기 / line-height | 굵기 | 색상 |
| --- | --- | ---: | --- |
| 화면 인사말 | `24px / 32px` | `800` | 주요 텍스트 |
| 요일 | `12px / 16px` | `500` | 보조 텍스트 |
| 날짜 숫자 | `16px / 22px` | `500`, 선택 `700` | 상태별 |
| AI Mate 라벨 | `15px / 21px` | `700` | 주요 보라 |
| AI 브리핑 본문 | `14px / 22px` | `600` | 주요 텍스트 |
| 섹션 제목 | `20px / 28px` | `800` | 주요 텍스트 |
| 개수 배지 | `14px / 20px` | `700` | 주요 보라 |
| 할 일 제목 | `15px / 22px` | `700` | 주요 텍스트 |
| 카드 메타 | `13px / 19px` | `500` | 보조 텍스트 |
| 마감 칩 | `12px / 18px` | `500` | 보조 텍스트 |
| 상세 근거 | `13px / 20px` | `400~500` | 보조 텍스트 |
| 탭 라벨 | `12px / 16px` | 활성 `600`, 비활성 `400` | 상태별 |

- 사용자의 글자 크기 설정을 존중하며 고정 높이로 텍스트를 자르지 않는다.
- 한글 단어 중간의 강제 분리를 피한다.
- 할 일 제목은 기본 2줄까지 표시하고 이후 말줄임한다.

### 6.3 간격과 형태

| 요소 | 값 |
| --- | ---: |
| 기본 짧은 간격 | `8px` |
| 카드 내부 간격 | `12px` |
| 섹션 내부 간격 | `16px` |
| 섹션 사이 | `24px` |
| 일반 카드 radius | `14px` |
| AI 브리핑 radius | `18px` |
| 날짜 버튼 radius | `999px` |
| 보조 버튼 radius | `12px` |
| 마감 칩 radius | `999px` |

일반 카드 그림자는 `0 6px 20px rgba(111, 75, 233, 0.06)`을 권장한다. 브리핑 카드는 그림자보다 테두리로 구분한다.

## 7. 컴포넌트 상세

### 7.1 할 일 메타데이터

- 인사말은 날짜를 바꿔도 그대로 유지할 수 있다.
- 개인 이름을 넣지 않는다. 실제 학생 개인정보를 화면 예시나 테스트에 사용하지 않는다.

### 7.2 이번 주 날짜 선택기

#### 구조

```text
[월] [화] [수] [목] [금] [토] [일]
 [7]  [8]  [9] [10] [11] [12] [13]
```

#### 치수

| 요소 | 값 | 판정 |
| --- | ---: | --- |
| 전체 너비 | `350px` | 계산값 |
| 열 수 | `7` | 확정 |
| 열 너비 | `42px` 내외, 균등 분배 | 추정 |
| 요일과 날짜 간격 | `8px` | 추정 |
| 날짜 버튼 시각 크기 | `40 × 40px` | 추정 |
| 날짜 버튼 터치 영역 | 최소 `44 × 44px` | 접근성 |
| 헤더와 선택기 간격 | `20px` | 추정 |
| 선택기와 AI 카드 간격 | `24px` | 추정 |

#### 상태

| 상태 | 시각 |
| --- | --- |
| 기본 | 흰 배경, `color.line` 1px 테두리, 주요 텍스트 |
| 선택 | `#151419` 배경, 흰 날짜 숫자, 테두리 없음 |
| 오늘이지만 미선택 | 보라색 2px 외곽선 또는 날짜 아래 `오늘` 접근성 텍스트 |
| 선택된 오늘 | 선택 스타일 적용, 스크린리더 이름에 `오늘` 포함 |
| 비활성 | 이번 주 밖 날짜는 표시하지 않으므로 별도 비활성 없음 |
| 포커스 | `color.purple` 2px outline, `2px` offset |
| pressed | 배경 명도 8% 감소 |

#### 동작

- 한 주의 순서는 반드시 월요일→일요일이다.
- Today 첫 진입 시 현재 날짜를 선택한다.
- 사용자가 다른 날짜를 선택하면 브리핑, 할 일, 예정 일정, 개수 배지를 해당 날짜 데이터로 함께 갱신한다.
- 날짜 변경 자체는 AI Mate 조정 횟수를 차감하지 않는다.
- 날짜 변경 중 별도의 전체 화면 로더를 띄우지 않는다. 데이터가 로컬에 있으면 즉시 전환하고, 원격 조회가 필요하면 각 섹션에 skeleton을 표시한다.
- URL 상태를 사용한다면 `?date=YYYY-MM-DD`처럼 날짜를 보존한다.

#### 접근성

- 전체는 `aria-label="이번 주 날짜 선택"` 그룹으로 묶는다.
- 각 버튼의 접근성 이름은 `7월 10일 목요일, 오늘, 선택됨` 형식으로 제공한다.
- 선택 버튼은 `aria-pressed="true"`를 사용한다.
- 키보드에서는 좌우 화살표로 이전·다음 날짜, `Home`으로 월요일, `End`로 일요일에 이동할 수 있다.

### 7.3 AI Mate 브리핑 카드

#### 구조

```text
┌──────────────────────────────────────┐
│ ✦  AI Mate                           │
│                                      │
│ 4주 일정을 고려해 중요한 일부터      │
│ 정리했어요.                           │
│ 오늘 집중하면 목표 달성이 더 쉬워요. │ [캐릭터]
└──────────────────────────────────────┘
```

#### 치수와 스타일

| 항목 | 값 | 판정 |
| --- | ---: | --- |
| 너비 | `350px` | 계산값 |
| 최소 높이 | `124px` | 추정 |
| 내부 패딩 | 상하 `20px`, 좌우 `18px` | 추정 |
| radius | `18px` | 디자인 시스템 |
| 테두리 | `1.5px solid color.ai.border` | 추정 |
| 배경 | `color.ai.surface` | 디자인 시스템 |
| 캐릭터 시각 크기 | 약 `72 × 72px` | 추정 |

- 좌측 텍스트 영역은 캐릭터와 겹치지 않도록 우측 패딩을 최소 `86px` 확보한다.
- 캐릭터는 카드 우측 하단에 배치하고 장식 이미지로 처리한다.
- 전용 에셋이 준비되기 전 임의의 학생 사진이나 개인 이미지를 사용하지 않는다.
- 스파클은 `sparkles` 계열 아이콘을 사용하고 `aria-hidden="true"`로 처리한다.
- 캐릭터 이미지에는 의미 전달이 없으므로 빈 `alt=""`를 사용한다.

#### 콘텐츠 규칙

- 첫 문장은 선택 날짜의 핵심 요약이다.
- 둘째 문장은 행동을 부드럽게 독려하거나 가장 중요한 추천 이유를 설명한다.
- 전체는 최대 3줄을 권장하며 최대 90자 이내로 생성한다.
- `4주 일정을 고려해 중요한 일부터 정리했어요.`처럼 추천의 근거가 드러나야 한다.
- 정확한 완료 확률, 성적, 성공 가능성, 성과 예측 수치를 쓰지 않는다.
- `반드시`, `무조건` 같은 명령형보다 `먼저 시작하는 것을 추천해요`처럼 참고용 표현을 사용한다.
- 데이터가 없을 때 AI가 분석한 것처럼 허위 문구를 표시하지 않는다.

#### 상호작용

- 카드 전체는 기본적으로 정보 영역이며 버튼이 아니다.
- 상세 추천 근거가 필요하면 카드 하단에 `AI Mate에게 이유 묻기` 텍스트 버튼을 조건부로 표시할 수 있다.
- 해당 버튼은 AI Mate 패널을 열고 선택 날짜를 문맥으로 전달한다.
- 브리핑 로딩 중에는 카드 크기를 유지한 2~3줄 skeleton을 표시한다.

### 7.4 할 일 섹션 헤더

#### 구조

```text
[7월 10일 할 일] [3개]                    [AI에게 추가 요청]
```

#### 텍스트

- 선택 날짜가 오늘이면 제목은 `오늘의 할 일`이다.
- 다른 날짜를 선택하면 `7월 11일 할 일`처럼 날짜 기반으로 바꾼다.
- 개수 배지는 미완료와 완료를 모두 포함한 해당 날짜의 전체 할 일 수를 표시한다.
- 목업의 짧은 라벨은 `추가`지만, 제품 범위를 명확히 하기 위해 접근성 이름과 tooltip은 `AI에게 할 일 추가 요청`으로 제공한다.

#### 치수

| 요소 | 값 |
| --- | ---: |
| AI 카드와 섹션 헤더 간격 | `28px` |
| 헤더 최소 높이 | `44px` |
| 제목과 배지 간격 | `10px` |
| 개수 배지 내부 패딩 | 세로 `4px`, 가로 `9px` |
| 추가 요청 버튼 | 최소 `44 × 44px`, 시각 높이 `36px` |

#### 추가 요청 버튼 동작

- 누르면 빈 할 일을 로컬에 직접 추가하지 않는다.
- AI Mate 패널을 열고 `선택한 날짜에 할 일을 추가하고 싶어요.` 초안을 입력창에 제공한다.
- 사용자가 메시지를 전송해 실제 계획 조정을 요청할 때만 하루 10회 제한을 적용한다.
- 잔여 횟수가 0이면 버튼은 여전히 AI Mate 패널을 열 수 있지만, 패널에서 제한 상태와 다음 사용 가능 시점을 안내한다.

### 7.5 할 일 카드

#### 구조

```text
┌──────────────────────────────────────┐
│ [체크] [할 일 제목]                  │
│        [우선순위 아이콘] 높음         │
│                  [달력 7/16 (수) 마감] [AI 조정]
│                                      │
│ [펼침 시: 예상 60분 · 추천 근거]      │
└──────────────────────────────────────┘
```

#### 카드 기본 치수

| 항목 | 값 | 판정 |
| --- | ---: | --- |
| 너비 | `350px` | 계산값 |
| 접힌 상태 최소 높이 | `92px` | 추정 |
| 내부 패딩 | `16px` | 추정 |
| 카드 간격 | `12px` | 추정 |
| radius | `14px` | 디자인 시스템 |
| 테두리 | `1px solid rgba(236,234,240,0.7)` | 추정 |
| 체크박스 시각 크기 | `22 × 22px` | 추정 |
| 체크박스 터치 영역 | `44 × 44px` | 접근성 |
| 조정 아이콘 시각 크기 | `20px` | 추정 |
| 조정 버튼 터치 영역 | `44 × 44px` | 접근성 |
| 마감 칩 높이 | `32px` | 추정 |

#### 필드 배치

- 체크박스는 카드 좌측 상단에 둔다.
- 제목은 체크박스 오른쪽에서 시작하며 조정 버튼과 겹치지 않게 우측 공간을 확보한다.
- 둘째 행에는 우선순위를 좌측, 마감 칩과 AI 조정 버튼을 우측에 배치한다.
- 폭이 좁거나 텍스트가 길면 둘째 행을 두 줄로 감싼다. 메타를 10px 이하로 축소하지 않는다.
- 과목명 또는 자료 출처가 있으면 제목 위에 `자료구조` 같은 짧은 eyebrow 텍스트로 표시하거나 펼침 상세에 넣는다. 제목보다 시각적으로 강하게 만들지 않는다.

#### 표시 데이터

| 필드 | 필수 | 표시 규칙 |
| --- | --- | --- |
| 완료 여부 | 필수 | 체크박스 |
| 할 일 제목 | 필수 | 최대 2줄 |
| 과목명 또는 자료 출처 | 필수 | 짧은 과목명 우선, 없으면 `업로드 자료` |
| 예상 소요시간 | 필수 | `예상 60분` |
| 우선순위 | 필수 | `높음`, `보통`, `낮음` |
| 마감일 | 조건부 | 원본 과제·시험에 마감이 있을 때만 칩 표시 |
| 추천 근거 | 필수 | 펼침 영역에 1~2문장 |

#### 우선순위

- 아이콘은 작은 깃발을 사용한다.
- `높음`은 보라색 깃발, `보통`은 회색 깃발, `낮음`은 옅은 회색 깃발을 사용할 수 있다.
- 색상만으로 구분하지 않고 텍스트를 항상 함께 표시한다.
- 우선순위는 사용자가 카드에서 직접 바꿀 수 없다.

#### 마감 칩

- 달력 아이콘 + `M/D (요일) 마감` 형식이다.
- 현재 연도와 같은 해에는 연도를 생략한다.
- 날짜 정보가 없으면 칩을 숨기며 임의의 날짜를 생성하지 않는다.
- 마감이 지났고 미완료면 `7/12 (토) 마감 지남`으로 명시하고 오류색은 보조적으로만 사용한다.
- 마감 칩을 눌러 날짜를 직접 편집하지 않는다.

#### 완료 체크

- 체크박스를 누르면 해당 계획 항목의 완료 상태만 변경한다.
- 완료 시 체크박스는 `color.success` 채움과 흰 체크로 바뀐다.
- 제목은 취소선을 사용할 수 있고 텍스트 명도를 낮춘다.
- 완료된 카드는 목록 아래로 즉시 재정렬하지 않는다. 사용자가 읽던 위치를 유지한다.
- 완료 체크 직후 남은 계획을 자동으로 재조정하지 않는다.
- 네트워크 저장 중에는 체크박스 안에 작은 진행 표시를 쓰고 중복 클릭을 막는다.
- 저장 실패 시 원래 상태로 되돌리고 `완료 상태를 저장하지 못했어요. 다시 시도해주세요.`를 카드 인접 영역 또는 toast로 알린다.

#### 추천 이유 확인

- 할 일 카드에서 추천 이유를 직접 펼치지 않는다.
- 추천 이유가 궁금하면 AI Mate 대화에서 해당 할 일을 물어본다.
- 이 문의는 대화형 정보 확인이며, 계획 항목의 직접 편집 기능을 제공하지 않는다.

#### AI 조정 버튼

- 목업의 연필 모양을 유지할 수 있지만 의미는 직접 편집이 아니라 `AI로 조정`이다.
- 접근성 이름은 `자료구조 과제 정리 AI로 조정`처럼 항목명을 포함한다.
- 누르면 AI Mate 패널을 열고 해당 `planItemId`, 선택 날짜, 제목을 문맥으로 전달한다.
- 패널 입력창에는 `자료구조 과제 정리 계획을 조정하고 싶어요.` 초안을 제공한다.
- 메시지를 보내기 전에는 데이터를 변경하지 않는다.
- 직접 편집 폼, 날짜 선택기, 우선순위 드롭다운을 열지 않는다.

### 7.6 예정 일정 요약 섹션

이 섹션은 목업의 현재 캡처 영역에는 보이지 않지만 `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`에 정의된 Today 필수 영역이다. 할 일 목록 다음에 이어지는 동일한 스크롤 흐름으로 구현한다.

#### 구조

```text
[오늘의 예정 일정] [2개]

┌──────────────────────────────────────┐
│ [유형 아이콘] 일정명                  │
│ 시간 · 유형 · 출처                    │
└──────────────────────────────────────┘
```

#### 포함 데이터

- 선택 날짜가 마감인 과제
- 선택 날짜에 제출해야 하는 항목
- 선택 날짜에 예정된 시험
- 중요 공지 일정
- Google Calendar 개인 일정
- CatchUp에서 직접 추가한 개인 일정

#### 할 일과의 구분

- 할 일은 AI가 주간 계획에서 해당 날짜에 배치한 학습 행동이다.
- 예정 일정은 원본 데이터에 날짜가 확정된 사건이다.
- 같은 과제의 `초안 작성` 할 일과 `최종 제출 마감` 일정이 같은 화면에 모두 나타날 수 있다.
- 중복처럼 보이지 않도록 섹션 제목, 유형, 출처를 명확히 표시한다.

#### 카드 필드

| 필드 | 표시 예 |
| --- | --- |
| 일정명 | `자료구조 과제 제출 마감` |
| 시간 | `오후 11:59` 또는 `종일` |
| 유형 | `마감`, `제출`, `시험`, `중요 공지`, `개인 일정` |
| 출처 | `업로드 자료`, `Google Calendar`, `CatchUp 직접 입력` |

- 학업 일정과 개인 일정을 서로 다른 카테고리 색만으로 구분하지 않는다.
- 출처와 유형 텍스트를 항상 표시한다.
- 예정 일정 카드는 Today에서 직접 수정하지 않는다.
- CatchUp 직접 입력 개인 일정 수정은 Month의 개인 일정 흐름으로 이동한다.

### 7.7 AI Mate 플로팅 버튼

- 크기는 `70 × 70px`, radius `35px`다.
- 보라색 그라디언트 원 안에 전용 AI Mate 얼굴 캐릭터를 표시한다.
- 바깥 흰색 `2px` 링과 약한 보라 그림자를 사용한다.
- 우측 `20px`, 하단 `calc(94px + env(safe-area-inset-bottom))`에 고정한다.
- 할 일 카드와 겹치는 경우에도 콘텐츠가 버튼 아래로 가려지지 않도록 본문 하단 패딩을 유지한다.
- 누르면 AI Mate 바텀시트를 연다.
- 접근성 이름은 `AI Mate 열기`다.
- AI Mate 패널이 열리면 플로팅 버튼을 숨긴다.
- 패널 세부 구현은 `AIMATE_DESIGN.md`를 따른다.

### 7.8 하단 탭바

#### 구성

| 탭 | 아이콘 | 현재 상태 |
| --- | --- | --- |
| Today | 활성 `home`, 비활성 `home-outline` | 활성 |
| Month | `calendar-outline` | 비활성 |
| Upload | `cloud-upload-outline` | 비활성 |

#### 명세

- 탭바 콘텐츠 높이는 `76px`이며 하단 Safe Area를 별도로 더한다.
- 배경은 `rgba(255,255,255,0.96)`, 상단 구분선은 `color.line` 1px다.
- 아이콘은 `26px`, 라벨은 `12px`, 둘 사이 간격은 `6px`다.
- 활성 탭은 보라색, 비활성 탭은 `color.icon.muted`다.
- 각 탭의 터치 영역은 폭을 균등 분배하고 높이는 최소 `44px` 이상이다.
- 현재 Today 탭을 다시 누르면 본문을 최상단으로 스크롤한다.
- 탭 전환 시 AI Mate 패널이 열려 있으면 먼저 닫거나 패널 안에서 전환을 차단한다.

## 8. 화면 문구

### 8.1 목업 정상 상태

| 위치 | 문구 |
| --- | --- |
| AI 라벨 | `AI Mate` |
| 브리핑 1 | `4주 일정을 고려해 중요한 일부터 정리했어요.` |
| 브리핑 2 | `오늘 집중하면 목표 달성이 더 쉬워요. 💪` |
| 할 일 제목 | `오늘의 할 일` |
| 개수 | `3개` |
| 추가 요청 버튼 | `추가` |
| 할 일 1 | `자료구조 과제 정리` |
| 할 일 2 | `마케팅 발표 준비` |
| 할 일 3 | `운영체제 퀴즈 복습` |
| 우선순위 | `높음`, `보통` |
| 마감 칩 | `7/16 (수) 마감`, `7/18 (금) 마감`, `7/12 (토) 마감` |
| 탭 | `Today`, `Month`, `Upload` |

위 텍스트는 목업 재현용 익명 샘플이다. 테스트 fixture로 사용할 수 있지만 실제 학생 이름, 계정, 시간표, 성적, 캘린더 데이터는 사용하지 않는다.

### 8.2 권장 빈 상태 문구

| 상태 | 제목 | 설명 / 행동 |
| --- | --- | --- |
| 이번 주 계획 없음 | `아직 이번 주 할 일이 없어요.` | `AI Mate에서 이번 주 계획을 생성해보세요.` |
| 선택 날짜 할 일 없음 | `이날의 할 일은 없어요.` | `다른 날짜를 확인하거나 AI Mate에게 계획 조정을 요청해보세요.` |
| 예정 일정 없음 | `예정된 일정이 없어요.` | 별도 CTA 없이 종료 |
| 캘린더 미연결 | `개인 일정을 불러오려면 Google Calendar 연결이 필요해요.` | `연결하기` |

### 8.3 제한 문구

- 주간 계획 생성 완료: `이번 주 계획은 이미 생성했어요. 변경이 필요하면 AI Mate에 조정을 요청해주세요.`
- 생성 가능 전: `다음 주간 계획은 {요일} {시간}에 생성할 수 있어요.`
- 조정 잔여 0회: `오늘의 계획 조정 10회를 모두 사용했어요. 내일 다시 요청할 수 있어요.`
- 자동 재조정 없음: 완료 체크 후 별도 안내를 상시 표시하지 않는다. 사용자가 요청할 때만 AI Mate로 조정한다.

## 9. 상태별 화면

### 9.1 초기 로딩

- 헤더와 하단 탭바는 즉시 표시한다.
- 날짜 선택기 7개, AI 카드, 카드 2~3개 높이의 skeleton을 표시한다.
- skeleton은 `prefers-reduced-motion`에서 움직이지 않는다.
- 300ms 미만의 빠른 로딩에는 skeleton을 생략해 깜빡임을 줄일 수 있다.

### 9.2 주간 계획 없음

- 날짜 선택기는 유지한다.
- AI 카드에는 `아직 생성된 주간 계획이 없어요.`를 표시한다.
- 할 일 섹션에 빈 상태와 `AI Mate 열기` 버튼을 제공한다.
- 사용자가 설정한 생성 요일·시간 전이면 생성 가능 시점을 안내한다.
- 같은 주에 이미 생성한 기록이 있는데 목록이 비었다면 재생성 CTA를 제공하지 않고 오류 복구 흐름을 사용한다.

### 9.3 선택 날짜 데이터 없음

- 브리핑은 `이날은 계획된 학습 항목이 없어요. 예정 일정을 확인하며 여유 있게 보내세요.`처럼 데이터에 맞게 표시한다.
- 할 일 개수는 `0개`다.
- 다른 날짜 선택은 계속 가능하다.
- 예정 일정이 있다면 예정 일정 섹션은 정상 표시한다.

### 9.4 모든 할 일 완료

- 완료한 카드를 유지해 성취 상태를 확인할 수 있게 한다.
- AI 카드는 `오늘 계획한 일을 모두 완료했어요. 남은 일정은 천천히 확인해보세요.`처럼 축하하되 성과를 예측하지 않는다.
- 자동으로 다음 날로 이동하거나 남은 주간 계획을 재배치하지 않는다.

### 9.5 부분 오류

- 브리핑 생성 실패와 할 일 조회 실패를 분리한다.
- 브리핑만 실패하면 할 일은 유지하고 AI 카드 안에 `브리핑을 불러오지 못했어요. 다시 시도`를 표시한다.
- 할 일만 실패하면 날짜 선택기와 AI 카드 아래에 오류 카드와 `다시 시도`를 표시한다.
- 오류 메시지에 OAuth 토큰, API 키, 원문 개인정보를 포함하지 않는다.

### 9.6 오프라인

- 마지막으로 동기화된 할 일과 일정을 읽기 전용으로 보여줄 수 있다.
- 완료 체크를 로컬 큐에 저장한다면 `동기화 대기 중` 상태를 명확히 표시한다.
- 오프라인에서 AI Mate 조정 요청은 보내지 않고 연결 복구 안내를 제공한다.

## 10. 인터랙션 흐름

### 10.1 날짜 변경

```text
날짜 버튼 선택
→ 선택 스타일 변경
→ 브리핑 로딩 또는 즉시 갱신
→ 할 일 제목·개수·목록 갱신
→ 예정 일정 제목·개수·목록 갱신
→ 스크롤 위치는 날짜 선택기 근처에 유지
```

### 10.2 완료 체크

```text
체크박스 선택
→ 낙관적 완료 상태 표시
→ 서버 저장
→ 성공: 완료 상태 유지
→ 실패: 이전 상태 복원 + 재시도 안내
```

완료 체크는 계획 조정 요청이 아니며 하루 10회 제한을 차감하지 않는다. 완료 후 자동 재계획 API를 호출하지 않는다.

### 10.3 할 일 추가 요청

```text
`추가` 선택
→ AI Mate 패널 열기
→ 선택 날짜를 문맥으로 전달
→ `선택한 날짜에 할 일을 추가하고 싶어요.` 초안 제공
→ 사용자 전송
→ 잔여 횟수 확인
→ 가능하면 AI가 계획 조정
→ Today 목록 갱신 + 추천 근거 표시
```

### 10.4 기존 할 일 조정

```text
카드의 AI 조정 아이콘 선택
→ AI Mate 패널 열기
→ 대상 planItemId와 날짜 전달
→ 조정 요청 초안 제공
→ 사용자 전송
→ 잔여 횟수 확인
→ 조정 결과 반영
```

### 10.5 추천 근거 확인

```text
카드 상세 펼치기
→ 짧은 추천 근거 확인
→ 더 자세한 설명이 필요하면 AI Mate 열기
→ 해당 항목의 추천 이유 질문
```

단순 상세 펼침과 추천 이유 질문은 계획 조정이 아니므로 조정 횟수를 차감하지 않는다.

## 11. 데이터 바인딩

### 11.1 화면 단위 모델

```ts
type TodayViewModel = {
  selectedDate: string; // YYYY-MM-DD
  weekStart: string; // 월요일
  days: WeekDayViewModel[];
  briefing: BriefingViewModel | null;
  todos: TodoViewModel[];
  schedules: ScheduleViewModel[];
  weeklyPlanStatus: "not_created" | "available" | "created";
  nextGenerationAt: string | null;
  adjustmentRemaining: number; // 0~10
  isLoading: boolean;
  error: TodaySectionError | null;
};
```

### 11.2 날짜 모델

```ts
type WeekDayViewModel = {
  date: string;
  weekdayLabel: "월" | "화" | "수" | "목" | "금" | "토" | "일";
  dayOfMonth: number;
  isToday: boolean;
  isSelected: boolean;
  todoCount: number;
  scheduleCount: number;
};
```

### 11.3 할 일 모델

```ts
type TodoViewModel = {
  id: string;
  title: string;
  courseOrSource: string;
  estimatedMinutes: number | null;
  priority: "high" | "medium" | "low";
  completed: boolean;
  dueAt: string | null;
  recommendationSummary: string;
};
```

### 11.4 예정 일정 모델

```ts
type ScheduleViewModel = {
  id: string;
  title: string;
  startsAt: string | null;
  allDay: boolean;
  type: "deadline" | "submission" | "exam" | "notice" | "personal";
  source: "upload" | "google_calendar" | "catchup";
};
```

### 11.5 데이터 규칙

- `weekStart`는 항상 해당 주 월요일이다.
- 선택 날짜의 할 일과 예정 일정은 별도 배열로 유지한다.
- 할 일 카드의 마감 정보는 연결된 원본 학업 일정에서 파생될 수 있다.
- 클라이언트는 AI가 반환하지 않은 추천 근거를 임의로 만들어내지 않는다.
- 날짜·시간 표시는 사용자 로케일과 `Asia/Seoul` 기준을 일관되게 적용한다.
- 요일은 서버 문자열이나 목업 예시를 그대로 쓰지 않고 날짜 라이브러리 또는 표준 `Intl.DateTimeFormat`으로 계산한다.
- 개수 배지는 렌더링된 현재 필터 결과와 일치해야 한다.
- 완료 항목을 숨기는 필터는 MVP 기본값으로 제공하지 않는다.

## 12. 접근성

- 문서의 `main` landmark와 `h1`을 제공한다. 시각 인사말을 `h1`으로 사용할 수 있다.
- 섹션 제목은 `h2`로 연결한다.
- 모든 아이콘 단독 버튼은 텍스트 접근성 이름을 가진다.
- 모든 터치 타깃은 최소 `44 × 44px`다.
- 포커스 표시를 제거하지 않는다.
- 주요 텍스트와 배경은 WCAG AA 대비를 목표로 한다.
- 보라색, 회색, 초록색만으로 상태를 구분하지 않고 텍스트와 아이콘을 함께 사용한다.
- 완료 체크박스는 네이티브 checkbox 또는 동등한 `role="checkbox"`와 `aria-checked`를 사용한다.
- 동적 목록 갱신은 `aria-live="polite"`로 `7월 11일 할 일 2개를 불러왔습니다.`처럼 짧게 알린다.
- 완료 저장 실패와 계획 조정 제한은 스크린리더에도 전달한다.
- 200% 확대에서도 가로 스크롤 없이 한 열 레이아웃을 유지한다.
- `prefers-reduced-motion: reduce`에서는 skeleton shimmer, 카드 전환, 체크 애니메이션을 제거하거나 최소화한다.

## 13. 모션

- 날짜 선택 배경 전환: `120ms ease-out`
- 카드 상세 펼침: `180ms ease-out`
- 완료 체크: `120ms ease-out`
- AI Mate 플로팅 버튼 pressed: `transform: scale(0.96)`, `100ms`
- 목록 전체가 좌우로 슬라이드하는 전환은 사용하지 않는다.
- 날짜 변경 시 콘텐츠는 `opacity`만 짧게 전환하고 레이아웃 높이 변화는 자연스럽게 처리한다.

모션은 정보 이해를 돕는 수준으로 제한하고 사용자가 빠르게 날짜를 넘길 때 지연을 만들지 않는다.

## 14. 반응형·오버플로 규칙

- `320px` 너비에서도 날짜 7개가 한 줄에 유지되도록 버튼 시각 크기를 최소 `36px`까지 줄일 수 있다. 터치 영역은 겹치지 않는 범위에서 `44px`에 가깝게 유지한다.
- 요일과 날짜는 가로 스크롤로 만들지 않는다.
- 할 일 카드 둘째 행이 좁으면 우선순위 행과 마감 행을 세로로 쌓는다.
- 긴 제목은 2줄 말줄임하되 상세 펼침에서 전체 제목을 볼 수 있게 한다.
- 긴 출처는 1줄 말줄임하고 접근성 이름에는 전체 값을 유지한다.
- 브리핑 문구가 길어지면 캐릭터 아래로 흐르지 않도록 텍스트 영역의 최대 너비를 제한한다.
- 화면 높이가 작아도 탭바와 플로팅 버튼은 고정하고 본문을 스크롤할 수 있어야 한다.

## 15. 에셋과 아이콘

### 15.1 필요한 에셋

- AI Mate 브리핑용 캐릭터 이미지
- AI Mate 플로팅 버튼용 얼굴 캐릭터 이미지

두 에셋이 같은 캐릭터를 사용하되 카드용은 몸체가 보이는 버전, 플로팅 버튼용은 얼굴 중심 버전이 적합하다. 최종 브랜드 에셋 파일명과 해상도는 별도 확정이 필요하다.

### 15.2 아이콘 의미

| 기능 | 권장 아이콘 |
| --- | --- |
| AI 브리핑 | `sparkles` |
| 완료 | `square` / `check-square` 또는 native checkbox |
| 우선순위 | `flag` |
| 마감 | `calendar-days` |
| AI 조정 | 목업 재현 시 `pencil`, 의미 명확성 우선 시 `sparkles` |
| Today | `home` |
| Month | `calendar-outline` |
| Upload | `cloud-upload-outline` |

아이콘 라이브러리는 프로젝트 전체에서 하나로 통일한다.

## 16. 구현 금지 사항

- 일요일부터 토요일 순서로 주간 날짜를 구현하지 않는다.
- Today에서 계획 항목을 직접 생성·수정하는 폼을 열지 않는다.
- 카드 연필 아이콘으로 날짜, 시간, 우선순위, 제목을 직접 저장하지 않는다.
- 완료 체크 후 자동으로 남은 계획을 즉시 재조정하지 않는다.
- 같은 주에 주간 계획을 다시 생성하는 버튼을 제공하지 않는다.
- AI Mate 조정을 하루 10회 넘게 허용하지 않는다.
- 정확한 완료 확률, 성적 향상 확률, 성공 예측 수치를 표시하지 않는다.
- Google Calendar 외 외부 캘린더 연결 UI를 추가하지 않는다.
- 학업 일정과 개인 일정을 색상만으로 구분하지 않는다.
- 실제 학생 개인정보나 실제 민감 캘린더 데이터를 fixture로 사용하지 않는다.
- 시스템 상태바, Dynamic Island, iPhone 프레임, 홈 인디케이터를 웹 UI로 그리지 않는다.
- 기능이 정의되지 않은 알림 센터나 알림 설정 화면을 목업의 벨 아이콘만 보고 추가하지 않는다.

## 17. 개발 완료 기준

### 17.1 시각

- [ ] 390px 기준 좌우 패딩이 20px로 일관된다.
- [ ] 배경, 카드, 보라색, 텍스트 색이 `DESIGN_SYSTEM.md` 토큰과 일치한다.
- [ ] 날짜 선택기는 월요일부터 일요일까지 한 줄이다.
- [ ] 선택 날짜는 진한 원형 배경과 흰 텍스트로 표시된다.
- [ ] AI 브리핑 카드는 연보라 테두리, 스파클 라벨, 캐릭터를 포함한다.
- [ ] 할 일 카드에는 체크, 제목, 우선순위, 마감 칩, AI 조정 액션이 있다.
- [ ] 예정 일정 요약이 할 일 목록 아래에 존재한다.
- [ ] 플로팅 버튼과 하단 탭바가 본문 스크롤과 독립적으로 고정된다.
- [ ] 긴 텍스트와 200% 확대에서 겹침이나 잘림이 없다.

### 17.2 동작

- [ ] 첫 진입 시 오늘이 선택된다.
- [ ] 날짜를 바꾸면 브리핑, 할 일, 예정 일정, 개수가 함께 바뀐다.
- [ ] 완료 체크는 상태만 저장하고 자동 재조정을 실행하지 않는다.
- [ ] `추가`와 카드 조정 액션은 직접 편집이 아닌 AI Mate를 연다.
- [ ] 추천 근거 상세를 계획 조정 횟수 차감 없이 볼 수 있다.
- [ ] 주간 계획 미생성, 선택 날짜 빈 상태, 모든 항목 완료, 부분 오류를 처리한다.
- [ ] AI Mate 조정 잔여 0회 상태를 올바르게 안내한다.
- [ ] 하단 탭이 Today, Month, Upload 세 개뿐이다.

### 17.3 접근성

- [ ] 모든 아이콘 버튼의 접근성 이름이 있다.
- [ ] 체크박스와 날짜 선택 상태를 스크린리더가 인식한다.
- [ ] 터치 타깃이 최소 44px에 가깝다.
- [ ] 키보드만으로 날짜 선택, 완료 체크, 상세 펼침, AI Mate 열기가 가능하다.
- [ ] 포커스가 가려지거나 사라지지 않는다.
- [ ] 색상 외 텍스트로 우선순위, 유형, 출처, 완료 상태를 알 수 있다.
- [ ] reduced motion 환경을 지원한다.

## 18. QA용 익명 샘플

```json
{
  "selectedDate": "2025-07-10",
  "briefing": {
    "title": "4주 일정을 고려해 중요한 일부터 정리했어요.",
    "supportingText": "오늘 집중하면 목표 달성이 더 쉬워요. 💪"
  },
  "todos": [
    {
      "id": "todo-sample-1",
      "title": "자료구조 과제 정리",
      "courseOrSource": "자료구조",
      "estimatedMinutes": 60,
      "priority": "high",
      "completed": false,
      "dueAt": "2025-07-16T23:59:00+09:00",
      "recommendationSummary": "마감까지 남은 기간과 예상 소요시간을 고려해 오늘 초안을 정리하는 것을 추천해요."
    },
    {
      "id": "todo-sample-2",
      "title": "마케팅 발표 준비",
      "courseOrSource": "마케팅",
      "estimatedMinutes": 45,
      "priority": "high",
      "completed": false,
      "dueAt": "2025-07-18T18:00:00+09:00",
      "recommendationSummary": "발표 전 검토 시간을 확보할 수 있도록 자료 구성을 먼저 시작하는 것을 추천해요."
    },
    {
      "id": "todo-sample-3",
      "title": "운영체제 퀴즈 복습",
      "courseOrSource": "운영체제",
      "estimatedMinutes": 30,
      "priority": "medium",
      "completed": false,
      "dueAt": "2025-07-12T09:00:00+09:00",
      "recommendationSummary": "퀴즈 일정이 가까워 핵심 개념을 짧게 복습하는 것을 추천해요."
    }
  ],
  "schedules": [
    {
      "id": "schedule-sample-1",
      "title": "팀 프로젝트 회의",
      "startsAt": "2025-07-10T16:00:00+09:00",
      "allDay": false,
      "type": "personal",
      "source": "google_calendar"
    }
  ],
  "adjustmentRemaining": 7
}
```

이 데이터는 레이아웃과 상태 검증용 가상 데이터다. 실제 계정, 학교 LMS, Google Calendar의 민감한 내용을 사용하지 않는다.
# 최종 확정 변경 (2026-08-03)

- 상단 인사말과 할 일·일정 개수를 제거한다.
- 선택 날짜는 `#8C6EF6`으로 채운다.
- 할 일은 완료 체크, 과목, 제목, `30M`·`1H`·`2.5H` 형식 예상시간, 필요 시 마감일만 표시한다.
- 할 일 추가·수정·추천 이유 버튼은 두지 않고 AI Mate 대화로 처리한다.
- 일정에는 `추가` 버튼을 두고 일정 카드 전체를 누르면 수정 창을 연다.
