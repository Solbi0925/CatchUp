# CatchUp 목 기반 프로토타입 디자인 QA

## 목적

2026-08-06 확정 디자인 후속 수정이 모바일 화면과 주요 상호작용에 반영됐는지 확인한다.

## 확인 범위

- Google Calendar 연결 온보딩
- Today의 주간 계획 생성 전·후 상태
- Today의 할 일 카드, 빈 날짜, 일정 추가·수정
- AI Mate의 안내 메시지, 프롬프트 칩, 입력 초안, 메시지 시간 표시
- Month의 일정 제목 표시, 날짜 상세, 일정 추가·수정, 6색 분류

## 결과

| 우선순위 | 확인 항목 | 결과 |
| --- | --- | --- |
| P0 | 주요 화면 진입과 계획 생성 흐름 | 통과 |
| P0 | Today·Month에서 일정 직접 수정 | 통과 |
| P1 | 계획 생성 CTA가 AI Mate를 열고 초안만 입력 | 통과 |
| P1 | 할 일 카드의 3개 프롬프트 칩과 빈 날짜 계획 조정 | 통과 |
| P1 | Google Calendar 아이콘 중앙 정렬, 연보라 프레임, 점선 비침 제거 | 통과 |
| P1 | AI Mate 메시지 시간 제거와 완료 안내 | 통과 |
| P2 | Today 상단 여백, 보라색 선택 날짜, 한 줄 브리핑, `추가` 라벨 | 통과 |
| P2 | Month 일정 제목 노출과 6색 선택 UI | 통과 |

## 상호작용 검증 메모

- `계획 생성하기`는 AI Mate를 열고 `이번 주 계획을 생성해줘`를 입력창에 채우며 자동 전송하지 않는다.
- 할 일 카드를 누르면 `할 일 추천이유`, `할 일 조정`, `[날짜]일 할 일 추가` 칩이 나타나고, 칩 선택은 입력창만 채운다.
- 할 일이 없는 날짜의 `계획 조정`은 `[날짜]일 계획을 조정해줘`를 입력창에 채우며 자동 전송하지 않는다.
- Today와 Month의 일정 선택 시 제목, 날짜, 시간, 유형, 색상을 현재 화면에서 수정할 수 있다.
- 과목 일정 색상 변경 안내는 같은 과목의 모든 일정에 적용됨을 명시한다.

## 자동 검증

- Vitest: 15개 파일, 58개 테스트 통과
- TypeScript: 통과
- Vite 프로덕션 빌드: 통과

최종 판정: **PASS**
