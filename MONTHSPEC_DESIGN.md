# CatchUp Month 날짜별 일정 상세 화면 디자인·구현 명세

## 1. 문서 목적

이 문서는 Month 달력에서 특정 날짜를 선택했을 때 열리는 일정 상세·개인 일정 추가/수정 바텀시트를 개발 구현 기준으로 정의한다. 개발자는 첨부 목업을 다시 보지 않아도 바텀시트의 레이어, 목록, 폼, 검증, 저장·삭제 동작, 권한별 상태, 키보드와 접근성 처리를 구현할 수 있어야 한다.

이 문서는 `MONTH_DESIGN.md`의 하위 화면 명세이며, `PRD.md`, `README.md`, `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`, `DESIGN_SYSTEM.md`를 함께 따른다. 충돌 시 제품 문서를 우선한다.

## 2. 화면 역할과 범위

- 화면 이름: 날짜별 일정 상세
- 표시 방식: Month 위에 열리는 modal bottom sheet
- 권장 URL 상태: `/month?month=2026-07&date=2026-07-18&sheet=schedule`
- 목적:
  - 선택 날짜의 모든 일정을 확인한다.
  - CatchUp 개인 일정을 추가한다.
  - CatchUp에서 직접 만든 개인 일정을 수정·삭제한다.
- 이 화면에서 직접 편집하지 않는 대상:
  - AI가 만든 주간 계획 `Todo`
  - 업로드 자료에서 추출된 학업 일정
  - Google Calendar에서 가져온 일정
- 학업 추출 정보 변경은 Upload의 추출 결과 수정 흐름으로 이동한다.
- 주간 계획 변경은 AI Mate를 통해 요청하며 하루 최대 10회 제한을 따른다.

## 3. 목업과 제품 기준의 차이 및 구현 결정

| 항목 | 목업 관찰 | 제품 기준 | 구현 결정 |
| --- | --- | --- | --- |
| 배경 선택 날짜 | 달력에서 `10`이 선택됨 | 시트 제목·폼 날짜는 `7월 18일` | 시트를 열 때 달력 선택 날짜, 제목, 목록, 폼 날짜를 반드시 같은 ISO 날짜로 동기화 |
| 일정 항목 | `팀 회의`, `중간고사` 모두 연필·삭제 아이콘 노출 | 개인 일정만 Month에서 직접 수정 가능 | 출처와 권한에 따라 액션을 다르게 표시 |
| `일정 추가` 아래 폼 | 폼이 동시에 항상 보임 | 정적 목업에는 상태 구분이 없음 | 기본은 목록 + 접힌 추가 버튼, 버튼을 누르면 폼 확장. 넓은/높은 화면에서 목업처럼 같이 보일 수 있음 |
| 과목/캘린더 | 단일 select | 개인 일정과 학업 일정의 출처 구분 필요 | 개인 일정 추가 시 `개인 일정` 캘린더만 기본 허용. 과목 선택은 Upload 추출 정보 편집과 혼동되지 않게 제한 |
| 유형 | 별도 select | CalendarEvent에 개인 일정 유형만 정의 | MVP 옵션은 `개인 일정`, 필요 시 `수업`만 허용. 과제·시험 생성은 Upload 흐름으로 안내 |
| 삭제 | 아이콘 누르면 즉시 삭제처럼 보임 | 파괴적 행동 확인 필요 | 확인 dialog 후 삭제, 진행 중 중복 요청 방지 |
| 바텀시트 높이 | 탭바 바로 위부터 화면 중간까지 큰 고정 패널 | 다양한 viewport와 키보드 대응 필요 | `max-height` 기반, 내부 스크롤, 키보드 오픈 시 폼 필드가 보이도록 조정 |

## 4. 목업 해석 기준

- 첨부 PNG 크기: `630 × 1378px`
- 기기 프레임과 운영체제 UI는 실제 웹 UI가 아니다.
- 내부 화면은 약 `390 × 844 CSS px` 기준으로 환산한다.
- 목업의 배경은 dim 처리된 Month 화면이다.
- 바텀시트는 하단 내비게이션 위가 아니라 화면 하단 safe area까지 이어지고, 목업에서는 하단 내비게이션이 시트 아래 별도 고정 영역으로 보인다.
- 구현에서는 modal이 열린 동안 배경 달력과 하단 내비게이션의 입력을 모두 차단한다.

## 5. 목업이 보여주는 상태

| 항목 | 상태 |
| --- | --- |
| 선택 날짜 | `7월 18일` |
| 일정 수 | 2개 |
| 목록 일정 | `팀 회의 10:00–11:00`, `중간고사 14:00–16:00` |
| 추가 폼 | 빈 생성 모드 |
| 폼 날짜 | `2026년 7월 18일 (토)` |
| 기본 시간 | `09:00–10:00` |
| 저장 버튼 | 활성처럼 표현 |
| 배경 | 어두운 반투명 dim |
| AI Mate 버튼 | 숨김 |

위 텍스트는 익명화된 샘플 데이터로만 사용한다. 실제 학생 일정이나 계정 정보는 fixture에 넣지 않는다.

## 6. 레이어와 정보 구조

```text
MonthPage
├─ CalendarContent (dim + inert)
├─ BottomTabBar (dim + inert)
└─ ScheduleSheetLayer
   ├─ Backdrop
   └─ ScheduleBottomSheet
      ├─ DragHandle
      ├─ SheetHeader
      │  └─ "{M}월 {D}일 일정"
      ├─ EventList
      │  ├─ EventRow[]
      │  └─ EmptyState
      ├─ AddEventButton
      ├─ Divider
      └─ EventForm
         ├─ TitleField
         ├─ DateField
         ├─ StartTimeField
         ├─ EndTimeField
         ├─ CalendarField
         ├─ TypeField
         ├─ FormError
         └─ SaveButton
```

## 7. 전체 레이아웃

### 7.1 Backdrop

| 속성 | 값 |
| --- | --- |
| 위치 | `position: fixed; inset: 0` |
| 배경 | `rgba(17,16,20,.24)` |
| backdrop blur | `2px` 선택 적용 |
| z-index | page와 sheet 사이 |

- 목업처럼 배경 내용은 인식할 수 있지만 조작할 수 없을 정도로 어둡게 한다.
- backdrop을 누르면 미변경 상태에서는 시트를 닫는다.
- 폼에 저장하지 않은 변경이 있으면 닫기 확인을 먼저 표시한다.
- 배경 스크롤을 잠그고 iOS scroll chaining을 방지한다.

### 7.2 바텀시트

기준 `390 × 844px`에서:

| 속성 | 권장값 |
| --- | ---: |
| 너비 | `100%` |
| 최대 너비 | `430px` |
| 최대 높이 | `calc(100dvh - max(72px, env(safe-area-inset-top) + 40px))` |
| 최소 가시 높이 | `430px` |
| 목업 정상 높이 | 약 `570px` |
| 상단 radius | `24px 24px 0 0` |
| 배경 | `rgba(255,255,255,.98)` |
| 그림자 | `0 -12px 36px rgba(38,31,61,.10)` |
| 좌우 패딩 | `20px` |
| 하단 패딩 | `max(20px, env(safe-area-inset-bottom))` |

- 시트는 앱 중앙 열 하단에 고정한다.
- 내부는 `grid-template-rows: auto auto minmax(0,1fr)` 또는 하나의 scroll container로 구성한다.
- 목업처럼 목록과 폼이 한 화면에 들어가면 모두 표시한다.
- 내용이 넘치면 시트 전체 콘텐츠 영역을 세로 스크롤하되 핸들과 제목은 sticky로 유지할 수 있다.
- 하단 내비게이션은 시각적으로 남겨도 modal이 열린 동안 `inert` 처리한다. 구현을 단순화하려면 시트가 내비게이션까지 덮어도 된다.

### 7.3 키보드가 열린 상태

- `100vh`가 아니라 `100dvh` 또는 `visualViewport`를 사용한다.
- 포커스된 필드와 저장 버튼이 키보드 뒤에 가려지지 않게 스크롤한다.
- 키보드가 열릴 때 시트의 상단 radius와 헤더는 유지한다.
- iOS에서 body가 배경으로 튀지 않도록 현재 scroll position을 보존한다.

## 8. 디자인 토큰

### 8.1 색상

| 역할 | 값 | 용도 |
| --- | --- | --- |
| 시트 배경 | `#FFFFFF` | 패널 |
| 주요 텍스트 | `#111014` | 제목, 일정명, 라벨 |
| 보조 텍스트 | `#747786` | 시간, placeholder |
| 아이콘 | `#687181` | 편집, 삭제, chevron |
| 선 | `#ECEAF0` | 행·섹션·입력 경계 |
| 보라 | `#806CF2` | 깃발, 추가, 활성 필드 |
| 진한 보라 | `#6F4BE9` | focus, pressed |
| 연보라 | `#E7DEFF` | 약한 선택 배경 |
| 점선 | `#C7B7FF` | 일정 추가 버튼 |
| CTA 시작 | `#9A79FF` | 저장 그라디언트 |
| CTA 끝 | `#744FF0` | 저장 그라디언트 |
| 오류 | `#C4566B` | 검증·삭제 오류 |
| backdrop | `rgba(17,16,20,.24)` | 배경 dim |

### 8.2 타이포그래피

| 용도 | 크기 / line-height | 굵기 |
| --- | --- | ---: |
| 시트 제목 | `18px / 26px` | `800` |
| 일정 제목 | `14px / 20px` | `700` |
| 일정 시간 | `12px / 17px` | `500` |
| 폼 라벨 | `12px / 18px` | `700` |
| 입력 텍스트 | `13px / 20px` | `500` |
| placeholder | `13px / 20px` | `400` |
| 추가 버튼 | `14px / 20px` | `600` |
| 저장 버튼 | `16px / 22px` | `500~600` |
| 도움·오류 | `11px / 16px` | `500` |

## 9. 컴포넌트 상세

### 9.1 드래그 핸들

- 시트 상단 중앙의 `44 × 5px` 회색 capsule.
- 시각 색상 `#AEB0B8`.
- 상단 여백 `10px`, 아래 여백 `16px`.
- 접근성 트리에서는 제외한다.
- MVP에서 연속 높이 조절은 필수가 아니다.
- 아래로 `80px` 이상 swipe하면 닫는 동작은 선택 적용한다.
- 미저장 변경이 있으면 swipe 닫기에도 확인을 적용한다.

### 9.2 시트 제목

- 형식: `{M}월 {D}일 일정`
- 예: `7월 18일 일정`
- 좌측 정렬, 한 줄 유지.
- 시트 좌우 `20px` 기준선에 맞춘다.
- 제목 아래 목록까지 `18px` 간격.
- 스크린리더 제목은 연도를 포함한 `2026년 7월 18일 일정`으로 제공한다.

### 9.3 일정 목록

- 선택 날짜의 일정을 시작 시간 오름차순으로 정렬한다.
- 종일 일정은 시간 지정 일정 위에 놓는다.
- 같은 시간에는 시험·마감 등 학업 일정, 개인 일정 순으로 둘 수 있으나 출처를 숨기지 않는다.
- 목록이 4개 이상이면 목록 영역의 최대 높이를 두고 내부 스크롤하거나 시트 전체 스크롤을 사용한다.

#### 일정 행

```text
[유형 아이콘] [일정명                    ] [수정] [삭제]
             [10:00 – 11:00 · 출처]
```

| 속성 | 권장값 |
| --- | ---: |
| 최소 높이 | `58px` |
| 좌우 패딩 | `14px` |
| 상하 패딩 | `10px` |
| 배경 | `#FFFFFF` |
| 테두리 | `1px solid #ECEAF0` |
| radius | `10px` |
| 행 사이 간격 | `0~8px` |
| 유형 아이콘 | `20px` |
| 액션 아이콘 | 시각 `20px`, 타깃 `44px` |

- 목업의 보라색 깃발은 일정 유형을 나타내는 통일된 아이콘이다.
- 색상으로 출처를 나누지 않으므로 시간 아래에 `업로드 자료`, `Google Calendar`, `CatchUp 직접 입력` 중 하나를 표시한다.
- 시간이 없으면 `종일`을 표시한다.
- 제목은 최대 2줄까지 허용하고 액션 버튼 영역을 침범하지 않는다.

#### 편집 권한별 액션

| 출처 | 수정 | 삭제 | 동작 |
| --- | --- | --- | --- |
| CatchUp 직접 입력 | 표시 | 표시 | 이 시트의 폼과 확인 dialog 사용 |
| Google Calendar | 숨김 | 숨김 | 읽기 전용. `Google Calendar 일정` 출처 표시 |
| 업로드 추출 | `추출 정보 수정` 텍스트 액션 선택 | 숨김 | Upload의 해당 추출 항목으로 이동 |
| AI 주간 계획 Todo | 직접 액션 숨김 | 숨김 | 필요 시 `AI Mate로 조정` 동작 제공 |

목업처럼 모든 항목에 동일한 연필·휴지통을 표시하지 않는다.

### 9.4 일정 추가 버튼

- 텍스트: `＋ 일정 추가`
- 목록 아래 `12px` 간격.
- 너비 `100%`, 높이 `48px`.
- `1.5px dashed #C7B7FF`, radius `12px`.
- 배경은 투명 또는 `rgba(242,238,255,.24)`.
- 텍스트와 plus 아이콘은 `color.purple`.
- 누르면 생성 모드 폼을 확장하고 제목 필드로 포커스를 이동한다.
- 폼이 이미 열린 경우 `일정 추가 닫기`로 접근성 상태를 전달하거나 버튼을 숨긴다.

### 9.5 폼 섹션

- 목록 영역과 `1px solid color.line`로 구분한다.
- 구분선 위·아래 간격은 각 `16px`.
- 2열 구조: 라벨 `82px`, 입력 `minmax(0,1fr)`.
- 행 사이 간격 `8px`.
- 모바일 340px 이하 또는 200% 확대에서는 라벨을 입력 위로 쌓는 1열 구조로 전환한다.

```css
.form-row {
  display: grid;
  grid-template-columns: 82px minmax(0, 1fr);
  gap: 12px;
  align-items: center;
}
```

### 9.6 공통 입력

| 속성 | 값 |
| --- | --- |
| 높이 | 최소 `42px` |
| 좌우 패딩 | `12px` |
| radius | `10px` |
| 테두리 | `1px solid #ECEAF0` |
| 배경 | `#FFFFFF` |
| focus | `2px solid #806CF2`, outline offset `1px` |
| disabled | `#F5F4F7` 배경 + 설명 텍스트 |

- placeholder만으로 필드 의미를 전달하지 않고 항상 외부 라벨을 제공한다.
- 오류가 있으면 테두리와 오류 문구를 함께 표시한다.
- 오류 발생 시 첫 오류 필드로 포커스를 옮긴다.

### 9.7 제목 필드

- 라벨: `제목`
- placeholder: `일정 제목을 입력하세요`
- 최대 길이: `60자`
- 앞뒤 공백을 제거한 뒤 1자 이상이어야 한다.
- 여러 줄 입력은 허용하지 않는다.
- 실제 학생 이름, 학번, 이메일 등 개인정보를 예시 placeholder로 사용하지 않는다.

### 9.8 날짜 필드

- 라벨: `날짜`
- 표시 형식: `YYYY년 M월 D일 (요일)`
- 내부 값: `YYYY-MM-DD`
- 우측에 달력 outline 아이콘.
- 기본값은 Month에서 선택한 날짜다.
- 누르면 접근 가능한 date picker를 연다.
- 날짜를 바꾸면 저장 후 Month의 선택 날짜를 새 날짜로 이동할지 묻지 않고, 저장 성공 후 새 날짜를 선택 상태로 반영한다.
- 수정 모드에서 날짜 변경은 CatchUp 직접 입력 일정에만 허용한다.

### 9.9 시작·종료 시간 필드

- 라벨: `시작 시간`, `종료 시간`
- 표시 형식: `HH:mm`, 24시간제.
- 기본 생성값:
  - 시작 시간: 다음 정시 또는 선택 날짜가 미래면 `09:00`
  - 종료 시간: 시작 시간 + 1시간
- 목업 재현용 샘플은 `09:00–10:00`.
- 옵션 간격은 MVP에서 `30분` 단위를 권장하되 직접 입력 시간도 정규화할 수 있다.
- 종료 시간은 시작 시간보다 늦어야 한다.
- 동일 시간 또는 역전이면 `종료 시간은 시작 시간보다 늦어야 합니다.`를 표시한다.
- 자정을 넘는 일정은 MVP에서 한 날짜 일정으로 저장하지 않고 날짜를 나누도록 안내한다.
- 종일 일정 지원 시 유형 또는 별도 checkbox로 시간 필드를 비활성화하고 `isAllDay=true`를 저장한다. 목업에는 checkbox가 없으므로 초기 구현에서 생략 가능하다.

### 9.10 과목/캘린더 필드

- 라벨: `과목/캘린더`
- 목업 placeholder: `캘린더 선택`
- 왼쪽에 보라색 깃발, 오른쪽에 chevron.
- 개인 일정 생성 시 옵션:
  - `개인 일정 · CatchUp`
  - 연결 상태 표시용 `Google Calendar`는 읽기 전용 또는 향후 동기화 옵션으로 표시하지 않음
- Upload에서 확정된 과목을 단순 분류값으로 연결하는 것이 구현되어 있다면 과목명을 선택지에 추가할 수 있으나, 이 폼에서 시험·과제 원본을 새로 만드는 기능으로 해석하지 않는다.
- 필수값이며 기본값은 `개인 일정 · CatchUp`.
- 선택지마다 색을 다르게 주지 않는다.

### 9.11 유형 필드

- 라벨: `유형`
- placeholder: `일정 유형 선택`
- 오른쪽 chevron.
- MVP 허용값:
  - `개인 일정`
  - `수업 일정` — 사용자가 직접 관리하는 개인 일정으로 취급할 때만
- `과제`, `시험`, `제출`, `중요 공지`는 업로드 추출 데이터의 유형이므로 개인 일정 추가 폼에서 기본 제공하지 않는다.
- 유형 목록을 늘리더라도 색상 카테고리 체계는 추가하지 않는다.

### 9.12 저장 버튼

- 텍스트:
  - 생성 모드: `저장`
  - 수정 모드: `변경사항 저장`
- 너비 `100%`, 높이 `52px`, radius `10px`.
- 배경 `linear-gradient(110deg, #9A79FF 0%, #744FF0 100%)`.
- 텍스트 흰색, `16px`.
- 폼 아래 `16px` 간격.

상태:

| 상태 | 표현·동작 |
| --- | --- |
| 초기 무효 | 비활성, `aria-disabled="true"` |
| 유효 | 보라 그라디언트 |
| hover | 밝기 소폭 증가 |
| active | `translateY(1px)` |
| focus-visible | 바깥 `3px` focus ring |
| 저장 중 | `저장 중…`, 중복 클릭 방지 |
| 성공 | 짧은 `저장했어요.` status 후 목록 갱신 |
| 실패 | 폼 상단 또는 버튼 아래 오류 + 재시도 |

저장 성공 시:

1. 서버 응답을 기준으로 목록에 반영한다.
2. 생성 모드 폼을 초기화하고 접는다.
3. Month 달력의 해당 날짜 점·칩을 갱신한다.
4. AI 계획에 참고되는 `CalendarEvent` 캐시를 무효화한다.
5. 주간 계획을 자동 즉시 재조정하지 않는다.

### 9.13 수정 모드

- 편집 아이콘 접근성 이름: `{일정명} 수정`.
- 누르면 폼에 해당 일정 값을 채운다.
- 폼 상단에 `일정 수정` 상태 라벨과 `취소` 텍스트 버튼을 추가한다.
- 동시에 한 일정만 편집한다.
- 다른 일정의 편집을 누를 때 미저장 변경이 있으면 확인한다.
- 저장 전에는 목록과 Month의 원본 데이터를 낙관적으로 바꾸지 않는다.

### 9.14 삭제

- 휴지통 버튼 접근성 이름: `{일정명} 삭제`.
- 누르면 확인 dialog를 연다.

```text
이 일정을 삭제할까요?
"팀 회의" 일정이 CatchUp에서 삭제됩니다.

[취소] [삭제]
```

- 삭제 CTA는 오류색을 사용한다.
- 삭제 중 버튼을 비활성화한다.
- 성공하면 목록과 Month 표시를 갱신한다.
- 삭제 실패 시 원래 행을 유지하고 재시도 안내를 표시한다.
- Google Calendar 일정과 학업 추출 일정에는 Month 삭제 버튼을 제공하지 않는다.
- 삭제 후에도 AI 주간 계획을 자동 즉시 재조정하지 않는다. 필요하면 AI Mate를 안내한다.

## 10. 시트 열기·닫기 동작

### 열기

1. Month의 날짜 버튼을 누른다.
2. 달력 선택 상태를 갱신한다.
3. backdrop을 `120ms` fade-in한다.
4. 시트를 `220ms` 동안 아래에서 위로 이동한다.
5. 시트 제목으로 포커스를 옮기거나 첫 상호작용 요소에 포커스를 둔다.
6. 일정 목록을 불러온다.

### 닫기

- backdrop 클릭
- Escape 키
- 아래로 swipe
- 브라우저 뒤로가기
- 명시적 닫기 버튼을 추가할 경우 해당 버튼

닫힌 뒤 포커스는 시트를 열었던 날짜 버튼으로 돌아간다.

### 미저장 변경

- dirty form 상태에서 닫기를 시도하면 `변경사항을 저장하지 않고 닫을까요?` 확인 dialog를 연다.
- `계속 작성`과 `저장하지 않고 닫기`를 제공한다.
- 단순히 폼을 열었지만 값이 바뀌지 않았다면 확인 없이 닫는다.

### 모션 축소

`prefers-reduced-motion: reduce`에서는 slide 애니메이션을 제거하고 짧은 opacity 전환 또는 즉시 표시한다.

## 11. 상태별 화면

### 11.1 일정 없음

```text
{M}월 {D}일 일정

이 날짜에는 일정이 없어요.

[＋ 일정 추가]
```

- 빈 상태 때문에 시트를 자동으로 닫지 않는다.
- 생성 폼은 `일정 추가`를 누른 뒤 연다.

### 11.2 목록 로딩

- 제목과 추가 버튼은 유지한다.
- 일정 행 위치에 높이 `58px` skeleton 2개를 표시한다.
- 폼 입력을 막을 필요는 없지만 같은 날짜 데이터 충돌을 피하려면 로딩 완료 후 활성화한다.

### 11.3 목록 오류

- `일정을 불러오지 못했어요.`와 `다시 시도`를 표시한다.
- 로컬 생성 폼은 사용할 수 있게 둘 수 있으나 저장 시 중복 검사를 수행한다.

### 11.4 저장 오류

- 사용자가 입력한 값을 지우지 않는다.
- 필드 오류는 해당 필드 아래, 서버 오류는 저장 버튼 위에 표시한다.
- OAuth 토큰이나 서버 stack trace를 표시하지 않는다.

### 11.5 읽기 전용 일정만 존재

- 일정 목록과 출처를 정상 표시한다.
- 각 항목에 편집·삭제 아이콘을 두지 않는다.
- CatchUp 개인 일정 추가 버튼은 유지한다.

### 11.6 매우 많은 일정

- 목록을 시간순으로 유지한다.
- 시트 내부 스크롤을 사용한다.
- 추가 버튼은 목록 뒤에 두되 필요하면 sticky 보조 CTA로 만들 수 있다.
- 화면 높이를 무제한 늘리거나 배경 페이지까지 밀어 올리지 않는다.

## 12. 데이터 계약

```ts
type ScheduleSheetMode = "list" | "create" | "edit";

type ScheduleFormValue = {
  title: string;
  date: string;          // YYYY-MM-DD
  startTime: string;     // HH:mm
  endTime: string;       // HH:mm
  calendarId: string;    // CatchUp 개인 일정 캘린더
  type: "personal" | "class";
  isAllDay: boolean;
};

type ScheduleListItem = {
  id: string;
  title: string;
  date: string;
  startTime?: string;
  endTime?: string;
  isAllDay: boolean;
  source: "upload" | "google-calendar" | "catchup" | "weekly-plan";
  sourceLabel: string;
  editable: boolean;
  deletable: boolean;
};
```

검증 규칙:

- `title`: trim 후 1~60자
- `date`: 유효한 ISO local date
- `startTime`, `endTime`: `HH:mm`
- `endTime > startTime`
- `calendarId`: 허용된 사용자의 CatchUp 캘린더
- `type`: 서버 허용 목록 중 하나
- 수정·삭제는 서버에서도 `source=catchup` 권한을 재검증

## 13. 접근성

- 바텀시트는 `role="dialog"`, `aria-modal="true"`, `aria-labelledby`를 사용한다.
- 배경 앱과 탭바는 `inert` 처리한다.
- 포커스를 시트 안에 가두고 닫을 때 원래 날짜 버튼으로 복귀한다.
- 시트 제목의 접근성 이름에는 연도를 포함한다.
- 모든 아이콘 버튼에 일정명을 포함한 접근성 이름을 준다.
- 폼 label과 control을 `for`/`id`로 연결한다.
- 오류 메시지는 `aria-describedby`로 필드와 연결한다.
- 저장 결과와 목록 갱신은 `aria-live="polite"`로 알린다.
- 삭제 dialog는 파괴적 행동임을 명확히 읽어준다.
- 터치 타깃은 최소 `44 × 44px`.
- 색상만으로 편집 가능 여부, 오류, 선택 상태를 표현하지 않는다.
- 200% 텍스트 확대에서 폼이 1열로 전환되고 가로 스크롤이 없어야 한다.

## 14. 구현 및 보안 주의

- 클라이언트의 `editable` 값만 믿지 말고 서버에서 일정 소유권과 출처를 검증한다.
- Google OAuth 토큰은 폼 데이터나 브라우저 로그에 노출하지 않는다.
- 일정 제목을 HTML로 직접 삽입하지 않고 텍스트로 escape한다.
- 중복 저장 방지를 위해 요청 중 버튼 잠금 또는 idempotency 처리를 한다.
- 날짜/시간 저장과 표시 모두 사용자 timezone을 명시적으로 사용한다.
- 테스트에는 `팀 회의`, `스터디`, `샘플 시험 일정` 같은 가짜 데이터만 사용한다.
- 저장·삭제 후 AI 주간 계획을 자동으로 즉시 재조정하지 않는다.

## 15. 구현 금지 사항

- 배경 달력의 선택 날짜와 시트 날짜를 서로 다르게 표시하지 않는다.
- 모든 일정에 편집·삭제 아이콘을 일괄 표시하지 않는다.
- Google Calendar 일정이나 학업 추출 일정을 CatchUp 로컬 삭제 API로 삭제하지 않는다.
- AI 주간 계획 항목을 이 폼으로 직접 수정하지 않는다.
- 종료 시간이 시작 시간보다 이른 값을 저장하지 않는다.
- 저장 중 폼 값을 초기화하지 않는다.
- 삭제 확인 없이 즉시 파괴적 요청을 보내지 않는다.
- 키보드가 열린 상태에서 저장 버튼을 화면 밖에 고정적으로 숨기지 않는다.
- 실제 개인정보나 실제 학교 계정 데이터를 샘플로 사용하지 않는다.

## 16. 개발 완료 조건

- [ ] Month에서 선택한 ISO 날짜와 시트 제목·목록·폼 날짜가 항상 일치한다.
- [ ] 일정 목록이 종일 우선, 시작 시간 오름차순으로 정렬된다.
- [ ] 일정 출처가 텍스트로 표시된다.
- [ ] CatchUp 직접 입력 일정만 이 화면에서 수정·삭제할 수 있다.
- [ ] 학업 추출 일정은 Upload 수정 흐름, AI Todo는 AI Mate 조정 흐름으로 연결된다.
- [ ] 일정이 없는 날짜에서도 개인 일정 추가가 가능하다.
- [ ] 제목·날짜·시간·캘린더·유형 검증이 동작한다.
- [ ] 종료 시간 역전, 저장 실패, 삭제 실패 상태가 구현된다.
- [ ] 저장·삭제 후 Month의 점·칩과 상세 목록이 갱신된다.
- [ ] 저장·삭제가 주간 계획을 자동 즉시 재조정하지 않는다.
- [ ] backdrop, Escape, 뒤로가기, swipe 닫기와 미저장 변경 확인을 검증했다.
- [ ] 모바일 키보드와 safe area에서 필드·저장 버튼이 가려지지 않는다.
- [ ] modal focus trap, 포커스 복귀, 아이콘 접근성 이름을 검증했다.
