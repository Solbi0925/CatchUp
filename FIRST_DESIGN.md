# CatchUp 첫 화면 디자인 명세

## 1. 문서 목적

이 문서는 CatchUp에 처음 진입했을 때 노출되는 브랜드 인트로 화면을 개발자가 목업 이미지 없이 구현할 수 있도록 정의한다. 화면의 시각 구성, 레이아웃, 타이포그래피, 전환 동작, 반응형 규칙, 접근성, 검수 기준을 포함한다.

이 문서는 `PRD.md`, `README.md`, `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`를 기준으로 하며, 충돌 시 `PRD.md`를 우선한다.

## 2. 화면 역할과 범위

- 화면 이름: 브랜드 인트로
- 권장 경로: `/onboarding/intro`
- 대상: CatchUp에 처음 진입한 사용자
- 목적: 서비스 이름과 핵심 가치를 짧게 전달하고 Google Calendar 연동 화면으로 자연스럽게 이동시킨다.
- 핵심 메시지: 오늘과 이번 주의 학업 계획을 한눈에 볼 수 있다.
- 주요 행동: 사용자 조작 없이 Google Calendar 연동 화면으로 자동 이동한다.
- 다음 화면: `/onboarding/calendar`
- 이 화면에는 버튼, 입력창, 하단 네비게이션, AI Mate를 표시하지 않는다.

## 3. 목업 해석 기준

목업은 iPhone 기기 프레임을 포함하고 있으나 실제 웹 UI에는 아래 요소를 렌더링하지 않는다.

- 금속 기기 프레임
- Dynamic Island
- iOS 시각 상태바의 시간, 통신, Wi-Fi, 배터리 아이콘
- 하단 홈 인디케이터

구현 기준 뷰포트는 `393 × 852 CSS px`이다. 앱 콘텐츠는 브라우저의 실제 viewport를 채우며, iOS PWA 또는 전체 화면 환경에서는 `env(safe-area-inset-*)`를 사용한다.

## 4. 화면 카피

화면에 표시되는 문구는 아래와 같이 고정한다.

```text
Catch Up

오늘과 이번 주
학업 계획을 한눈에
```

- 프로젝트명은 문서와 일반 UI에서 `CatchUp`으로 표기한다.
- 이 화면의 워드마크만 목업과 동일하게 `Catch Up`으로 띄어 쓸 수 있다.
- 보조 문구는 2줄 고정이 기본이며, 임의로 마침표를 추가하지 않는다.

## 5. 화면 구조

```text
IntroPage
├─ BackgroundLayer
│  ├─ BaseGradient
│  ├─ TopLeftLightArc
│  ├─ BottomRightOrb
│  └─ SparkleGroup
└─ BrandContent
   ├─ Wordmark
   └─ Tagline
```

권장 DOM 구조:

```html
<main class="intro-page" aria-labelledby="intro-title">
  <div class="intro-background" aria-hidden="true"></div>
  <section class="intro-content">
    <h1 id="intro-title">Catch Up</h1>
    <p>오늘과 이번 주<br />학업 계획을 한눈에</p>
  </section>
</main>
```

배경 장식은 의미 없는 시각 요소이므로 접근성 트리에서 제외한다.

## 6. 레이아웃 명세

### 6.1 전체 화면

| 속성 | 값 |
| --- | --- |
| 너비 | `100vw` |
| 최소 높이 | `100dvh` |
| 기준 크기 | `393 × 852px` |
| 콘텐츠 정렬 | 가로 중앙 |
| 오버플로 | 숨김 |
| 기본 좌우 안전 여백 | `24px` |
| 상단 safe area | `env(safe-area-inset-top)` |
| 하단 safe area | `env(safe-area-inset-bottom)` |

`100vh` 대신 모바일 주소창 변화에 대응하는 `100dvh`를 우선 사용하고, 미지원 브라우저에는 `100vh`를 fallback으로 둔다.

### 6.2 브랜드 콘텐츠 블록

기준 뷰포트에서 콘텐츠 블록의 중심은 화면 가로 중앙, 세로 약 `43%` 지점에 둔다.

| 항목 | 기준값 |
| --- | --- |
| 콘텐츠 최대 너비 | `345px` |
| 상단 위치 | viewport 높이의 약 `37%` |
| 워드마크와 설명 사이 | `18px` |
| 정렬 | 중앙 정렬 |
| 쌓임 순서 | 배경 장식보다 위 |

고정 좌표만 사용하지 말고 아래 범위 안에서 유동적으로 유지한다.

```css
.intro-content {
  position: absolute;
  top: clamp(280px, 37vh, 332px);
  left: 50%;
  width: min(calc(100% - 48px), 345px);
  transform: translateX(-50%);
  text-align: center;
}
```

### 6.3 작은 화면 대응

- viewport 높이가 `700px` 미만이면 콘텐츠의 상단 위치를 `32vh`까지 올린다.
- 최소 지원 너비는 `320px`이다.
- 너비 `320px`에서도 워드마크가 한 줄을 유지하도록 글자 크기를 축소한다.
- 장식 그래픽은 잘려도 되지만 워드마크와 설명을 침범하면 안 된다.

## 7. 시각 스타일

### 7.1 색상 토큰

목업을 기준으로 한 권장 토큰이다. 실제 구현 시 대비 검사를 거쳐 미세 조정할 수 있다.

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--intro-bg-base` | `#F7F4FF` | 기본 배경 |
| `--intro-bg-white` | `#FFFFFF` | 상단 빛 번짐 |
| `--intro-lavender-100` | `#EEE8FF` | 옅은 보라 안개 |
| `--intro-lavender-200` | `#DDD0FF` | 하단 구체 |
| `--intro-purple-500` | `#8F6BFF` | 워드마크 시작색 |
| `--intro-purple-600` | `#7456F6` | 워드마크 중심색 |
| `--intro-purple-300` | `#C6A7FF` | 워드마크 끝색 |
| `--intro-text` | `#18151F` | 설명 문구 |
| `--intro-sparkle` | `rgba(255,255,255,0.92)` | 별 장식 |

### 7.2 배경

전체 배경은 흰색과 연보라색이 부드럽게 번지는 저대비 그라디언트다. 단일 선형 그라디언트보다 여러 radial gradient를 겹쳐 구현한다.

권장 배경 구성:

```css
background:
  radial-gradient(75% 45% at 17% 10%, rgba(255,255,255,.98) 0%, rgba(255,255,255,.62) 44%, transparent 72%),
  radial-gradient(74% 60% at 88% 83%, rgba(199,180,255,.62) 0%, rgba(223,211,255,.40) 50%, transparent 79%),
  linear-gradient(145deg, #FAF8FF 0%, #F2ECFF 48%, #E7DCFF 100%);
```

배경에는 뚜렷한 밴딩, 강한 채도, 어두운 보라색을 사용하지 않는다. 전체 인상은 밝고 공기감 있는 라벤더 톤이어야 한다.

### 7.3 상단 좌측 빛의 곡선

- 화면 좌측 상단 바깥에서 시작하는 큰 원호 형태다.
- 기준 지름: 약 `460px`
- 기준 위치: `left: -290px`, `top: -120px`
- 테두리: `1px solid rgba(255,255,255,.72)`
- 내부: 투명 또는 매우 약한 흰색
- blur: `0~2px`

원호는 목업의 빛 번짐을 보조하는 요소이며, 실제 이미지 에셋이 있다면 그 에셋을 우선 사용한다.

### 7.4 하단 우측 라벤더 구체

- 화면 우측과 하단을 넘어 잘리는 큰 원형 또는 타원형 빛 덩어리다.
- 기준 크기: `360 × 360px`
- 기준 위치: `right: -105px`, `bottom: -90px`
- 테두리: `1px solid rgba(255,255,255,.64)`
- 배경: 흰색에서 라벤더로 번지는 radial gradient
- shadow: 내부에 `0 0 70px rgba(151,113,255,.12)`

이 요소는 콘텐츠보다 뒤에 있어야 하며 텍스트 가독성을 낮추면 안 된다.

### 7.5 별 장식

목업에는 워드마크 우측에 3개의 흰색 별빛이 있다.

| 별 | 크기 | 기준 위치 |
| --- | --- | --- |
| 큰 별 | 약 `36 × 36px` | `right: 42px`, `top: 300px` |
| 중간 별 | 약 `10 × 10px` | `right: 43px`, `top: 418px` |
| 작은 별 | 약 `7 × 7px` | `right: 28px`, `top: 401px` |

- 별은 이모지나 텍스트 문자로 구현하지 않는다.
- 브랜드 에셋 또는 검증된 아이콘 에셋을 사용한다.
- 장식 전체의 opacity는 `0.75~0.95` 범위로 둔다.
- 별 장식에는 포인터 이벤트를 주지 않는다.

## 8. 타이포그래피

### 8.1 공통 폰트

```css
font-family:
  "Pretendard Variable",
  Pretendard,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  sans-serif;
```

폰트 로딩 전후 레이아웃 이동을 줄이기 위해 적절한 fallback을 반드시 둔다.

### 8.2 워드마크

| 속성 | 값 |
| --- | --- |
| 텍스트 | `Catch Up` |
| 크기 | `clamp(48px, 13.5vw, 58px)` |
| 행간 | `0.98` |
| 굵기 | `800` 또는 `850` |
| 스타일 | `italic` |
| 자간 | `-0.045em` |
| 정렬 | 중앙 |
| 줄바꿈 | 금지 |

글자색은 좌측의 진한 보라에서 우측의 연한 라벤더로 흐르는 수평 그라디언트다.

```css
background: linear-gradient(96deg, #7658F6 4%, #9975FF 53%, #CEAEFF 100%);
background-clip: text;
color: transparent;
```

그라디언트 텍스트를 지원하지 않거나 고대비 모드가 활성화된 경우 `#7456F6` 단색을 사용한다.

### 8.3 설명 문구

| 속성 | 값 |
| --- | --- |
| 크기 | `18px` |
| 행간 | `1.55` 또는 `28px` |
| 굵기 | `500` |
| 자간 | `-0.02em` |
| 색상 | `#18151F` |
| 정렬 | 중앙 |

두 줄의 폭과 중심을 동일하게 맞추고, 워드마크보다 시각적 우선순위가 낮아야 한다.

## 9. 진입 및 자동 전환

### 9.1 기본 동작

1. 화면 진입 즉시 배경과 콘텐츠를 렌더링한다.
2. 콘텐츠는 `180ms` 동안 opacity `0 → 1`로 나타난다.
3. 화면 진입 후 `1,800ms`가 지나면 Google Calendar 연동 화면으로 이동한다.
4. 이동 전 `220ms` 동안 전체 화면을 약하게 fade-out한다.
5. history에는 인트로를 남기지 않도록 `replace` 방식의 이동을 권장한다.

총 체류 시간은 약 `2초`를 넘기지 않는다. 전환 중 네트워크 요청이나 OAuth를 시작하지 않는다.

### 9.2 중복 노출 방지

- 사용자가 인트로를 이미 완료했다면 앱 재방문 때마다 반복 노출하지 않는다.
- 온보딩 상태 저장 예시: `introSeen: true`
- Google Calendar 연결 여부와 인트로 노출 여부는 별도 상태로 관리한다.
- 이미 연결된 사용자는 인트로를 건너뛰고 메인 앱의 `Today`로 이동할 수 있다.

### 9.3 모션 축소 설정

`prefers-reduced-motion: reduce`인 경우:

- fade-in과 fade-out을 제거한다.
- 장식의 반짝임 또는 반복 애니메이션을 사용하지 않는다.
- 자동 이동 자체는 유지하되 즉시 화면이 바뀌는 것처럼 보이지 않도록 최소 `1,000ms`의 정적 노출 시간을 둔다.

## 10. 반응형 규칙

### 모바일: `320~767px`

- 전체 viewport를 채운다.
- 기준 레이아웃을 유지한다.
- 워드마크는 `48~58px` 사이에서 유동 조정한다.
- safe area를 반영한다.

### 태블릿·데스크톱: `768px 이상`

CatchUp은 모바일과 데스크톱 웹을 모두 지원하므로 인트로 콘텐츠를 무제한 확장하지 않는다.

- 배경은 브라우저 전체를 채운다.
- 중앙에 최대 `430px` 폭의 콘텐츠 안전 영역을 둔다.
- 워드마크 최대 크기는 `64px`다.
- 하단 구체와 별 장식은 중앙 콘텐츠 영역을 기준으로 배치한다.
- 모바일 기기 프레임을 데스크톱에서 장식으로 추가하지 않는다.

## 11. 접근성

- 문서의 최상위 제목은 `h1` 하나로 구성한다.
- 장식용 배경과 별은 `aria-hidden="true"` 처리한다.
- 스크린 리더 읽기 순서는 `Catch Up` 다음 핵심 설명이어야 한다.
- 자동 전환 화면이므로 중요한 약관, 오류, 사용자 선택을 이 화면에 넣지 않는다.
- 설명 문구는 배경과 최소 `4.5:1` 대비를 확보한다.
- 워드마크는 일반 텍스트가 아니라 브랜드 표시이지만 고대비 모드에서 단색 fallback을 제공한다.
- 페이지 제목은 `CatchUp` 또는 `CatchUp 시작`으로 설정한다.
- 화면 전환 뒤 포커스는 Google Calendar 연동 화면의 `h1`으로 이동시킨다.

## 12. 구현 시 금지 사항

- 목업 속 iPhone 프레임, Dynamic Island, 상태바, 홈 인디케이터를 HTML로 재현하지 않는다.
- 별 장식에 이모지를 사용하지 않는다.
- 워드마크를 일반 이미지로만 제공해 스크린 리더에서 서비스명이 사라지게 하지 않는다.
- 배경을 하나의 진한 보라색으로 단순화하지 않는다.
- 사용자가 기다려야 하는 로딩 화면처럼 스피너나 진행률을 추가하지 않는다.
- 이 화면에서 Google OAuth 요청을 미리 실행하지 않는다.

## 13. 개발 완료 조건

- [ ] `393 × 852px`에서 워드마크와 설명이 목업과 같은 중앙 상단 비율에 배치된다.
- [ ] 워드마크가 한 줄이며 보라색 그라디언트와 italic 굵은 서체를 사용한다.
- [ ] 설명은 정확히 2줄이고 문구가 일치한다.
- [ ] 배경은 흰색과 라벤더색의 은은한 빛 번짐을 가진다.
- [ ] 큰 별 1개와 작은 별 2개가 워드마크 우측에 배치된다.
- [ ] 하단 우측의 큰 라벤더 구체가 viewport 밖으로 자연스럽게 잘린다.
- [ ] 약 2초 이내에 Google Calendar 연동 화면으로 자동 이동한다.
- [ ] 이미 인트로를 본 사용자는 반복 노출되지 않는다.
- [ ] `320px` 너비와 데스크톱 너비에서 콘텐츠가 잘리거나 과도하게 늘어나지 않는다.
- [ ] reduced motion, 고대비 모드, 키보드·스크린 리더 전환을 검증한다.

## 14. 목업만으로 확정할 수 없는 항목

다음 값은 목업에서 정확히 확인할 수 없어 구현 기준값으로 정의했다.

- 실제 브랜드 전용 폰트의 정확한 종류
- 배경 그라디언트의 원본 색상값과 blur 수치
- 인트로 노출 시간
- 전환 애니메이션의 easing
- 재방문 시 인트로 노출 정책

위 값은 이 문서의 권장값으로 먼저 구현하고, 실제 디자인 토큰이나 브랜드 에셋이 제공되면 해당 원본을 우선한다.
