# CatchUp Screen Spec

## 1. 문서 목적

> 최초 계획이 없으면 `주간계획 생성`을 제공하고, 계획이 있으면 Today 할 일 컨텍스트의 `주간계획 수정`·`할 일 추천이유`를 제공한다. 새 확정 학업 정보나 개인 일정으로 계획 변경이 필요하면 필요한 질문 후 자동 조정하고, 결과 설명과 `아냐, 취소해줘`를 제공한다. Google Calendar는 읽기 전용으로 동기화한다.

이 문서는 `IA.md`와 `USER_FLOW.md`를 바탕으로 CatchUp MVP의 화면별 구성 요소, 표시 정보, 사용자 행동, 상태를 정의한다.

화면은 발표용 노트북의 localhost에서 동작한다. UI는 `frontend/`, 파일 임시 처리와 `codex exec` 결과 반환, Google OAuth token 관리는 `backend/` Local Backend/Bridge가 담당하며, 화면에는 정규화된 일정만 Local Storage에서 표시한다.

현재 화면 구조는 아래를 기준으로 한다.

```text
온보딩
-> Google Calendar 연동
-> 메인 앱

하단 네비게이션
├─ Today
├─ Month
└─ Upload

전역 플로팅
└─ AI Mate
```

## 2. 공통 화면 규칙

### 네비게이션

- 메인 앱은 하단 네비게이션 바를 사용한다.
- 하단 네비게이션에는 `Today`, `Month`, `Upload` 3개 탭만 둔다.
- AI Mate는 하단 네비게이션의 개별 탭으로 만들지 않는다.
- AI Mate는 모든 메인 화면에서 접근 가능한 전역 플로팅 버튼으로 제공한다.
- 이전의 가로 스크롤 페이지 이동은 사용하지 않는다.

### 공통 데이터 표시 원칙

- 학업 일정과 개인 일정은 출처를 구분해 표시한다.
- AI가 추출한 정보 중 불확실한 값은 `확인 필요`로 표시한다.
- 추천 근거는 Today 카드에 별도 버튼으로 표시하지 않고, 플로팅 AI Mate에서 질문한다.
- 수치형 완료 확률은 표시하지 않는다.
- Today 할일과 주간 계획 항목의 직접 수정 UI는 MVP에서 제공하지 않는다. 계획의 생성과 수정은 AI Mate로만 처리한다.
- 주요 포인트 컬러는 보라 계열로 통일한다.
- 월간 일정은 지정된 6색 제목 칩으로 표시하며 같은 과목과 개인 일정 카테고리는 각각 공통 색상을 사용한다.

### 공통 제한

- Plan 생성은 사용자가 원하는 날짜에 요청하며 요청일부터 정확히 7일이다.
- Today의 날짜 순서는 Plan 시작일과 관계없이 월요일부터 일요일까지다.
- Today는 실제 오늘이 포함된 Calendar Week를 기본 표시하고 이전 주·다음 주 이동을 제공한다.
- 현재 Plan 범위 밖 날짜의 날짜 원은 회색으로 표시한다.
- AI Mate를 통한 일일/주간 계획 조정 요청은 하루에 최대 10번만 가능하다.
- Google Calendar 외 다른 외부 캘린더는 연결하지 않는다.

### 공통 상태

| 상태 | 설명 |
| --- | --- |
| 빈 상태 | 아직 업로드, 연결, 계획 생성이 완료되지 않은 상태 |
| 로딩 상태 | 자료 추출, 일정 불러오기, 계획 생성 중인 상태 |
| 확인 필요 | AI 추출 결과 중 사용자가 검토해야 하는 항목이 있는 상태 |
| 오류 상태 | 업로드 실패, 추출 실패, 캘린더 연결 실패 등 |
| 정상 상태 | 일정과 계획이 표시되는 상태 |
| 제한 도달 | 주간 계획 생성 또는 AI Mate 조정 요청 가능 횟수를 모두 사용한 상태 |

## 3. 온보딩 화면

### 목적

사용자가 CatchUp의 첫인상과 서비스 무드를 빠르게 인지한 뒤 Google Calendar 연동으로 자연스럽게 이동하도록 한다.

### 화면 구성

```text
[포인트 컬러 그라디언트 배경]
[Catch Up 워드마크]
[짧은 한 줄 설명]
[자동 전환 상태]
```

### 표시 정보

- 워드마크: Catch Up
- 한 줄 설명: 오늘과 이번 주 학업 계획을 한눈에
- 보라 계열 포인트 컬러 그라디언트
- 은은한 빛 번짐 또는 별빛 보조 그래픽
- 다음 화면으로 이동 중임을 느낄 수 있는 짧은 로딩 또는 전환 상태

### 사용자 행동

- 앱 소개 확인
- 별도 조작 없이 Google Calendar 연동 화면으로 자동 이동

### 전환 규칙

- 첫 실행 시 온보딩 화면을 짧게 노출한 뒤 Google Calendar 연동 화면으로 자동 전환한다.
- 프로젝트명과 문서명은 `CatchUp`을 유지하되, 온보딩 워드마크에서는 `Catch Up` 표기를 사용할 수 있다.

## 4. Google Calendar 연동 화면

### 목적

사용자의 개인 일정을 불러와 학업 일정과 함께 주간 계획 생성에 반영할 수 있도록 한다.

### 화면 구성

```text
[Google Calendar 연동 안내]
[Google Calendar 아이콘과 연결 그래픽]
[연동 필요 이유]
[Google Calendar 연결 버튼]
[연결 상태]
```

### 표시 정보

- Google Calendar 연동 안내
- 개인 일정과 학업 일정 충돌 방지 설명
- 주간 계획 생성 시 개인 일정이 반영된다는 설명
- 연결 시 반영되는 정보
  - 개인 일정과 수업 시간을 주간 계획에 반영
  - 충돌 없는 일정으로 하루 만들기
  - 언제든 연결 해제 가능
- 연결 상태
  - 연결 전
  - 연결 중
  - 연결 완료
  - 연결 실패
- Google Calendar 아이콘은 연보라색 프레임의 정중앙에 배치하고, 점선 연결선은 두 아이콘 프레임 사이에만 표시한다.

### 사용자 행동

- Google Calendar 연결 버튼 선택
- 권한 승인
- 연결 실패 시 재시도
- 연결 완료 후 메인 앱 진입

OAuth는 Local Bridge의 localhost callback으로 처리한다. Google 일정은 CatchUp에서 읽기 전용이며 변경은 Google Calendar에서 수행한다. 자동화 테스트에서는 실제 계정 대신 Fake Calendar 응답을 사용한다.

### 오류 상태

- 연결 실패 시 재시도 안내를 표시한다.
- Google Calendar 연결이 완료되지 않으면 주간 계획 생성 전에 연결을 완료하도록 안내한다.

## 5. 메인 공통 레이아웃

### 화면 구성

```text
[현재 탭 콘텐츠]

[플로팅 AI Mate 버튼]

[하단 네비게이션]
- Today
- Month
- Upload
```

### 플로팅 AI Mate 버튼

- 모든 메인 탭에서 표시한다.
- 하단 네비게이션 오른쪽 위 또는 화면 오른쪽 하단에 배치한다.
- 사용자가 누르면 AI Mate 패널을 연다.
- 버튼은 주요 콘텐츠를 가리지 않도록 배치한다.

## 6. 탭 1: Today

### 목적

AI가 이번 주 계획에서 선택한 날짜로 배치한 학습 항목과 예정 일정을 빠르게 확인한다. 첫 진입 시 오늘 날짜가 기본 선택된다.

### 화면 구성

```text
[이번 주 날짜 선택 영역]

[AI Mate 브리핑 카드]

[선택한 날짜의 할일]

[선택한 날짜의 예정 일정 요약]

[플로팅 AI Mate 버튼]
```

정확한 날짜가 있는 학업 이벤트는 정보 충분성 확정 여부와 무관하게 해당 날짜에만 표시하고, 미확정이면 `미확정 학업 일정`을 함께 표시한다. 정확한 날짜 없이 예정 주차만 있는 이벤트는 주차-날짜 기준이 확보된 경우 그 주의 모든 날짜에서 `미확정 일정 · [이벤트명] 주`로 표시한다. 기준이 없으면 특정 날짜를 추측하지 않는다.

### 섹션 1: 이번 주 날짜 선택 영역

#### 정의

현재 Calendar Week의 월요일부터 일요일까지를 한 줄로 배치해 사용자가 날짜별 할 일을 확인할 수 있게 한다. Plan 범위와 Calendar Week가 다를 수 있으므로 Plan 범위 밖 날짜 원은 회색으로 표시한다.

#### 표시 정보

| 항목 | 설명 |
| --- | --- |
| 요일 | 월, 화, 수, 목, 금, 토, 일 |
| 날짜 | 해당 주의 날짜 숫자 |
| 선택 상태 | 선택한 날짜를 `#8C6EF6` 배경과 흰색 숫자로 강조 |
| 오늘 상태 | 오늘 날짜를 기본 선택하고 필요 시 별도 표시 |
| 일정 존재 여부 | 날짜 안에 색상 일정 제목 칩을 최대 2개 표시하고, 나머지는 `+N`으로 요약 |

#### 사용자 행동

- 날짜 선택
- 선택한 날짜의 할일 목록과 예정 일정 확인

### 섹션 3: AI Mate 브리핑 카드

#### 역할

선택한 날짜의 핵심 행동을 가볍게 북돋아주고, 오늘 집중하면 좋은 항목을 짧게 알려준다.

#### 표시 정보

- AI Mate 라벨
- 격려 문구
- 선택한 날짜의 핵심 요약
- 마감이 가까운 항목 또는 먼저 시작하면 좋은 항목
- 추천 근거 한 줄

#### 표현 원칙

- 행동을 부드럽게 독려하는 톤을 사용한다.
- 정확한 완료 확률, 성과 예측, 점수 예측은 표시하지 않는다.
- 긴 설명은 넣지 않고 필요 시 AI Mate 패널에서 자세히 묻도록 한다.

### 섹션 4: 선택한 날짜의 할일

#### 정의

선택한 날짜의 할일은 AI가 앞으로 4주간의 일정, 난이도, 마감일, 우선순위, 예상 소요시간, Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정을 고려해 주간 계획에서 해당 날짜로 배치한 학습 항목이다.

#### 표시 정보

| 항목 | 설명 |
| --- | --- |
| 과목명 또는 자료 출처 | 어떤 수업 또는 자료에서 나온 일인지 표시 |
| 할일 제목 | 오늘 해야 할 학습 행동 |
| 예상 소요시간 | 계획 이해를 돕는 시간 정보 |
| 완료 여부 | 완료 체크 상태 |

#### 할일 유형

- 과제 진행
- 시험 공부
- 수업 준비
- 복습 또는 예습

#### 사용자 행동

- 선택한 날짜의 할일 확인
- 완료 체크
- 할 일 카드 본문을 눌러 AI Mate의 `할 일 추천이유`, `할 일 조정`, 날짜별 할 일 추가 Prompt Chips 열기
- Prompt Chip을 눌러 입력 초안을 채운 뒤 사용자가 직접 전송

#### 빈 상태

- 7일 Plan이 없으면 `아직 계획된 할 일이 없어요.`와 `계획 생성하기`를 표시한다.
- `계획 생성하기`는 AI Mate를 열고 `이번 주 계획을 생성해줘`를 입력하되 자동 전송하지 않는다.
- 선택한 날짜에 할 일이 없으면 `계획 조정`을 표시하고 `{M/D}일 계획을 조정해줘`를 입력하되 자동 전송하지 않는다.

### 섹션 5: 선택한 날짜의 예정 일정 요약

#### 정의

선택한 날짜의 예정 일정은 날짜 기준으로 해당 날짜에 예정된 일정이다. AI가 임의로 배치한 학습 항목이 아니라, 학업 자료에서 추출된 일정, Google Calendar 개인 일정 또는 CatchUp에서 직접 추가·수정한 개인 일정이다.

#### 표시 정보

| 항목 | 설명 |
| --- | --- |
| 일정명 | 마감, 제출, 시험, 개인 일정 제목 |
| 시간 | 오늘 중 예정 시간 또는 종일 |
| 유형 | 마감, 제출, 시험, 중요 공지, 개인 일정 |
| 출처 | 업로드 자료, Google Calendar 또는 CatchUp 직접 입력 |

#### 사용자 행동

- 선택한 날짜의 예정 일정 확인
- 일정 출처 확인
- `추가` 버튼으로 일정 추가
- 일정 카드를 눌러 상세 확인. Google Calendar 일정은 읽기 전용으로 표시하고 Upload 추출·CatchUp 직접 입력 일정만 현재 화면에서 수정

### 탭 1에서 제외하는 기능

- 주간 계획 생성 입력
- 계획 조정 입력
- 학업 자료 업로드
- 할일의 시간, 날짜, 우선순위, 내용 직접 수정

## 7. 탭 2: Month

### 목적

학업 일정과 Google Calendar 개인 일정, CatchUp에서 직접 추가·수정한 개인 일정을 월간 캘린더로 확인한다. 이번 주 요일별 할일 확인은 Today 탭의 날짜 선택 영역에서 처리한다.

### 화면 구성

```text
[월간 일정 요약 캘린더]

[선택 날짜 일정 상세 요약]

[플로팅 AI Mate 버튼]
```

### 섹션 1: 월간 일정 요약 캘린더

#### 역할

월간 단위로 학업 일정과 개인 일정을 확인한다. 월간 화면은 일정 확인용이며, 월간 세부 자동 계획을 생성하지 않는다.

#### 표시 정보

- AI가 학업 자료에서 추출한 마감일
- 제출일
- 시험일
- 중요 공지 일정
- Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정

#### 표시 규칙

- 날짜는 일반 달력처럼 숫자로 표시한다.
- 일정은 날짜 칸 안의 제목 칩으로 표시하고 넘치는 일정은 `+N`으로 축약한다.
- 정규 반복 수업 시간표는 Month selector에서 제외하고 Today의 예정 일정에는 유지한다.
- 단일 날짜 일정과 주차 Bar는 Calendar row마다 공통 3개 lane만 사용한다. Bar는 해당 row 밖이나 날짜 숫자·divider 영역을 침범하지 않는다.
- 3개 lane을 넘는 일정은 날짜별 `+N`으로 표시하고, 선택하면 기존 날짜 상세 팝업에서 전체 일정을 보여준다.
- 색상은 `#C7B9FA`, `#E9E0FF`, `#F8B1FB`, `#FEE8FF`, `#A5D1FF`, `#D9F0FF`만 사용한다.
- 같은 과목의 모든 일정은 같은 색상을 공유하고, 개인 일정은 하나의 공통 색상을 공유한다.
- 정확한 날짜 없이 예정 주차만 있는 학업 이벤트는 주차-날짜 기준이 확보된 경우 7개 이벤트를 복제하지 않고 해당 주 전체를 잇는 하나의 점선 스타일 `[이벤트명] 주` 바로 표시한다.
- 주차 기반 미확정 일정도 원본 AcademicEvent 상세 편집 대상으로 열 수 있지만, 선택한 날짜를 정확한 일정일로 자동 입력하지 않는다.

#### 사용자 행동

- 날짜 선택
- 선택한 날짜의 일정 상세 팝업 확인
- 일정 출처 확인
- 개인 일정 직접 추가 및 수정

### 섹션 2: 일정 상세 팝업

날짜를 선택하면 해당 날짜의 일정 상세 요약을 팝업으로 표시한다.

```text
7일
- 알고리즘 과제 제출 마감일
- Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정
```

#### 팝업 표시 정보

| 항목 | 설명 |
| --- | --- |
| 선택 날짜 | 사용자가 선택한 날짜 |
| 일정명 | 해당 날짜의 일정 제목 |
| 유형 | 마감, 제출, 시험, 중요 공지, 개인 일정 |
| 출처 | 업로드 자료 또는 Google Calendar |

### 탭 2에서 제외하는 기능

- 주간 계획 생성 입력
- 계획 조정 입력
- 학업 자료 업로드
- 주간 계획 항목의 시간, 날짜, 우선순위, 내용 직접 수정
- 전체 월간 세부 계획 생성

### 개인 일정 직접 추가 및 수정

- Month 탭에서 개인 일정의 제목, 날짜, 시작·종료 시간을 직접 입력해 추가하거나 수정할 수 있다.
- CatchUp에서 직접 추가·수정한 개인 일정은 Google Calendar 일정과 함께 Today, Month, AI Mate의 계획 생성 근거에 반영한다.
- 이 기능은 AI가 생성한 주간 계획 항목을 직접 수정하는 기능이 아니다.

## 8. 탭 3: Upload

### 목적

학업 자료를 업로드하고 AI가 추출한 정보를 직접 확인 및 수정한다. 업로드 자료와 수정된 추출 정보는 플로팅 AI Mate가 주간 계획을 생성할 때 사용하는 핵심 근거가 된다.

### 화면 구성

```text
[학업 자료 업로드]

[업로드한 자료 목록]

[학업 이벤트 Preview: 최대 4개]

[별도 학업 이벤트 확인 및 수정 페이지]

[플로팅 AI Mate 버튼]
```

### 섹션 1: 학업 자료 업로드

#### 표시 정보

- 업로드 영역
- 점선 테두리의 드롭 영역
- 업로드 아이콘
- 지원 파일 형식 안내
- 업로드 진행 상태
- 파일별 `PDF/IMG · 파일명 · 대기/추출중/추출 완료/추출 실패`를 작은 셀로 표시하고 파일명은 20자 이후 줄임 표시
- 기본 Preview는 최근 대기·추출중·실패 파일을 완료 파일보다 앞에 둔 가로 한 줄이며, 초과분은 `…`와 전체 compact 목록으로 확인
- 이미 추출 완료한 파일은 재분석하지 않고, 신규 파일만 기존 학업 이벤트 맥락과 함께 분석
- `?` 도움말에서 지속 추가 업로드와 기존 일정 보완/새 일정 추가 규칙 안내

#### 지원 범위

- MVP에서는 PDF와 이미지 파일 형식을 모두 지원한다.
- PDF와 이미지 외의 파일 형식은 지원하지 않는다.

#### 사용자 행동

- 강의계획서 업로드
- LMS 공지 업로드
- 과제 명세서 업로드
- 업로드 실패 시 재시도

### 섹션 2: 업로드한 자료 목록

#### 표시 정보

| 항목 | 설명 |
| --- | --- |
| 파일명 | 업로드한 학업 자료 이름 |
| 파일 형식 | PDF 또는 이미지 |
| 파일 크기 | 업로드 자료 크기 |
| 업로드 날짜 | 파일을 올린 날짜 |
| 추출 상태 | 추출 중, 추출 완료, 확인 필요, 추출 실패 |
| 확인 필요 여부 | 사용자가 검토해야 하는지 표시 |

#### 사용자 행동

- 업로드한 파일 확인
- 추출 상태 확인
- 확인 필요 파일 선택

### 섹션 3: AI 추출 결과 확인 및 수정

#### 역할

AI가 학업 자료에서 추출한 정보를 사용자에게 보여주고, 사용자가 틀린 부분을 직접 입력해 수정한다.

- Upload 본문에는 학업 이벤트를 최대 4개만 Preview로 표시하고, 더 있으면 `...`로 알린다.
- `학업 이벤트 N` 옆 화살표 또는 Preview 카드 어느 것을 선택해도 같은 확인 및 수정 페이지로 이동한다.
- 어떤 경로로 진입해도 모든 이벤트 상세 영역은 닫혀 있고 미리 선택된 이벤트는 없다.
- 기존의 별도 `이벤트 중심 결과 확인 및 수정` 카드는 사용하지 않는다.

#### 표시 정보

| 필드 | 설명 |
| --- | --- |
| 과제명 | 추출된 과제 이름 |
| 마감일 | 과제 또는 제출 마감일 |
| 시험일 | 시험 예정일 |
| 제출 방식 | 온라인 제출, LMS 제출 등 |
| 준비물 | 수업 또는 과제 준비물 |
| 중요 공지 | 놓치면 안 되는 공지 |
| 수업 일정 정보 | 수업 관련 일정 |
| 반복 수업 시간 | 시간표에서 추출한 요일, 시작·종료 시간, 강의실. 한 과목에 여러 수업 시간 가능 |

#### 상태

| 상태 | 설명 |
| --- | --- |
| 추출 중 | AI가 정보를 추출하는 중 |
| 추출 완료 | 사용자가 확인할 수 있는 상태 |
| 추출 실패 | 기술 요청 실패 또는 유효한 학업 정보가 0개인 상태 |
| 확인 필요 | 일부 값이 불확실하거나 누락된 상태 |
| 저장 완료 | 사용자가 수정한 정보가 반영된 상태 |
| 확정 | 정확한 날짜와 유형별 핵심 세부 정보가 충분한 상태 |
| 미확정 | 이벤트 존재는 확인했지만 핵심 정보가 부족한 상태 |

#### 사용자 행동

- 추출 결과 확인
- 틀린 값 직접 수정
- 누락된 값 직접 입력
- 확인 필요 항목 검토
- 수정 내용 저장
- Today/Month에서 개인·수업·확정·미확정·주차 기반 일정을 원본 데이터에 수정 또는 삭제
- Today/Month에서 AcademicEvent를 선택하면 Upload 검토 화면과 동일한 상세 편집기를 사용하고, 저장 결과를 하나의 원본 AcademicEvent에 반영
- 일정 유형은 원본값으로 고정하고 변경 selector를 표시하지 않음
- 시험의 정확한 날짜, 예정 주차 원문, 시험 유형, 범위를 구분해 확인 및 수정
- 시간표 과목의 반복 수업 시간을 추가·수정·삭제하고 저장
- 플로팅 AI Mate를 열어 업로드 자료 기반 주간 계획 생성 요청

#### 제외 사항

- AI Mate가 추출 정보를 대신 수정하는 흐름은 포함하지 않는다.
- 추출 결과 수정은 사용자의 직접 입력으로 처리한다.

## 9. 전역 플로팅 AI Mate

### 목적

사용자가 어떤 메인 화면에 있든 AI Mate를 열어 주간 계획 생성, 계획수립 요청사항 입력, 생성된 계획 조정, 추천 근거 질문을 할 수 있도록 한다.

### 화면 구성

```text
[플로팅 AI Mate 버튼]

버튼 선택 시:
[AI Mate 패널]
- 패널 헤더와 닫기 버튼
- 대화 영역
- 자연어 입력 영역
- 7일 Plan 기간과 생성 상태
- 계획 조정 잔여 횟수
```

### 플로팅 버튼

#### 표시 규칙

- 모든 메인 탭에서 표시한다.
- 하단 네비게이션 오른쪽 위 또는 화면 오른쪽 하단에 배치한다.
- 현재 콘텐츠와 하단 네비게이션을 가리지 않도록 배치한다.

#### 사용자 행동

- 버튼 선택
- AI Mate 패널 열기
- 패널 닫기

### AI Mate 패널

#### 표시 정보

| 항목 | 설명 |
| --- | --- |
| 패널 헤더 | `AI Mate` 이름, 조정 잔여 횟수, 닫기 버튼 표시 |
| 대화 영역 | AI와 사용자의 메시지 표시 |
| 메시지 시간 | AI와 사용자 말풍선 모두 표시하지 않음 |
| Prompt Chips | Today의 할 일 문맥에서 입력창 위에 표시하며 선택 시 초안만 입력 |
| 자연어 입력 영역 | 계획 생성 요청, 계획 조정 요청, 질문 입력 |
| Plan 시작일 | 사용자가 계획 생성을 요청한 날짜 |
| Plan 종료일 | Plan 시작일 + 6일 |
| 다음 생성 | 자동 생성하지 않으며 사용자가 원하는 날짜에 요청 |
| 이번 주 생성 여부 | 이번 주 계획 생성 완료 여부 |
| 조정 잔여 횟수 | 오늘의 AI Mate 계획 조정 가능 횟수 |

#### AI가 참고하는 정보

- 자료 업로드 화면에 업로드된 학업 자료
- 사용자가 직접 수정한 AI 추출 결과
- Google Calendar 개인 일정
- 완료한 일과 미완료 항목
- 사용자의 자연어 요청사항

#### 입력 가능 요청

- 주간 계획 생성 요청
- 계획수립 요청사항 입력
- 오늘 계획 조정 요청
- 이번 주 계획 조정 요청
- 추천 근거 질문
- 자동 조정 취소

#### 요청 예시

- "이번 주 계획 짜줘."
- "자료 업로드 화면에 올린 강의계획서랑 과제 명세서 기반으로 이번 주 계획 짜줘."
- "일요일에는 쉬는 시간을 많이 확보해줘."
- "오늘 할 일을 조금 줄여줘."
- "수요일에 개인 일정이 많으니 그날 할 일을 줄여줘."
- "왜 이 과제를 오늘 해야 해?"

#### 응답에 포함할 정보

- 생성 또는 조정된 계획 요약
- 반영한 사용자 요청사항
- 추천 근거
- 충돌 또는 위험도 안내
- 오늘 남은 계획 조정 횟수

### 제한 상태

| 상태 | 화면 동작 |
| --- | --- |
| 7일 Plan 생성 전 | 사용자가 원하는 날짜에 계획 생성 요청 가능 |
| 7일 Plan 생성 완료 | 현재 Plan 기간과 미완료 과제 이월 상태 표시 |
| 조정 잔여 1~10회 | AI Mate 계획 조정 요청 가능 |
| 조정 잔여 0회 | 추가 조정 요청 차단, 다음 날 재사용 가능 안내 |

새 확정 이벤트에 사용자 수준 정보가 필요하면 플로팅 말풍선에 `질문이 있어요!`를 표시한다. 답변이 완료되면 별도 승인 칩 없이 자동 조정하고, 실제 변경 근거·추가·삭제·이동 결과를 대화에 남긴다. 성공한 자동 조정만 10회 한도에 합산하며 최근 자동 조정은 추가 차감 없이 취소한다.

### 표현 제한

- 정확한 완료 확률 수치는 표시하지 않는다.
- `우선순위 높음`, `완료 위험도 높음`처럼 정성적 표현을 사용한다.

## 10. 주요 데이터 표시 구분

### 선택한 날짜의 할일과 예정 일정 구분

| 구분 | 선택한 날짜의 할일 | 선택한 날짜의 예정 일정 |
| --- | --- | --- |
| 기준 | AI가 선택한 날짜로 배치한 학습 행동 | 날짜 기준으로 선택한 날짜에 예정된 일정 |
| 생성 방식 | 주간 계획 생성 결과 | 학업 자료 추출, Google Calendar 또는 CatchUp 직접 입력 |
| 예시 | 과제 2시간 진행, 시험 공부 40분 | 과제 마감, 시험, 개인 약속 |
| 사용자 행동 | 완료 체크, AI Mate에 계획 조정 요청 | 일정 확인, 출처 확인 |

### 학업 일정과 개인 일정 구분

| 구분 | 학업 일정 | 개인 일정 |
| --- | --- | --- |
| 출처 | 업로드한 학업 자료 | Google Calendar 또는 CatchUp 직접 입력 |
| 예시 | 마감일, 제출일, 시험일, 중요 공지 | 병원, 약속, 아르바이트, 개인 일정 |
| 수정 위치 | Upload·Today·Month가 공유하는 AcademicEvent 상세 편집기 | Google 일정은 Google Calendar 원본에서 수정, CatchUp 직접 입력 일정은 Today·Month에서 수정 |

## 11. MVP 제외 화면 및 기능

- AI Mate를 하단 네비게이션의 개별 탭으로 제공하는 화면
- 네 번째 메인 탭
- 별도 월간 상세 계획 페이지
- 모든 LMS 자동 연동 화면
- Google Calendar 외 캘린더 연결 화면
- 친구와 공동 일정 관리 화면
- 장학금, 근로, 대외활동 추천 화면
- 정확한 완료 확률을 보여주는 예측 화면
- Today 할일 또는 주간 계획 항목을 사용자가 직접 수정하는 화면
- 자동 생성 스케줄을 설정하거나 표시하는 화면
- AI Mate 계획 조정 요청을 하루에 10번 넘게 허용하는 화면

## 12. 초기 구현 이후 추가 검토 기능

- 일정 요약 캘린더에서 카테고리별 색상으로 학업 일정과 개인 일정을 구분하는 기능
- 일정 요약 캘린더에서 확인 필요 일정을 일반 일정과 다르게 표시하는 기능
- Google Calendar 외 다른 외부 캘린더 연동
- 개인 시간표 직접 연동
