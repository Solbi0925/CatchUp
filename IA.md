# CatchUp IA

## 1. IA 목표

CatchUp의 MVP 정보 구조는 Google Calendar를 사용하는 사용자가 학업 자료와 Google Calendar 개인 일정, CatchUp에서 직접 추가·수정한 개인 일정, 기존 미완료 과제를 바탕으로 현재부터 7일 동안 해야 할 학습 항목을 확인하도록 구성한다. Today는 이 Plan 기간과 분리된 월~일 Calendar Week 단위로 표시한다.

이번 MVP는 발표용 노트북의 localhost에서만 동작한다. `frontend/`는 화면과 필요한 Local Storage를, `backend/` Local Backend/Bridge는 파일 임시 처리와 `codex exec` 결과 반환을 담당한다. Google Calendar 실제 OAuth 연결은 핵심 Upload·AI·계획 흐름을 완성한 뒤 localhost에서 진행한다.

AI Mate는 하단 네비게이션의 개별 탭으로 두지 않고, 어떤 화면에 있어도 접근할 수 있는 전역 플로팅 탭으로 제공한다. 사용자는 Upload 화면에 올린 학업 자료와 일정, 기존 미완료 과제를 바탕으로 원하는 날짜에 AI Mate에게 7일 Plan 생성을 요청하고, 계획 생성 시 요청사항을 자연어로 입력할 수 있다. 계획 생성 후 수정하고 싶은 부분이 있으면 같은 플로팅 AI Mate를 통해 계획 조정을 요청한다.

## 2. 전체 구조

| 구분 | 화면 이름 | 핵심 목적 |
| --- | --- | --- |
| 온보딩 | 브랜드 인트로 | Catch Up 워드마크와 포인트 컬러 그라디언트로 서비스 무드를 전달한다. |
| 온보딩 | Google Calendar 연동 | 개인 일정을 불러와 학업 계획 생성에 반영할 수 있도록 연결한다. |
| 메인 탭 1 | Today | 실제 오늘이 포함된 월~일 Calendar Week에서 날짜별 할 일을 빠르게 확인한다. |
| 메인 탭 2 | Month | 학업 일정과 개인 일정을 월간 캘린더로 확인한다. |
| 메인 탭 3 | Upload | 학업 자료를 업로드하고 AI 추출 결과를 확인 및 수정한다. |
| 전역 플로팅 | AI Mate | 어떤 화면에서든 7일 Plan 생성, 계획 조정, 추천 근거 질문을 처리한다. |

## 3. 이동 방식

### 3.1 첫 진입 흐름

```text
온보딩
-> Google Calendar 연동
-> 메인 앱
-> Today
```

- 첫 사용자는 온보딩을 본 뒤 Google Calendar 연동 화면으로 이동한다.
- 온보딩은 워드마크 중심의 짧은 브랜드 인트로이며, 일정 시간 노출 후 Google Calendar 연동 화면으로 자동 전환한다.
- Google Calendar 연동을 완료하면 메인 앱으로 진입한다. 핵심 기능 완성 전에는 Mock 연결 상태로 같은 화면 흐름을 검증한다.
- 메인 앱의 기본 진입 탭은 `Today`이다.

### 3.2 메인 네비게이션

메인 앱은 하단 네비게이션 바를 사용한다.

```text
CatchUp
├─ Today
├─ Month
└─ Upload
```

- MVP에서는 하단 네비게이션의 3개 탭을 핵심 구조로 사용한다.
- `AI Mate`는 하단 네비게이션 탭이 아니라 전역 플로팅 탭으로 제공한다.
- 플로팅 AI Mate는 `Today`, `Month`, `Upload` 화면 어디에서든 열 수 있다.
- 이전 IA의 가로 스크롤 페이지 이동은 사용하지 않는다.
- 각 탭은 독립된 주요 목적을 가지며, MVP 범위를 벗어나는 별도 페이지는 만들지 않는다.

### 3.3 전역 플로팅 AI Mate

```text
모든 메인 화면
└─ 플로팅 AI Mate 버튼
   └─ AI Mate 패널
```

- 플로팅 버튼은 메인 앱의 모든 탭 위에 항상 접근 가능하게 둔다.
- 사용자가 버튼을 누르면 AI Mate 패널이 열린다.
- AI Mate 패널은 현재 화면 맥락과 사용자가 업로드한 자료, Google Calendar 일정, 생성된 주간 계획을 함께 참고한다.
- 주간 계획 생성, 계획수립 요청사항 입력, 계획 생성 후 수정 요청, 추천 근거 질문은 모두 플로팅 AI Mate에서 처리한다.

## 4. 온보딩 IA

### 4.1 브랜드 인트로

#### 목적

사용자가 CatchUp의 첫인상과 서비스 무드를 빠르게 인지한 뒤 Google Calendar 연동으로 자연스럽게 이동하도록 한다.

#### 포함 정보

- 워드마크: Catch Up
- 포인트 컬러 그라디언트 배경
- 짧은 한 줄 설명
  - 오늘과 이번 주 학업 계획을 한눈에
- 은은한 별빛 또는 빛 번짐 같은 보조 그래픽
- 자동 전환 상태

#### 주요 행동

- 서비스 핵심 가치 확인
- 짧은 브랜드 인트로 확인
- Google Calendar 연동 화면으로 자동 이동

#### 표기 원칙

- 프로젝트명과 문서명은 기존처럼 `CatchUp`을 사용한다.
- 온보딩 워드마크에서는 시각적 가독성을 위해 `Catch Up` 표기를 사용할 수 있다.

### 4.2 Google Calendar 연동 화면

#### 목적

사용자의 개인 일정을 불러와 학업 일정과 함께 계획 생성에 반영할 수 있도록 한다.

#### 포함 정보

- Google Calendar 연동 안내
- 연동 필요 이유
  - 개인 일정과 학업 일정 충돌 방지
  - 오늘의 일정 확인
  - 주간 계획 생성 시 개인 일정 반영
- Google Calendar 연결 버튼
- 연결 상태
  - 연결 전
  - 연결 중
  - 연결 완료
  - 연결 실패
- 연결 실패 시 재시도 안내

#### 주요 행동

- Google Calendar 연결
- 연결 권한 승인
- 연결 완료 후 메인 앱 진입

#### MVP 제한

- Google Calendar 외 다른 외부 캘린더는 연결하지 않는다.
- 개인 시간표 직접 연동은 포함하지 않는다.
- CatchUp 안에서 개인 일정을 직접 추가하고 수정할 수 있다. 이 기능은 주간 계획 항목을 직접 수정하는 기능과 별개다.
- 실제 민감 일정은 저장하지 않고, 필요한 일정 정보만 계획 생성에 사용한다.

## 5. 메인 탭별 IA

### 5.1 탭 1: Today

#### 목적

사용자가 앱을 열었을 때 이번 주 날짜를 선택하며 날짜별 할일을 확인하고, 기본값으로 오늘 해야 할 학습 항목을 빠르게 파악하도록 한다.

#### 포함 정보

- 월~일 Calendar Week 날짜 선택 영역
  - 계획 생성 요일과 관계없이 월요일부터 일요일까지 가로로 배치
  - Today 진입 시 실제 오늘이 포함된 주차와 오늘 날짜를 기본 선택
  - 이전 주/다음 주 이동
  - 현재 7일 Plan 범위 밖 날짜는 날짜 원(circle)을 회색 표시
  - 선택한 날짜의 할일 목록으로 전환
- AI Mate 브리핑 카드
  - 선택한 날짜의 핵심 할일을 가볍게 북돋아주는 문구
  - 마감, 개인 일정, 우선순위에 대한 짧은 요약
- 선택한 날짜의 할일 목록
  - AI가 향후 약 4주간의 일정, 난이도, 마감일, 중요도·우선순위, 예상 소요시간, 기존 미완료 과제, Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정을 고려해 7일 Plan에 배치한 학습 항목
  - 과제 진행
  - 시험 공부
  - 수업 준비
  - 복습 또는 예습
- 각 할일의 기본 정보
  - 과목명 또는 자료 출처
  - 할일 제목
  - 예상 소요시간
  - 완료 여부
- 선택한 날짜의 예정 일정 요약
  - 해당 날짜가 마감인 과제
  - 해당 날짜에 제출해야 하는 항목
  - 해당 날짜에 예정된 시험
  - Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정
- 플로팅 AI Mate 버튼
- 7일 Plan 미생성 상태의 `계획 생성하기` 버튼
- 할 일 카드 선택 시 표시되는 `할 일 추천이유`, `할 일 조정`, 날짜별 할 일 추가 Prompt Chips
- 할 일이 없는 날짜의 `계획 조정` 버튼

#### 주요 행동

- 현재 Calendar Week 날짜 선택 및 이전 주/다음 주 이동
- 선택한 날짜의 할일 확인
- 선택한 날짜의 예정 일정 요약 확인
- 완료 체크
- 할 일 카드를 눌러 AI Mate의 추천 이유·조정·추가 Prompt Chips 확인
- 주간 계획 미생성 또는 빈 날짜의 문맥형 버튼으로 AI Mate 입력 초안 열기
- 예정 일정 카드를 눌러 출처와 관계없이 현재 화면에서 일정 수정

#### MVP 제한

- 사용자가 선택한 날짜의 할일 시간, 날짜, 우선순위, 내용을 직접 수정하는 기능은 초기 구현에서 제외한다.
- 7일 Plan 생성 또는 조정의 실제 전송은 AI Mate에서만 처리한다. Today의 버튼과 Prompt Chips는 입력 초안만 채우며 자동 전송하지 않는다.

### 5.2 탭 2: Month

#### 목적

학업 중요 일정과 Google Calendar 개인 일정, CatchUp에서 직접 추가·수정한 개인 일정을 월간 캘린더로 확인하도록 한다. 이번 주 요일별 계획 확인은 Today 탭의 주간 날짜 선택 영역에서 처리한다.

#### 포함 정보

- 월간 일정 요약 캘린더
  - 일반 달력처럼 날짜를 숫자로 표시
  - 일정이 있는 날짜는 일정 제목 칩으로 표시하고 넘치는 일정은 `+N`으로 축약
  - 지정된 6색 안에서 과목별 공통 색상과 개인 일정 공통 색상으로 구분
  - AI가 학업 자료에서 추출한 마감일
  - 제출일
  - 시험일
  - 중요 공지 일정
  - Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정
  - 날짜 선택 시 일정 상세와 추가·수정 화면 표시
- 일정 상세 팝업
  - 선택 날짜
  - 일정명
  - 유형: 마감, 제출, 시험, 중요 공지, 개인 일정
  - 출처: 업로드 자료, Google Calendar 또는 CatchUp 직접 입력
- 플로팅 AI Mate 버튼

#### 주요 행동

- 월간 캘린더에서 일정이 있는 날짜 확인
- 날짜를 선택해 해당 날짜의 일정 상세 팝업 확인
- Upload 추출 일정, Google Calendar 일정, CatchUp 직접 입력 일정 추가 및 수정
- 계획 변경이 필요하면 플로팅 AI Mate를 열어 조정 요청

#### MVP 제한

- 월간 화면은 일정 확인용 캘린더이며, 전체 월간 계획을 세부 단위까지 자동 생성하지 않는다.
- Google Calendar 외 다른 외부 캘린더는 연결하지 않는다.
- 사용자가 주간 계획 항목의 시간, 날짜, 우선순위, 내용을 직접 수정하는 기능은 MVP에서 제외한다.
- 원본 일정은 출처와 관계없이 일정 편집창에서 수정할 수 있지만, AI가 만든 주간 계획 항목은 직접 수정하지 않고 AI Mate로만 변경한다.
- 확인 필요 일정을 일반 일정과 다르게 표시하는 기능은 초기 구현에서 제외한다.

### 5.3 탭 3: Upload

#### 목적

사용자가 강의계획서, LMS 공지, 과제 명세서 등 학업 자료를 업로드하고, AI가 추출한 정보를 직접 확인 및 수정하도록 한다.

업로드한 자료와 사용자가 수정한 추출 정보는 플로팅 AI Mate가 주간 계획을 생성하거나 계획수립 요청사항을 반영할 때 사용하는 핵심 근거가 된다.

#### 포함 정보

- 학업 자료 업로드 영역
  - MVP에서는 PDF와 이미지 파일 형식을 모두 지원
  - 지원 파일 형식과 업로드 가능 상태를 명확히 표시
  - 강의계획서
  - LMS 공지
  - 과제 명세서
- 업로드한 자료 목록
  - 파일명
  - 업로드 날짜
  - 추출 상태
  - 확인 필요 여부
- AI 추출 결과 확인 및 수정 영역
  - AI가 추출을 완료한 뒤 추출 정보를 사용자에게 표시
  - 사용자가 틀린 부분을 직접 입력해 수정
  - 확인 필요 항목 표시
  - 과제명
  - 마감일
  - 시험일
  - 제출 방식
  - 준비물
  - 중요 공지
  - 수업 일정 정보
- 플로팅 AI Mate 버튼

#### 주요 행동

- 학업 자료 업로드
- 업로드한 자료 목록 확인
- AI 추출 정보 확인
- 잘못 추출된 정보 직접 수정
- 확인 필요 항목 검토
- 수정된 추출 정보 저장
- 업로드한 자료를 바탕으로 플로팅 AI Mate에 주간 계획 생성 요청
- 플로팅 AI Mate에 계획수립 요청사항 입력

#### MVP 제한

- PDF와 이미지 외의 파일 형식은 지원하지 않는다.
- 모든 LMS와 학교 시스템을 자동 연동하지 않는다.
- 추출 정보 수정은 AI Mate가 대신 처리하지 않고, 사용자가 직접 입력해 수정한다.

## 6. 전역 플로팅 AI Mate IA

### 목적

사용자가 어떤 화면에 있든 AI Mate를 열어 7일 Plan 생성, 계획수립 요청사항 입력, 생성된 계획 조정, 추천 근거 질문을 할 수 있도록 한다.

### 포함 정보

- 플로팅 AI Mate 버튼
  - 모든 메인 탭에서 접근 가능
  - 하단 네비게이션 오른쪽 위 또는 화면 오른쪽 하단에 배치
- AI Mate 패널
  - 화면 하단에서 올라오는 오버레이 형태
  - 대화 영역
  - 자연어 입력 영역
  - 닫기 버튼
  - 7일 Plan 생성 상태와 기간
  - 오늘의 AI Mate 계획 조정 잔여 횟수
- 7일 Plan 생성 정보
  - 마지막 생성 요청일(`planStartDate`)
  - 현재 Plan 종료일(`planStartDate + 6일`)
  - 다음 계획 생성을 요청할 수 있는 상태
- AI가 참고하는 정보
  - 자료 업로드 화면에 업로드된 학업 자료
  - 사용자가 직접 수정한 AI 추출 결과
  - Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정
  - 완료한 일과 기존 미완료 항목
  - 사용자의 자연어 요청사항

### 주요 행동

- 계획 생성 요청일 기준 7일 Plan 생성 요청
- 업로드 자료 기반 7일 Plan 생성 요청
- 계획 생성 시 자연어 요청사항 입력
- 생성된 계획에서 수정하고 싶은 부분을 자연어로 요청
- 오늘 또는 이번 주 계획 조정 요청
- 추천 이유 질문
- 오늘의 계획 조정 잔여 횟수 확인

### 계획 생성 요청 예시

- "오늘부터 7일 계획 짜줘."
- "자료 업로드 화면에 올린 강의계획서랑 과제 명세서 기반으로 지금부터 7일 계획 짜줘."
- "일요일에는 쉬는 시간을 많이 확보해줘."
- "매일 15분 이상 영어 공부를 넣어줘."
- "팀플 회의가 많으니 과제는 짧게 나눠서 배치해줘."

### 계획 조정 요청 예시

- "오늘 할 일을 조금 줄여줘."
- "마감이 가까운 과제부터 하고 싶어."
- "시험 공부 시간을 늘려줘."
- "수요일에 개인 일정이 많으니 그날 할 일을 줄여줘."
- "이 계획에서 알고리즘 과제 시간을 더 앞당겨줘."

### 추천 근거 요소

- 마감일까지 남은 기간
- 예상 소요시간
- 중요도
- Google Calendar 개인 일정과의 충돌 여부
- 시험과의 연관성

### MVP 제한

- AI Mate는 하단 네비게이션의 개별 탭으로 만들지 않는다.
- Plan은 사용자가 요청한 날짜부터 정확히 7일이다. 자동으로 다음 계획을 생성하지 않는다.
- Today의 Calendar Week는 Plan과 분리된 월요일~일요일 단위다.
- Plan 범위가 두 Calendar Week에 걸치면 Today에서 이전 주·다음 주로 이동해 모두 확인할 수 있다.
- AI Mate를 통한 일일/주간 계획 조정 요청은 하루에 최대 10번만 가능하다.
- 사용자가 직접 계획 항목을 손으로 수정하는 기능은 MVP에서 제외한다. 계획의 생성과 수정은 AI Mate로만 처리한다.
- 정확한 완료 확률 같은 수치형 예측은 제공하지 않는다.

## 7. 초기 구현 이후 추가 검토 기능

아래 기능은 있으면 좋지만 10주 MVP 초기 구현 범위에서는 제외하고, 시간이 남을 때 추가로 검토한다.

- 일정 요약 캘린더에서 확인 필요 일정을 일반 일정과 다르게 표시하는 기능
- 자동 생성 스케줄 기능
- AI Mate 계획 조정 요청을 하루에 10번 넘게 허용하는 기능
- Google Calendar 외 다른 외부 캘린더 연동
- 개인 시간표 직접 연동

## 8. MVP 범위 자체 점검

| 점검 항목 | 결과 | 근거 |
| --- | --- | --- |
| 온보딩과 Google Calendar 연동 흐름이 있는가? | 적합 | 첫 진입 흐름을 온보딩 -> Google Calendar 연동 -> 메인 앱으로 정의했다. |
| 메인 네비게이션이 3개 탭으로 정리되었는가? | 적합 | Today, Month, Upload 3개 탭으로 정의했다. |
| AI Mate가 개별 탭이 아닌 플로팅 탭인가? | 적합 | 모든 메인 화면에서 접근 가능한 전역 플로팅 AI Mate로 정의했다. |
| 각 탭의 목적과 포함 정보가 있는가? | 적합 | 모든 탭에 목적, 포함 정보, 주요 행동, MVP 제한을 명시했다. |
| 자료 업로드와 AI Mate의 연결이 명확한가? | 적합 | 업로드한 자료와 수정된 추출 정보를 AI Mate가 주간 계획 생성의 근거로 사용한다고 명시했다. |
| PRD의 MVP 핵심 기능을 포함하는가? | 적합 | 자료 업로드, AI 정보 추출, Google Calendar 기반 7일 Plan, Plan/Calendar Week 분리, 자연어 요구사항, 챗봇 기반 계획 조정, 추천 근거를 포함했다. |
| MVP 제외 기능이 들어갔는가? | 적합 | 다중 캘린더, 모든 LMS 자동 연동, 전체 월간 세부 계획, 수치형 완료 확률, 공동 일정 관리, 사용자의 직접 계획 수정, 주간 계획 다중 생성 등은 제외했다. |
| 월간 일정이 MVP 범위를 넘지 않는가? | 적합 | 월간은 일정 확인용 캘린더로만 정의하고, 월간 세부 자동 계획 생성은 제외했다. |

## 9. 결론

이 IA는 기존 MVP 범위를 유지하면서 화면 구조를 `브랜드 인트로 -> Google Calendar 연동 -> 하단 네비게이션 3개 탭 + 전역 플로팅 AI Mate`로 재정리했다. 기능은 PDF·이미지 학업 자료 업로드, AI 일정 추출, Google Calendar와 CatchUp 직접 입력 개인 일정 기반 Today/Month 확인, 요청일 기준 7일 Plan 생성, 하루 10회 AI Mate 기반 계획 조정으로 제한했으며, MVP 범위를 벗어나는 장기 확장 기능은 포함하지 않았다.
