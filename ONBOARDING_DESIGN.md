# CatchUp Google Calendar 연동 화면 디자인 명세

## 1. 문서 목적

이 문서는 브랜드 인트로 직후 나타나는 Google Calendar 연동 화면을 개발자가 목업 이미지 없이 구현할 수 있도록 정의한다. 기본 화면뿐 아니라 OAuth 연결 중, 연결 성공, 연결 실패, 연결 미루기, 재진입 상태와 접근성 요구사항까지 포함한다.

이 문서는 `PRD.md`, `README.md`, `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`를 기준으로 하며, 충돌 시 `PRD.md`를 우선한다.

## 2. 화면 역할과 MVP 범위

- 화면 이름: Google Calendar 연동
- 권장 경로: `/onboarding/calendar`
- 이전 화면: `/onboarding/intro`
- 다음 화면: 메인 앱의 `Today`
- 목적: 사용자의 개인 일정과 수업 시간을 불러와 학업 계획에 함께 반영할 수 있음을 설명하고 Google Calendar OAuth 연결을 시작한다.
- 연결 대상: Google Calendar만 지원한다.
- 제외 대상: 다른 외부 캘린더, 개인 시간표 직접 연동, LMS 완전 자동 연동
- 사용자는 연결을 미룰 수 있다.
- 연결을 미룬 사용자는 메인 앱을 둘러볼 수 있지만, Google Calendar 정보가 필요한 주간 계획 생성 전에는 연결을 다시 안내받아야 한다.

`나중에 할게요` 동작은 목업에는 있으나 기존 첫 사용 흐름 문서에는 명시가 약하다. 따라서 MVP 정합성을 위해 “메인 진입은 허용하되 Google Calendar 기반 계획 생성은 연결 전까지 제한”하는 방식으로 해석한다.

## 3. 화면 카피

### 상단

```text
Google Calendar 연결

개인 일정과 수업 시간을
함께 반영해요
```

### 카드 혜택 목록

```text
개인 일정과 수업 시간을 같은 계획에 반영
충돌 없는 일정으로 하루 만들기
언제든 연결 해제 가능
```

### 행동

```text
캘린더 연결하기
나중에 할게요
```

카피는 목업과 동일하게 유지하며, `구글 캘린더`로 임의 번역하지 않는다.

## 4. 목업 해석 기준

- 기준 뷰포트: `393 × 852 CSS px`
- 목업의 기기 프레임, Dynamic Island, 상태바, 홈 인디케이터는 실제 웹 UI가 아니다.
- 앱은 `100dvh`를 채우고 상하단 safe area만 반영한다.
- 화면은 모바일 우선으로 설계하되 PRD에 따라 데스크톱 웹에서도 사용할 수 있어야 한다.
- 기준 높이에서는 스크롤 없이 전체 내용이 보여야 한다.
- 높이가 짧거나 글자가 확대되면 세로 스크롤을 허용한다.

## 5. 화면 구조

```text
CalendarOnboardingPage
├─ BackgroundLayer
│  ├─ BaseGradient
│  └─ SoftLightBlobs
└─ Content
   ├─ Header
   │  ├─ Title
   │  └─ Subtitle
   └─ ConnectionCard
      ├─ Illustration
      │  ├─ GoogleCalendarLogo
      │  ├─ CalendarOutline
      │  ├─ ConnectorCurve
      │  └─ LinkBadge
      ├─ BenefitList
      │  ├─ BenefitItem
      │  ├─ BenefitItem
      │  └─ BenefitItem
      ├─ ConnectButton
      ├─ StatusMessage
      └─ SkipButton
```

권장 시맨틱 구조:

```html
<main aria-labelledby="calendar-onboarding-title">
  <header>
    <h1 id="calendar-onboarding-title">Google Calendar 연결</h1>
    <p>개인 일정과 수업 시간을<br />함께 반영해요</p>
  </header>
  <section aria-label="Google Calendar 연결 안내">
    <div aria-hidden="true"><!-- illustration asset --></div>
    <ul><!-- benefits --></ul>
    <button type="button">캘린더 연결하기</button>
    <p role="status" aria-live="polite"></p>
    <button type="button">나중에 할게요</button>
  </section>
</main>
```

## 6. 전체 레이아웃

### 6.1 페이지

| 속성 | 값 |
| --- | --- |
| 너비 | `100vw` |
| 최소 높이 | `100dvh` |
| 기준 크기 | `393 × 852px` |
| 좌우 페이지 패딩 | `24px` |
| 상단 패딩 | `max(76px, env(safe-area-inset-top) + 40px)` |
| 하단 패딩 | `max(24px, env(safe-area-inset-bottom) + 12px)` |
| 정렬 | 가로 중앙 |
| 세로 overflow | `auto` |

배경은 첫 화면과 같은 라벤더 계열이지만 텍스트와 카드가 읽히도록 첫 화면보다 채도를 낮춘다.

### 6.2 상단 헤더

| 항목 | 기준값 |
| --- | --- |
| 최대 너비 | `345px` |
| 제목 상단 위치 | 약 `118px` |
| 제목과 부제 사이 | `16px` |
| 부제와 카드 사이 | `28px` |
| 정렬 | 중앙 |

헤더가 카드와 별도 영역으로 읽히되 큰 여백으로 분리되지 않게 한다.

### 6.3 연결 카드

| 속성 | 값 |
| --- | --- |
| 너비 | `min(100%, 325px)` |
| 최소 높이 | 약 `500px` |
| 내부 패딩 | `38px 18px 24px` |
| 모서리 반경 | `28px` |
| 배경 | `rgba(255,255,255,.72)` |
| 테두리 | `1px solid rgba(255,255,255,.78)` |
| backdrop blur | `24px` |
| shadow | `0 18px 42px rgba(111, 80, 188, .10)` |
| 정렬 | 가로 중앙 |

카드는 바깥 배경과 구분되지만 불투명한 흰색 박스처럼 무겁게 보이면 안 된다.

## 7. 배경 스타일

### 색상 토큰

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--onboarding-bg` | `#F7F4FF` | 페이지 기본 배경 |
| `--onboarding-lavender` | `#E8DFFF` | 배경 보라 번짐 |
| `--onboarding-card` | `rgba(255,255,255,.72)` | 카드 |
| `--onboarding-title` | `#111016` | 제목 |
| `--onboarding-body` | `#656071` | 부제 |
| `--onboarding-text` | `#24212A` | 목록 |
| `--onboarding-purple` | `#8E68FF` | 아이콘 |
| `--onboarding-purple-dark` | `#7B55F6` | CTA 끝색 |
| `--onboarding-purple-light` | `#AD86FF` | CTA 시작색 |
| `--onboarding-error` | `#B42318` | 오류 메시지 |
| `--onboarding-focus` | `#5B35D5` | focus ring |

권장 배경:

```css
background:
  radial-gradient(70% 40% at 18% 8%, rgba(255,255,255,.96) 0%, transparent 74%),
  radial-gradient(68% 54% at 92% 78%, rgba(211,195,255,.48) 0%, transparent 78%),
  linear-gradient(145deg, #FAF8FF 0%, #F4EFFF 55%, #EAE1FF 100%);
```

## 8. 타이포그래피

공통 폰트:

```css
font-family:
  "Pretendard Variable",
  Pretendard,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  sans-serif;
```

### 제목

| 속성 | 값 |
| --- | --- |
| 텍스트 | `Google Calendar 연결` |
| 크기 | `28px` |
| 행간 | `1.25` 또는 `35px` |
| 굵기 | `750~800` |
| 자간 | `-0.035em` |
| 색상 | `#111016` |
| 줄바꿈 | 기본적으로 금지 |

너비 `340px` 이하에서는 `26px`까지 줄여 한 줄을 우선 유지한다.

### 부제

| 속성 | 값 |
| --- | --- |
| 크기 | `16px` |
| 행간 | `1.55` 또는 `25px` |
| 굵기 | `450~500` |
| 자간 | `-0.015em` |
| 색상 | `#656071` |
| 정렬 | 중앙 |

### 혜택 문구

| 속성 | 값 |
| --- | --- |
| 크기 | `15px` |
| 행간 | `22px` |
| 굵기 | `500` |
| 자간 | `-0.02em` |
| 색상 | `#24212A` |

### CTA

| 속성 | 값 |
| --- | --- |
| 크기 | `18px` |
| 행간 | `24px` |
| 굵기 | `600` |
| 색상 | `#FFFFFF` |

### 보조 행동

| 속성 | 값 |
| --- | --- |
| 크기 | `16px` |
| 행간 | `24px` |
| 굵기 | `500` |
| 색상 | `#686371` |

## 9. 상단 일러스트레이션

### 9.1 전체 영역

| 속성 | 값 |
| --- | --- |
| 카드 내부 너비 | 약 `260px` |
| 높이 | 약 `210px` |
| 카드 상단과 간격 | `8px` |
| 혜택 목록과 간격 | `18px` |

일러스트는 단일 검증된 SVG 또는 이미지 에셋으로 제공하는 것을 권장한다. 이모지, 텍스트 문자, 임시 placeholder, 손으로 만든 저품질 CSS 도형으로 대체하지 않는다.

### 9.2 구성 설명

개발자가 에셋을 준비할 수 있도록 시각 구성을 아래처럼 정의한다.

- 좌측 상단: 공식 Google Calendar 앱 로고
  - 약 `112 × 112px`
  - Google의 파랑, 빨강, 노랑, 초록 색상을 그대로 사용
  - 흰색 중앙에 파란색 숫자 `31`
  - 비율을 변형하거나 단색 보라색으로 바꾸지 않는다.
- 우측: 연한 라벤더색 달력 윤곽
  - 약 `122 × 104px`
  - 둥근 사각형 프레임과 5열의 작은 날짜 셀
  - 중앙 셀 하나만 진한 보라색으로 강조
- 하단: Google Calendar 로고에서 우측 달력으로 이어지는 연보라색 곡선
  - 연결 관계를 표현하는 부드러운 U자 형태
- 우측 하단: 보라색 원형 링크 배지
  - 약 `68 × 68px`
  - 밝은 보라색 radial gradient
  - 흰색 또는 매우 옅은 라벤더색 링크 아이콘
  - 아래 방향의 부드러운 shadow
- 달력 위: 작은 라벤더 별빛 1개

공식 Google Calendar 로고는 Google 브랜드 가이드에 맞는 원본 에셋을 사용한다.

## 10. 혜택 목록

### 10.1 공통

| 속성 | 값 |
| --- | --- |
| 목록 너비 | `100%` |
| 좌우 여백 | 약 `0~8px` |
| 항목 사이 | `18px` |
| 아이콘과 텍스트 사이 | `14px` |
| 아이콘 크기 | `24px` |
| 최소 행 높이 | `28px` |

각 항목은 `grid-template-columns: 28px 1fr` 구조를 권장한다. 텍스트가 두 줄이 되면 아이콘은 첫 줄의 중앙에 맞춘다.

### 10.2 항목별 아이콘

| 순서 | 문구 | 아이콘 의미 |
| --- | --- | --- |
| 1 | 개인 일정과 수업 시간을 같은 계획에 반영 | 테두리형 작은 달력 |
| 2 | 충돌 없는 일정으로 하루 만들기 | 테두리형 시계 |
| 3 | 언제든 연결 해제 가능 | 체크가 들어간 방패 |

- 아이콘은 동일한 선 두께와 `#8E68FF` 색상을 사용한다.
- 가장 가까운 아이콘 라이브러리의 `CalendarDays`, `Clock3`, `ShieldCheck` 계열을 사용할 수 있다.
- 아이콘은 장식용이므로 중복 낭독을 막기 위해 `aria-hidden="true"`로 처리한다.
- 목록의 의미는 아이콘이 아니라 문구만으로도 전달되어야 한다.

## 11. 버튼

### 11.1 캘린더 연결하기

| 속성 | 값 |
| --- | --- |
| 너비 | `100%` |
| 높이 | `66px` |
| 상단 여백 | `32px` |
| 좌우 카드 inset | `0px` 또는 카드 콘텐츠 기준 |
| 모서리 반경 | `20px` |
| 배경 | `linear-gradient(110deg, #AD86FF 0%, #7B55F6 100%)` |
| shadow | `0 8px 18px rgba(119, 82, 238, .24)` |
| 텍스트 | 흰색 |
| 최소 터치 영역 | `44 × 44px` 이상 |

상태별 스타일:

| 상태 | 동작 및 스타일 |
| --- | --- |
| 기본 | 위 그라디언트와 흰색 텍스트 |
| hover | 밝기 약 `1.03`, shadow 약간 강화 |
| active | `transform: translateY(1px)`, shadow 축소 |
| focus-visible | 바깥쪽 `3px` focus ring |
| 연결 중 | 중복 클릭 방지, 버튼 비활성화, `연결 중…` 표시 |
| 성공 | `연결 완료`와 체크 아이콘을 짧게 표시한 뒤 Today로 이동 |
| disabled | opacity만 낮추지 말고 명도와 cursor를 함께 변경 |

연결 중에는 CTA 텍스트가 바뀌어도 버튼 너비가 흔들리지 않아야 한다.

### 11.2 나중에 할게요

시각적으로는 카드 하단 중앙의 텍스트 버튼이다.

| 속성 | 값 |
| --- | --- |
| 높이 | 최소 `44px` |
| 상단 여백 | `12px` |
| 좌우 패딩 | `16px` |
| 배경 | 투명 |
| 텍스트 색상 | `#686371` |
| 밑줄 | 기본 없음, hover 시 허용 |
| focus-visible | `3px` focus ring |

링크처럼 보이더라도 앱 상태를 변경하고 이동하므로 `<button type="button">`을 권장한다.

## 12. 연결 흐름과 상태

### 12.1 초기 상태

- CTA 활성화
- 상태 메시지 없음
- 보조 행동 활성화
- 사용자의 캘린더 데이터는 아직 요청하거나 저장하지 않는다.

### 12.2 연결 시작

1. 사용자가 `캘린더 연결하기`를 누른다.
2. 버튼을 `연결 중…` 상태로 바꾸고 중복 클릭을 막는다.
3. Google OAuth 인증을 시작한다.
4. 필요한 최소 권한과 사용 목적을 Google 동의 화면에서 명확히 안내한다.
5. 성공 시 연결 상태를 저장하고 메인 앱의 `Today`로 이동한다.

OAuth 팝업을 사용하는 경우 팝업 차단이나 사용자의 창 닫기를 오류와 구분한다.

### 12.3 연결 성공

- 버튼 문구: `연결 완료`
- 성공 상태는 `aria-live="polite"`로 알린다.
- 약 `500~800ms` 뒤 `Today`로 이동한다.
- 뒤로 가기로 연결 전 화면이 다시 나타나지 않도록 replace 이동을 권장한다.

### 12.4 연결 실패

카드가 무너지거나 별도 전체 오류 페이지로 이동하지 않는다.

- CTA 바로 아래에 인라인 오류 메시지를 표시한다.
- 권장 문구: `캘린더를 연결하지 못했어요. 잠시 후 다시 시도해 주세요.`
- CTA 문구를 `다시 연결하기`로 바꾼다.
- 오류 메시지 색상은 배경과 `4.5:1` 대비를 확보한다.
- 오류 영역에는 `role="alert"`를 사용한다.
- 사용자가 취소한 경우에는 오류로 과도하게 경고하지 않고 `연결이 취소되었어요.`로 안내한다.

### 12.5 연결 미루기

1. `나중에 할게요`를 누르면 `calendarConnection: skipped` 상태를 저장한다.
2. 메인 앱의 `Today`로 이동한다.
3. 이후 Google Calendar가 필요한 주간 계획 생성을 요청하면 연결 안내를 다시 표시한다.
4. CatchUp 안에서 개인 일정을 직접 추가·수정하는 기능은 계속 사용할 수 있다.
5. 연결을 미룬 상태를 연결 완료로 취급하지 않는다.

### 12.6 이미 연결된 사용자

- 이 화면에 직접 접근하면 연결 완료 상태를 확인한 뒤 `Today`로 이동한다.
- 불필요하게 OAuth를 다시 요청하지 않는다.
- 연결 해제는 이 온보딩 화면이 아니라 향후 연결 관리 위치에서 처리한다.

## 13. 데이터 및 개인정보 원칙

- API 키, OAuth 토큰, 비밀번호를 클라이언트 코드나 문서에 직접 적지 않는다.
- OAuth 토큰은 안전한 서버 측 저장소 또는 보안 세션 방식으로 관리한다.
- 화면에는 실제 학생 이름, 이메일, 일정 제목 등 개인정보 예시를 표시하지 않는다.
- 개발과 테스트에는 가짜 Google 계정 또는 익명화된 샘플 일정만 사용한다.
- 연결 목적은 개인 일정과 학업 계획의 충돌을 줄이는 데 필요한 범위로 제한한다.
- “언제든 연결 해제 가능” 문구가 실제 설정 또는 계정 관리 기능과 일치해야 한다.

## 14. 반응형 규칙

### 모바일: `320~767px`

- 카드 너비는 `calc(100vw - 48px)`를 넘지 않는다.
- 높이 `740px` 미만에서는 상단 패딩과 카드 내부 간격을 약 `12~20%` 줄인다.
- 콘텐츠를 축소해 억지로 한 화면에 맞추지 않고 필요하면 페이지 스크롤을 허용한다.
- `320px` 너비에서 혜택 문구가 2줄이 되어도 카드 밖으로 넘치지 않아야 한다.
- safe area와 브라우저 하단 UI를 고려해 보조 버튼 아래 여백을 확보한다.

### 태블릿·데스크톱: `768px 이상`

- 페이지 배경은 전체 viewport를 채운다.
- 헤더와 카드를 포함한 단일 컬럼을 화면 중앙에 둔다.
- 카드 최대 너비는 `380px`까지 허용하되 내부 구성은 과도하게 확대하지 않는다.
- 카드 상단이 viewport 밖으로 밀리지 않도록 세로 중앙 정렬보다 상단 기준 배치를 우선한다.
- 모바일 기기 프레임을 추가하지 않는다.

### 텍스트 확대

- 브라우저 글자 확대 `200%`에서도 모든 문구와 버튼을 사용할 수 있어야 한다.
- 글자 확대 시 카드 고정 높이를 해제하고 `height: auto`로 확장한다.
- CTA와 보조 버튼의 순서는 유지한다.

## 15. 접근성

- 화면 제목은 `h1` 하나로 제공한다.
- 혜택은 시맨틱 `ul`과 `li`로 구현한다.
- Google Calendar 로고와 연결 그래픽은 같은 정보를 본문이 제공하므로 `aria-hidden="true"` 처리한다.
- CTA의 접근 가능한 이름은 보이는 문구와 동일한 `캘린더 연결하기`로 둔다.
- 키보드 탭 순서는 `캘린더 연결하기 → 나중에 할게요`다.
- 모든 버튼은 최소 `44 × 44px` 터치 영역을 가진다.
- focus-visible 상태는 밝은 배경과 CTA 양쪽에서 구분되어야 한다.
- 상태 변경은 `aria-live`로 알리고, 오류는 `role="alert"`를 사용한다.
- 색상만으로 연결 상태를 전달하지 않고 문구 또는 아이콘과 함께 표시한다.
- OAuth 팝업에서 돌아왔을 때 포커스를 연결 버튼 또는 상태 메시지로 복원한다.
- 실제 대비는 구현 후 WCAG AA 기준으로 검사한다. 목업만으로 전체 접근성 준수를 확정하지 않는다.

## 16. 구현 시 금지 사항

- Google Calendar 외 다른 캘린더 선택지를 추가하지 않는다.
- 목업의 iPhone 프레임과 상태바를 웹 UI로 구현하지 않는다.
- 공식 Google Calendar 로고를 임의 변형하거나 보라색 단색으로 바꾸지 않는다.
- 혜택 아이콘에 이모지를 사용하지 않는다.
- 연결이 끝나기 전에 완료 상태로 표시하지 않는다.
- 연결 실패 후 사용자가 빠져나갈 수 없는 상태를 만들지 않는다.
- `나중에 할게요`를 눌렀다는 이유로 연결 완료 플래그를 저장하지 않는다.
- 실제 사용자 일정이나 OAuth 토큰을 프론트엔드 로그, 분석 이벤트, 테스트 fixture에 남기지 않는다.

## 17. 개발 완료 조건

- [ ] `393 × 852px`에서 제목, 부제, 카드가 목업과 같은 세로 흐름으로 배치된다.
- [ ] 제목은 `Google Calendar 연결`, 부제는 정확히 2줄이다.
- [ ] 반투명 흰색 카드와 라벤더 배경이 명확히 구분된다.
- [ ] 카드 상단에 Google Calendar 로고, 달력 윤곽, 연결 곡선, 링크 배지가 포함된다.
- [ ] 혜택 3개와 해당 선형 아이콘이 정확한 순서로 표시된다.
- [ ] CTA는 보라색 그라디언트, 큰 둥근 모서리, 흰색 텍스트를 사용한다.
- [ ] `나중에 할게요`가 최소 터치 영역을 가진 실제 버튼으로 동작한다.
- [ ] 연결 중, 성공, 실패, 취소, 재시도 상태가 구현된다.
- [ ] 연결 성공 시 `Today`로 이동한다.
- [ ] 연결 미루기 후에도 `Today`로 이동하되 주간 계획 생성 전 재연결을 요구한다.
- [ ] Google Calendar 외 다른 외부 캘린더는 노출하지 않는다.
- [ ] `320px` 너비, 짧은 화면, 데스크톱, 글자 확대 `200%`에서 사용할 수 있다.
- [ ] 키보드, 스크린 리더, focus-visible, 상태 알림, 색상 대비를 검증한다.
- [ ] 테스트 데이터는 가짜 또는 익명화된 일정만 사용한다.

## 18. 목업만으로 확정할 수 없는 항목

다음은 정적 목업만으로 확인할 수 없어 이 문서에서 구현 기준을 제안한 항목이다.

- OAuth를 팝업, redirect, 별도 창 중 어떤 방식으로 처리할지
- 요청할 Google Calendar 권한의 정확한 scope
- 연결 성공 후 대기 시간과 전환 모션
- 연결 실패, 취소, 팝업 차단의 실제 오류 카피
- `나중에 할게요` 이후 재연결 안내의 정확한 위치
- 연결 해제 기능의 실제 진입 경로
- 공식 일러스트레이션 원본 에셋의 존재 여부

OAuth scope, 토큰 저장 방식, 연결 해제 위치는 기술 설계와 개인정보 정책에서 확정해야 한다. 원본 일러스트레이션 에셋이 제공되면 이 문서의 구성 설명보다 원본 에셋을 우선한다.
# 최종 확정 변경 (2026-08-03)

- `Google Calendar 연결` 제목 아래 보조 설명을 삭제한다.
- Google Calendar 표시는 사용자 제공 파란색 `31` 이미지 자산을 사용한다.
- 연결 상태, 혜택 목록, 연결·건너뛰기 동작은 유지한다.
