# CatchUp Month 화면 디자인·구현 명세

## 최종 UX 보완 (2026-08-06)

- 날짜 상세의 일정 생성 버튼 문구는 `추가`다.
- Upload 추출, Google Calendar, CatchUp 직접 입력 일정 모두 Month의 동일한 일정 편집창에서 수정한다.
- Upload 추출 일정의 수정은 공유 추출 상태에 반영되어 Today와 Month에 함께 표시된다.

## 1. 문서 목적

이 문서는 첨부된 Month 화면 목업을 개발 구현 기준으로 변환한 화면 전용 명세다. 개발자는 원본 목업을 다시 보지 않아도 이 문서만으로 월간 캘린더의 정상 상태, 상호작용, 반응형 규칙, 데이터 표시 규칙과 MVP에 필요한 파생 상태를 구현할 수 있어야 한다.

이 문서는 `PRD.md`, `README.md`, `IA.md`, `USER_FLOW.md`, `SCREEN_SPEC.md`를 제품 기준으로 사용하고 `DESIGN_SYSTEM.md`의 공통 토큰을 따른다. 충돌 시 우선순위는 `PRD.md` → `README.md` → `IA.md` → `USER_FLOW.md` → `SCREEN_SPEC.md` → 목업 → 본 문서의 보완 결정 순이다.

## 2. 화면 역할과 MVP 범위

- 화면 이름: Month
- 권장 경로: `/month`
- 진입 경로: 하단 내비게이션의 `Month`
- 목적: 학업 중요 일정과 개인 일정을 월 단위로 훑고, 특정 날짜의 일정 상세를 연다.
- 표시 대상:
  - 업로드 자료에서 추출된 과제 마감, 제출, 시험, 중요 공지, 수업 일정
  - Google Calendar에서 불러온 개인 일정
  - CatchUp에서 직접 추가한 개인 일정
- 허용 행동:
  - 월 이동
  - 날짜 선택
  - 날짜별 일정 상세 바텀시트 열기
  - 모든 출처의 원본 일정 추가·수정
  - 전역 AI Mate 열기
- 제외 행동:
  - 월간 세부 학습 계획 자동 생성
  - AI가 만든 주간 계획 항목의 직접 편집
  - Google Calendar 외 외부 캘린더 연결
  - 색상만으로 학업/개인 일정 유형 구분

Month는 “월간 일정 확인 화면”이다. AI 계획을 직접 편집하는 화면으로 확장하지 않는다.

## 3. 제품 문서와 목업의 차이 및 구현 결정

| 항목 | 목업 관찰 | 제품 기준 | 구현 결정 |
| --- | --- | --- | --- |
| 2026년 7월 날짜 배열 | 첫째 주 이후 날짜가 실제 달력과 어긋남 | 날짜는 실제 캘린더 데이터여야 함 | 날짜·요일을 하드코딩하지 않고 로케일 캘린더 계산 결과로 렌더링 |
| 선택 날짜 | `10`이 보라색 원으로 선택됨 | 선택 날짜는 사용자의 입력 상태 | 최초 진입은 오늘, 이후 마지막 선택 날짜 유지 |
| 일정 표시 | 날짜 아래 점 중심 | 날짜 안에서 일정을 즉시 구분 | 일정 제목 칩을 최대 2개 표시하고, 나머지는 `+N`으로 표시 |
| 일정 유형 색상 | 모두 보라 계열 | 사용자가 유형을 한눈에 구분 | 지정된 6색 팔레트만 사용하며, 같은 과목은 같은 색을 공유 |
| 상단 달력 아이콘 | 별도 아이콘 버튼 | 기능이 명시되지 않음 | `오늘로 이동` 버튼으로 정의 |
| 상단 메뉴 아이콘 | 햄버거 버튼 | 별도 메뉴 범위 미정 | 앱 설정/계정 메뉴가 구현된 경우만 노출, 아니면 슬롯 제거 |
| 달력 주 시작 | 일요일부터 토요일 | Month 주 시작 정책은 별도 제한 없음 | 목업과 한국 일반 달력 관례에 맞춰 일→토 사용. 주간 계획의 월→일 정책과 혼동하지 않음 |
| 일정 칩 텍스트 | `과제 제출`, `중간고사`, `팀 회의`, `퀴즈 복습` | Month는 학업 중요 일정과 개인 일정 표시 | AI 주간 계획의 행동 항목이 아니라 원본 일정/마감 데이터만 칩으로 표시 |

목업의 `퀴즈 복습`이 AI가 특정 날짜에 배치한 Todo라면 Month에 직접 편집 가능한 일정으로 표시하지 않는다. Month에 노출할지는 제품 구현 시 `Todo`가 아니라 실제 일정 데이터인지 확인한다.

## 4. 목업 해석 기준

- 첨부 PNG 크기: `680 × 1400px`
- 이미지에는 iPhone 기기 프레임, Dynamic Island, 시스템 상태바, 홈 인디케이터가 포함되어 있다.
- 실제 웹앱 DOM에는 기기 프레임과 운영체제 UI를 렌더링하지 않는다.
- 내부 앱 화면은 약 `390 × 844 CSS px` 모바일 뷰포트로 환산한다.
- 목업 비율에서 추정한 수치는 약 `±2px` 오차를 허용한다.
- 실제 구현은 고정 이미지 좌표가 아니라 CSS Grid와 날짜 데이터에 기반해야 한다.

## 5. 목업이 보여주는 정상 상태

| 항목 | 상태 |
| --- | --- |
| 표시 월 | `2026년 7월` |
| 활성 탭 | `Month` |
| 선택 날짜 | `10일` |
| 캘린더 주 시작 | 일요일 |
| 월 표시 행 | 5개 |
| 이전·다음 달 날짜 | 회색으로 노출 |
| 일정 표시 | 보라색 점과 연보라색 칩 |
| AI Mate | 닫힘, 플로팅 버튼 노출 |
| 날짜 상세 | 닫힘 |
| 하단 내비게이션 | 고정 |

## 6. 정보 구조

```text
AppShell
├─ main.MonthPage
│  ├─ MonthHeader
│  │  ├─ MonthSelector
│  │  ├─ TodayButton
│  │  └─ MenuButton(optional)
│  └─ MonthCalendar
│     ├─ WeekdayHeader
│     └─ CalendarGrid
│        └─ CalendarWeek × 4~6
│           └─ DayCell × 7
│              ├─ DateButton
│              ├─ EventIndicator
│              └─ RepresentativeEventChip(optional)
├─ AiMateFloatingButton
└─ BottomTabBar
```

날짜 상세 화면은 별도 페이지가 아니라 `MONTHSPEC_DESIGN.md`에서 정의하는 바텀시트로 연다.

## 7. 전체 레이아웃

### 7.1 모바일 기준

기준 뷰포트는 `390 × 844px`이다.

| 요소 | 권장값 |
| --- | ---: |
| 앱 콘텐츠 너비 | `100%` |
| 넓은 화면 최대 너비 | `430px` |
| 페이지 배경 | `#FFFFFF` |
| 좌우 기본 패딩 | `20px` |
| 상단 콘텐츠 패딩 | `max(18px, env(safe-area-inset-top)) + 22px` |
| 헤더 높이 | `48px` |
| 헤더와 요일 행 간격 | `24px` |
| 요일 행 높이 | `24px` |
| 캘린더 그리드 상단 간격 | `10px` |
| 하단 탭바 콘텐츠 높이 | `76px` |
| 하단 탭바 총 높이 | `calc(76px + env(safe-area-inset-bottom))` |

본문 영역은 다음 높이를 사용한다.

```css
min-height: 100dvh;
padding-bottom: calc(76px + env(safe-area-inset-bottom));
```

5주 또는 6주 달력 모두 탭바 위에 들어가도록 달력 행 높이를 가용 높이에 맞춘다. 기준 화면에서 5주형 행 높이는 약 `110px`, 6주형은 약 `91px`다. 행 높이는 최소 `78px` 아래로 줄이지 않으며, 더 작아지면 본문 세로 스크롤을 허용한다.

### 7.2 데스크톱·태블릿

- `768px` 이상에서는 앱 열을 `430px` 너비로 중앙 정렬한다.
- 하단 탭바와 AI Mate 버튼도 중앙 앱 열을 기준으로 고정한다.
- 달력을 데스크톱 대형 월간 보드로 확장하지 않는다. 10주 MVP에서는 모바일 정보 밀도를 유지한다.
- 앱 열 바깥 배경은 `color.canvas`를 사용하고 앱 표면은 흰색으로 둔다.

## 8. 디자인 토큰

공통 토큰은 `DESIGN_SYSTEM.md`를 재사용하고 Month 전용 별칭만 추가한다.

### 8.1 색상

| 역할 | 토큰 / 값 | 용도 |
| --- | --- | --- |
| 화면 배경 | `color.surface` / `#FFFFFF` | 페이지·달력 |
| 주요 텍스트 | `color.ink` / `#111014` | 월 제목, 현재 월 날짜 |
| 보조 텍스트 | `color.muted` / `#747786` | 요일, 메타 |
| 다른 달 날짜 | `#A7A7AC` | 이전·다음 달 숫자 |
| 선 | `color.line` / `#ECEAF0` | 주 구분선, 탭바 |
| 주요 보라 | `color.purple` / `#806CF2` | 활성 탭, 일정 점 |
| 진한 보라 | `color.purple.dark` / `#6F4BE9` | 포커스·pressed |
| 선택 날짜 시작 | `#A88BFF` | 원형 그라디언트 |
| 선택 날짜 끝 | `#7655EF` | 원형 그라디언트 |
| 일정 칩 시작 | `#EEE8FF` | 칩 배경 |
| 일정 칩 끝 | `#DED3FF` | 칩 배경 |
| 탭바 표면 | `rgba(255,255,255,.96)` | 고정 내비게이션 |

선택 날짜는 흰색 텍스트와 최소 `4.5:1` 대비를 확보한다.

### 8.2 타이포그래피

```css
font-family:
  system-ui,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  sans-serif;
```

| 용도 | 크기 / line-height | 굵기 | 자간 |
| --- | --- | ---: | ---: |
| 월 제목 | `22px / 30px` | `800` | `-0.035em` |
| 요일 | `12px / 18px` | `500` | `-0.02em` |
| 날짜 숫자 | `15px / 22px` | `500` | `-0.02em` |
| 선택 날짜 | `16px / 22px` | `500` | `-0.02em` |
| 일정 칩 | `12px / 18px` | `500` | `-0.02em` |
| 탭 라벨 | `12px / 16px` | 활성 `600`, 비활성 `400` | `-0.01em` |

## 9. 컴포넌트 상세

### 9.1 상단 헤더

구조:

```text
[2026년 7월 ˅]                         [오늘로 이동] [메뉴]
```

- 좌우 페이지 패딩 기준선에 맞춘다.
- 제목 영역과 우측 액션을 한 행에 배치한다.
- 제목 버튼은 최소 `44px` 높이이며 텍스트, 아래쪽 chevron 순이다.
- 제목과 chevron 사이 간격은 `8px`, chevron 시각 크기는 `16px`다.
- 월 제목은 `YYYY년 M월` 형식으로 지역화한다.
- 제목을 누르면 월/연도 선택 UI를 연다.
- 우측 아이콘의 시각 크기는 `22~24px`, 실제 터치 영역은 `44 × 44px`다.
- 헤더 아이콘은 2px 내외의 둥근 outline 스타일로 통일한다.

#### 오늘로 이동 버튼

- 달력 윤곽 아이콘을 사용한다.
- 접근성 이름: `오늘 날짜로 이동`
- 누르면 표시 월과 선택 날짜를 오늘로 변경한다.
- 오늘이 이미 선택된 경우 상태만 유지하고 네트워크 요청을 반복하지 않는다.

#### 메뉴 버튼

- 3개의 수평선 아이콘을 사용한다.
- 접근성 이름: `메뉴 열기`
- MVP에 연결할 설정/계정 메뉴가 없다면 버튼 자체를 렌더링하지 않는다.
- 장식 목적으로 비활성 버튼을 남기지 않는다.

### 9.2 월·연도 선택

- 제목 버튼을 누르면 네이티브에 가까운 월 선택 dialog 또는 바텀시트를 연다.
- 연도와 월을 키보드와 터치로 선택할 수 있어야 한다.
- 선택 완료 시 해당 월 1일을 기본 선택하지 않고, 기존 선택 일자가 새 달에 존재하면 같은 일자를 유지한다. 존재하지 않으면 새 달의 마지막 날을 선택한다.
- 별도 선택 UI 구현이 늦어지면 제목 버튼은 비활성화하지 말고 이전/다음 월 버튼을 접근 가능한 보조 컨트롤로 제공한다.

### 9.3 요일 헤더

```text
일  월  화  수  목  금  토
```

- `grid-template-columns: repeat(7, minmax(0, 1fr))`
- 각 셀은 가운데 정렬한다.
- 요일 문자열은 한국어 한 글자 표기를 사용한다.
- 일요일과 토요일에 별도 빨강·파랑을 적용하지 않는다. 목업의 중립 톤을 유지한다.
- 스크린리더에는 `일요일`처럼 전체 요일 이름을 제공한다.

### 9.4 월간 캘린더 그리드

- 열은 항상 7개다.
- 행은 월의 시작 요일과 일수에 따라 4~6개를 계산한다.
- 각 주의 시작은 일요일이다.
- 각 주 행 사이에 `1px solid color.line` 구분선을 넣는다.
- 첫 주 위에는 구분선을 넣지 않는다.
- 행별 높이는 동일하게 분배한다.
- 날짜 셀은 `position: relative`인 세로 플렉스 또는 grid로 구성한다.
- 셀 전체가 터치 타깃이되 인접 날짜 셀과 겹치지 않는다.

권장 구조:

```html
<div role="grid" aria-label="2026년 7월 달력">
  <div role="row">
    <button role="gridcell" aria-selected="false">
      <span>1</span>
      <span aria-hidden="true"><!-- indicator --></span>
    </button>
  </div>
</div>
```

### 9.5 날짜 셀

| 요소 | 권장값 |
| --- | ---: |
| 셀 내부 상단 패딩 | `12px` |
| 날짜 시각 영역 | `36 × 36px` |
| 날짜 터치 영역 | 셀 전체, 최소 `44px` 높이 |
| 날짜와 점 간격 | `5px` |
| 점 지름 | `4px` |
| 점 사이 간격 | `3px` |
| 날짜와 칩 간격 | `7px` |

#### 기본 상태

- 현재 표시 월 날짜는 `color.ink`.
- 이전·다음 달 날짜는 `#A7A7AC`.
- 배경은 투명하다.

#### 선택 상태

- 날짜 숫자 뒤에 `36 × 36px` 원형 보라 그라디언트를 둔다.
- 텍스트는 흰색이다.
- 원형에는 `0 5px 12px rgba(111,75,233,.18)` 그림자를 적용한다.
- `aria-selected="true"`를 제공한다.

#### 오늘이지만 미선택

- 날짜 원 둘레에 `2px solid color.purple`을 적용한다.
- 색상만으로 구분하지 않도록 접근성 이름에 `오늘`을 포함한다.

#### 다른 달 날짜

- 선택할 수 있다.
- 누르면 해당 날짜가 속한 달로 전환하고 날짜 상세를 연다.
- 일정 제목 칩은 기본적으로 숨겨 현재 월의 정보 밀도를 유지한다.

### 9.6 일정 제목 칩

- 해당 날짜에 표시 가능한 일정이 있으면 날짜 아래에 제목 칩으로 표시한다.
- 칩은 해당 날짜 셀 안에 최대 2개를 세로로 표시하며, 남은 일정은 `+N`으로 요약한다.
- 칩 색상은 6색 팔레트(`#C7B9FA`, `#E9E0FF`, `#F8B1FB`, `#FEE8FF`, `#A5D1FF`, `#D9F0FF`) 중 하나다.
- 같은 과목의 수업·과제·시험은 하나의 카테고리 키와 색상을 공유하고, Google Calendar 개인 일정은 하나의 공통 카테고리를 사용한다.
- 칩을 누르면 날짜 상세를 열고, 그 안에서 사용자 일정의 제목·시간·색상을 수정할 수 있다.
- 칩 높이 `24px`, 좌우 패딩 `12px`, radius `999px`.
- 텍스트는 한 줄, 최대 `8~10자` 후 말줄임한다.
- 배경은 연보라 그라디언트, 텍스트는 `color.ink`.
- 칩을 누르면 해당 날짜의 상세 바텀시트를 열고 해당 항목에 포커스를 둔다.
- 일정이 여러 개면 칩 하나와 `+N` 보조 텍스트를 사용할 수 있다.
- 칩 우선순위 권장: 시험·마감 → 시간 고정 개인 일정 → 중요 공지 → 기타.
- 이 우선순위는 성과 예측이 아니라 표시 밀도를 위한 정렬 규칙이다.

### 9.8 AI Mate 플로팅 버튼

- 공통 `DESIGN_SYSTEM.md`를 따른다.
- 크기 `70 × 70px`, radius `35px`.
- 위치 `right: 20px`, `bottom: calc(94px + env(safe-area-inset-bottom))`.
- 보라색 그라디언트 원 안에 AI Mate 전용 캐릭터 에셋을 표시한다.
- 접근성 이름: `AI Mate 열기`
- Month에서 열면 `현재 보고 있는 월`과 `선택 날짜`를 대화 문맥으로 전달할 수 있다.
- 날짜 상세 바텀시트가 열린 동안에는 중복 플로팅 버튼을 숨긴다.

### 9.9 하단 내비게이션

- `Today`, `Month`, `Upload` 3개 탭만 표시한다.
- 콘텐츠 높이 `76px`, 하단 safe area는 별도로 더한다.
- 상단에 `1px solid color.line`.
- 아이콘 `26px`, 라벨 `12px`, 간격 `6px`.
- `Month` 아이콘과 라벨은 `color.purple`, 나머지는 `color.icon.muted`.
- 현재 탭에는 `aria-current="page"`를 적용한다.
- AI Mate를 네 번째 탭으로 추가하지 않는다.

## 10. 날짜·일정 상호작용

### 10.1 날짜 선택

1. 사용자가 날짜 셀을 누른다.
2. 선택 원이 새 날짜로 이동한다.
3. 선택 날짜 상태를 `YYYY-MM-DD`로 저장한다.
4. 해당 날짜의 일정 데이터를 조회한다.
5. `MONTHSPEC_DESIGN.md`의 바텀시트를 연다.

- 이미 선택된 날짜를 다시 눌러도 상세 바텀시트를 연다.
- 일정이 없는 날짜도 바텀시트를 열고 빈 상태와 `추가`를 제공한다.
- 날짜 선택은 AI Mate 조정 횟수를 차감하지 않는다.

### 10.2 월 이동

- 터치 환경: 달력 본문에서 좌우 swipe를 선택적으로 지원한다.
- 키보드/포인터: 월 선택 UI 또는 이전/다음 버튼을 제공한다.
- swipe 임계값 권장: 가로 이동 `48px` 이상이고 세로 이동보다 커야 한다.
- 월 전환 애니메이션은 `180~220ms`로 제한한다.
- `prefers-reduced-motion: reduce`에서는 이동 애니메이션을 제거한다.
- 월 변경 중 기존 일정이 새 달의 데이터처럼 보이지 않게 calendar skeleton 또는 짧은 loading 상태를 표시한다.

### 10.3 브라우저 뒤로가기

- 날짜 상세 바텀시트가 열린 상태에서 뒤로가기는 먼저 바텀시트만 닫는다.
- Month 탭 자체를 벗어나는 것은 그 다음 뒤로가기에서 처리한다.
- URL 상태 예시: `/month?month=2026-07&date=2026-07-18`.

## 11. 데이터 바인딩 규칙

### 11.1 데이터 유형

| UI 데이터 | 원본 모델 | 편집 위치 |
| --- | --- | --- |
| 학업 중요 일정 | `ExtractedItem` | Upload 추출 결과 확인·수정 |
| Google 개인 일정 | `CalendarEvent(source=Google Calendar)` | Month 일정 편집창에서 수정 |
| CatchUp 개인 일정 | `CalendarEvent(source=CatchUp 직접 입력)` | 날짜 상세 바텀시트 |
| AI 주간 계획 행동 | `Todo` | Month 직접 편집 금지, AI Mate 조정 |

### 11.2 필수 필드

```ts
type MonthEvent = {
  id: string;
  title: string;
  date: string;          // YYYY-MM-DD
  startTime?: string;    // HH:mm
  endTime?: string;      // HH:mm
  isAllDay: boolean;
  type: "assignment" | "submission" | "exam" | "notice" | "class" | "personal";
  source: "upload" | "google-calendar" | "catchup";
  editable: boolean;
};
```

- 날짜와 시간은 사용자 timezone인 `Asia/Seoul` 기준으로 표시한다.
- 서버 ISO timestamp를 날짜 문자열로 자를 때 UTC 날짜가 바뀌지 않도록 timezone 변환 후 그룹화한다.
- 종료 시간이 시작 시간보다 이르면 저장하지 않는다.
- 실제 학생 개인정보나 실제 민감 일정은 테스트 fixture에 사용하지 않는다.

## 12. 상태별 화면

### 12.1 로딩

- 헤더와 하단 내비게이션은 유지한다.
- 달력 숫자는 즉시 계산해 표시하고 일정 점·칩 영역만 skeleton으로 처리할 수 있다.
- 전체 화면 spinner로 월 이동을 막지 않는다.

### 12.2 빈 월

- 날짜 그리드는 정상 표시한다.
- 점과 칩을 표시하지 않는다.
- 선택한 날짜 상세에서 `이 날짜에는 일정이 없어요.`와 `추가`를 제공한다.

### 12.3 오류

- 달력 날짜는 유지한다.
- 그리드 상단 또는 선택 날짜 상세에 `일정을 불러오지 못했어요. 다시 시도해주세요.`를 표시한다.
- `다시 시도` 버튼을 제공한다.
- OAuth 토큰, 계정 식별자, 원문 개인정보를 오류 문구에 노출하지 않는다.

### 12.4 Google Calendar 미연결

- CatchUp 일정과 학업 일정은 정상 표시한다.
- 화면 전체를 막지 않는다.
- 필요 시 비차단 안내로 `Google Calendar를 연결하면 개인 일정도 함께 볼 수 있어요.`와 연결 동작을 제공한다.

### 12.5 일정 과다

- 점은 최대 3개, 대표 칩은 최대 1개만 표시한다.
- 전체 일정은 상세 바텀시트에서 스크롤 목록으로 제공한다.
- 셀 안에 모든 일정을 쌓아 행 높이를 늘리지 않는다.

## 13. 접근성

- 월 제목은 페이지의 `h1`로 제공한다.
- 캘린더에는 `role="grid"`와 `aria-label="{YYYY년 M월} 달력"`을 제공한다.
- 날짜 버튼 접근성 이름 예시: `2026년 7월 18일 토요일, 일정 2개`.
- 선택 상태, 오늘 상태, 다른 달 상태를 접근성 이름과 속성으로 전달한다.
- 키보드 화살표로 인접 날짜 이동을 지원한다.
  - 좌/우: 하루
  - 위/아래: 7일
  - Page Up/Down: 이전/다음 달
  - Enter/Space: 날짜 상세 열기
- 포커스 링은 `2px solid color.purple.dark`, offset `2px`.
- 터치 타깃은 최소 `44 × 44px`를 확보한다.
- 점과 색상만으로 일정의 유형이나 존재를 전달하지 않는다.
- 200% 텍스트 확대에서도 날짜 숫자와 핵심 액션이 잘리지 않아야 한다.

## 14. 성능 및 구현 주의

- 달력 날짜 행렬은 검증된 date library 또는 `Temporal` 호환 유틸리티로 계산한다.
- 목업의 날짜 숫자를 fixture로 그대로 복사하지 않는다.
- 월 이동마다 모든 이벤트를 재요청하지 않도록 월 단위 캐시를 사용할 수 있다.
- 이벤트 목록을 날짜 키(`YYYY-MM-DD`)로 미리 그룹화해 셀마다 전체 배열을 순회하지 않는다.
- 고정 탭바 때문에 마지막 달력 행이 가려지지 않도록 safe area를 실제 기기에서 검증한다.
- 서버와 클라이언트가 서로 다른 timezone으로 첫 렌더를 계산해 hydration mismatch가 나지 않게 한다.

## 15. 구현 금지 사항

- iPhone 프레임, Dynamic Island, 상태바, 홈 인디케이터를 HTML로 재현하지 않는다.
- 실제 달력과 맞지 않는 날짜·요일을 하드코딩하지 않는다.
- 학업·개인 일정을 여러 원색으로 구분하지 않는다.
- 확인 필요 일정을 별도 색상으로 강조하지 않는다.
- AI Todo를 일반 개인 일정처럼 연필 아이콘으로 직접 수정하게 하지 않는다.
- 한 셀에 모든 일정 제목을 쌓지 않는다.
- 메뉴 기능 없이 햄버거 아이콘만 장식으로 남기지 않는다.
- AI Mate를 하단 내비게이션 탭으로 추가하지 않는다.

## 16. 개발 완료 조건

- [ ] 실제 연·월 데이터로 일→토 7열 달력이 정확히 계산된다.
- [ ] 4~6주 달력 모두 하단 탭바와 겹치지 않는다.
- [ ] 현재 월, 다른 달, 오늘, 선택 날짜 상태가 구분된다.
- [ ] 일정 수에 따라 점 1~3개가 표시된다.
- [ ] 대표 일정 칩이 한 주 행의 레이아웃을 깨지 않는다.
- [ ] 날짜 선택 시 같은 날짜의 상세 바텀시트가 열린다.
- [ ] 일정이 없는 날짜에서도 `추가` 흐름에 진입할 수 있다.
- [ ] Month 활성 상태의 하단 내비게이션과 AI Mate 버튼이 고정된다.
- [ ] 학업 일정·Google 일정·CatchUp 일정의 출처와 편집 권한이 혼동되지 않는다.
- [ ] 320px 너비, 390px 기준, 430px 앱 열에서 레이아웃을 검증했다.
- [ ] 키보드 날짜 이동, 포커스 복귀, 스크린리더 이름을 검증했다.
# 최종 확정 변경 (2026-08-03)

- 날짜 아래 점 대신 일정 제목 칩을 직접 표시하고 넘치는 일정은 `+N`으로 축약한다.
- 색상은 `#C7B9FA`, `#E9E0FF`, `#F8B1FB`, `#FEE8FF`, `#A5D1FF`, `#D9F0FF` 6개로 제한한다.
- 같은 과목은 같은 색상, Google Calendar와 CatchUp 개인 일정은 하나의 개인 일정 색상을 공유한다.
- 일정 상세에서 색상을 바꾸면 같은 과목 전체 또는 개인 일정 전체에 적용한다.
- 일정 행 전체를 눌러 수정하고 별도 연필 버튼은 두지 않는다.
