# CatchUp User Flow

## 1. 문서 목적

이 문서는 `IA.md`를 바탕으로 CatchUp MVP의 핵심 사용자 흐름을 정의한다.

현재 앱 구조는 아래를 기준으로 한다.

```text
온보딩
-> Google Calendar 연동
-> 메인 앱

하단 네비게이션
├─ Today
├─ Month
└─ Upload

전역 플로팅
└─ AI Mate
```

AI Mate는 하단 네비게이션의 개별 탭이 아니라, 모든 메인 화면에서 접근 가능한 플로팅 탭이다.

## 2. 첫 사용 흐름

### 목적

Google Calendar를 사용하는 사용자가 CatchUp의 핵심 기능을 이해하고, Google Calendar를 연결해 주간 계획 생성에 필요한 기본 상태를 만든다.

### 흐름

```text
앱 실행
-> Catch Up 워드마크 온보딩 확인
-> 포인트 컬러 그라디언트 인트로 노출
-> Google Calendar 연동 화면으로 자동 전환
-> Google Calendar 연동 화면
-> Google Calendar 연결
-> 연결 완료
-> 메인 앱 진입
-> Today 탭 표시
```

### 예외 흐름

```text
Google Calendar 연결 실패
-> 실패 안내 표시
-> 다시 연결 시도
-> 연결 완료 후 메인 앱 진입
```

### MVP 제한

- Google Calendar 외 다른 외부 캘린더는 연결하지 않는다.
- 개인 시간표 직접 연동은 포함하지 않는다.
- CatchUp 안에서 개인 일정을 직접 추가하고 수정할 수 있다.

## 3. 학업 자료 업로드 흐름

### 목적

사용자가 강의계획서, LMS 공지, 과제 명세서 등 학업 자료를 업로드하고, AI가 추출한 정보를 직접 확인 및 수정한다.

### 흐름

```text
Upload 탭 이동
-> 학업 자료 업로드
-> 업로드한 자료 목록에 파일 표시
-> AI 추출 진행
-> 기존 미확정 이벤트와 동일 이벤트인지 비교하고 정보·출처 병합
-> Upload에서 최대 4개 이벤트 Preview 표시
-> 이벤트 카드 또는 전체 보기 화살표 선택
-> 모든 상세 영역이 닫힌 이벤트 확인 및 수정 페이지 표시
-> 사용자가 추출 정보 확인
-> 틀린 값 또는 누락된 값 직접 수정
-> 확정 또는 미확정 상태 재판정
-> 수정 내용과 두 상태 모두 저장
```

### 추출 정보

- 과제명
- 마감일
- 시험일
- 제출 방식
- 준비물
- 중요 공지
- 수업 일정 정보

### 상태 흐름

| 상태 | 의미 | 사용자 행동 |
| --- | --- | --- |
| 추출 중 | AI가 학업 자료에서 정보를 추출하는 중 | 대기 |
| 추출 완료 | 추출 결과가 표시된 상태 | 확인 후 저장 |
| 확인 필요 | 일부 값이 불확실하거나 누락된 상태 | 직접 수정 |
| 추출 실패 | 파일 인식 또는 추출 실패 | 재시도 |
| 확정 이벤트 | 일정 활용에 필요한 핵심 정보가 충분함 | 저장 또는 필요 시 수정 |
| 미확정 이벤트 | 이벤트 존재는 확인했지만 날짜·세부 정보 등이 부족함 | 추가 자료 업로드 또는 직접 보완 |

### 중요 원칙

- 추출 정보 수정은 AI Mate가 대신 처리하지 않는다.
- 사용자가 화면에 표시된 필드 값을 직접 입력해 수정한다.
- 수정된 추출 정보는 플로팅 AI Mate의 주간 계획 생성 근거로 사용된다.
- AI는 빈 정보를 임의로 만들지 않는다.
- 시험의 정확한 날짜와 자료에 명시된 예정 주차는 별도 필드로 보존한다.
- 시간표 이미지는 시간표 자료로 분류하고 과목별 반복 수업 시간을 추출한다. 사용자가 요일·시작/종료 시간·강의실을 확인 및 수정해 저장하면 해당 요일의 Today 예정 일정에 바로 표시한다.

## 4. 주간 계획 생성 흐름

### 목적

사용자가 업로드한 학업 자료, 수정된 추출 정보, Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정을 바탕으로 AI Mate를 통해 주간 계획을 생성한다.

### 흐름

```text
자료 업로드 완료
-> 플로팅 AI Mate 열기
-> `주간계획 생성` Prompt Chip 선택
-> `주간계획 생성해줘. 다음의 요청사항을 반영해: ` 입력 초안 확인 및 추가 요구사항 입력
-> 주간 계획 생성 요청
-> 주차만 있는 일정에 1주차 기준일이 없으면 이번 학기 1주차 시작일을 가장 먼저 한 번 질문
-> 답변을 저장해 주차 기반 학업 일정을 Today와 Month의 실제 날짜에 매핑
-> 하루 최대 학습시간이 저장되지 않았으면 다음으로 질문
-> 과제별 누락 날짜·마감·범위는 질문하지 않고 Upload의 학업 일정 확인 화면에서 보완
-> 학업자료 특성과 확인된 정보로 예상 소요시간 산정
-> 확정 이벤트, 앞으로 4주 일정, 기존 미완료 할 일 참고
-> 확정 이벤트가 없으면 확정된 시간표 수업으로 최소 복습 계획 구성
-> 요청일부터 앞으로 7일 계획 생성
-> Today 탭의 날짜별 할일에 반영
```

### AI가 참고하는 정보

- Upload 화면에 업로드된 학업 자료
- 사용자가 직접 수정한 AI 추출 결과
- Google Calendar 개인 일정
- CatchUp에서 직접 추가·수정한 개인 일정
- 과제 난이도
- 예상 소요시간
- 마감일과 시험일
- 완료한 일과 미완료 항목
- 사용자의 자연어 요청사항

### 계획수립 요청사항 예시

- "일요일에는 쉬는 시간을 많이 확보해줘."
- "매일 15분 이상 영어 공부를 넣어줘."
- "팀플 회의가 많으니 과제는 짧게 나눠서 배치해줘."
- "마감이 가까운 과제부터 우선 배치해줘."

### 생성 제한 흐름

```text
사용자가 같은 주에 주간 계획 생성을 다시 요청
-> 시스템이 이번 주 생성 완료 여부 확인
-> 이미 생성 완료면 추가 생성 차단
-> 다음 생성 가능 요일과 시간 안내
-> 계획 변경은 AI Mate의 계획 조정 요청으로 유도
```

### 생성 제한 규칙

- 최초 계획은 검토 완료 후 명시적 요청으로 생성한다.
- Plan 범위는 요청일부터 7일이며 Today 주차는 월요일부터 일요일까지다.
- 같은 주 안에서 주간 계획을 여러 번 생성하지 않는다.

### 새 정보에 따른 자동 조정

```text
새 확정 학업 이벤트 또는 CatchUp 개인 일정 추가·변경·삭제
-> 현재 WeeklyPlan 영향 판단
-> 새로 확정된 이벤트에 필요한 개인화 정보가 있으면 `질문이 있어요!`
-> 필요한 질문만 완료
-> WeeklyPlan 자동 조정
-> 실제 변경 이유와 Task 추가·삭제·이동 결과 설명
-> `아냐, 취소해줘` 선택 시 최근 자동 조정 직전 WeeklyPlan으로 복구
```

- 미확정 AcademicEvent는 Calendar 표시만 갱신하고 WeeklyPlan 작업을 만들지 않는다.
- 성공한 사용자 수정과 자동 조정은 하루 10회를 공유한다. 취소는 추가 차감하지 않는다.
- 10회 한도 도달 시 신규 정보를 pending으로 보존하고 다음 날 자동 조정한다.

## 5. Today 확인 흐름

### 목적

사용자가 이번 주 날짜를 선택하며 날짜별 할일을 확인하고, 기본값으로 오늘 해야 할 학습 항목과 오늘 예정 일정을 빠르게 확인한다.

### 흐름

```text
Today 탭 진입
-> 오늘 날짜가 기본 선택된 이번 주 날짜 스트립 확인
-> 필요하면 월요일~일요일 중 다른 날짜 선택
-> AI Mate 브리핑 카드 확인
-> 선택한 날짜의 할일 목록 확인
-> 필요하면 할 일 카드를 눌러 추천 이유·조정·추가 Prompt Chips 확인
-> 선택한 날짜의 예정 일정 요약 확인
-> 완료한 항목 체크
-> 계획 변경이 필요하면 Prompt Chip 또는 빈 상태의 계획 조정 버튼으로 AI Mate 입력 초안 열기
```

주간 계획이 아직 없으면 `계획 생성하기` 또는 플로팅 AI Mate를 열고 `주간계획 생성` 칩을 선택한다. 칩은 `주간계획 생성해줘. 다음의 요청사항을 반영해: `만 입력하고 자동 전송하지 않는다.

### 선택한 날짜의 할일

AI가 이번 주 계획에서 선택한 날짜로 배치한 학습 항목이다. Today 탭에 처음 들어오면 오늘 날짜가 기본 선택된다.

- 과제 진행
- 시험 공부
- 수업 준비
- 복습 또는 예습

### 선택한 날짜의 예정 일정

날짜 기준으로 선택한 날짜에 예정된 일정이다.

- 선택한 날짜가 마감인 과제
- 선택한 날짜에 제출해야 하는 항목
- 선택한 날짜에 예정된 시험
- Google Calendar 개인 일정과 CatchUp에서 직접 추가·수정한 개인 일정
- Upload 자료에서 추출된 학업 일정

예정 일정 카드를 누르면 Today 안에서 제목, 날짜, 시간, 색상을 수정할 수 있다.

### AI Mate 브리핑

- 선택한 날짜의 할일을 시작할 수 있도록 가볍게 북돋아주는 문구를 표시한다.
- 마감이 가까운 항목, 개인 일정 충돌 가능성, 오늘 먼저 보면 좋은 항목을 짧게 요약한다.
- 정확한 완료 확률이나 성과 예측처럼 보이는 수치는 표시하지 않는다.

## 6. Month 확인 흐름

### 목적

사용자가 학업 일정과 Google Calendar 개인 일정, CatchUp에서 직접 추가·수정한 개인 일정을 월간 캘린더로 확인한다.

### 흐름

```text
Month 탭 진입
-> 월간 일정 요약 캘린더 확인
-> 날짜 칸의 색상별 일정 제목 칩 확인
-> 날짜 선택
-> 일정 상세 팝업 확인
-> 필요하면 Upload 추출 일정과 CatchUp 직접 입력 일정을 추가 또는 수정하고, Google 일정은 상세 확인
-> 계획 변경이 필요하면 플로팅 AI Mate 열기
```

### 월간 캘린더 규칙

- 날짜는 일반 달력처럼 숫자로 표시한다.
- 정규 반복 수업은 Month에서 숨기고 Today의 예정 일정에만 표시한다.
- 단일 날짜 일정과 주차 기반 Bar는 한 주의 공통 3개 lane을 사용하며, 초과 일정은 해당 날짜의 `+N`으로 축약한다.
- `+N`을 누르면 기존 날짜 상세 화면에서 숨겨진 일정까지 모두 확인한다.
- 색상은 지정된 6개로 제한하며 같은 과목은 같은 색상, 개인 일정은 하나의 공통 색상을 사용한다.
- 일정 상세에서 색상을 바꾸면 같은 과목의 모든 일정 또는 모든 개인 일정에 적용한다.
- 날짜를 선택하면 해당 날짜의 일정 상세 팝업을 표시한다.
- Google Calendar 일정은 CatchUp에서 읽기 전용이며 변경은 Google Calendar에서 수행한다. Upload 추출 일정과 CatchUp 직접 입력 일정의 제목, 날짜, 시간은 현재 화면에서 수정할 수 있다. 이 기능은 AI가 만든 주간 계획 항목 직접 수정 기능이 아니다.
- 월간 화면은 일정 확인용이며, 월간 세부 자동 계획 생성 기능은 제공하지 않는다.

## 7. 계획 조정 흐름

### 목적

사용자가 생성된 계획 중 수정하고 싶은 부분을 플로팅 AI Mate를 통해 자연어로 조정한다.

### 흐름

```text
Today 또는 Month 확인
-> 수정하고 싶은 부분 발견
-> 플로팅 AI Mate 열기
-> 계획 조정 요청 입력
-> 오늘의 조정 잔여 횟수 확인
-> 잔여 횟수가 있으면 AI가 계획 조정
-> 조정 결과와 추천 근거 표시
-> Today의 날짜별 할일 / Month 일정 요약에 반영
```

### 조정 요청 예시

- "오늘 할 일을 조금 줄여줘."
- "수요일에 개인 일정이 많으니 그날 할 일을 줄여줘."
- "시험 공부 시간을 늘려줘."
- "알고리즘 과제를 더 앞쪽에 배치해줘."

### 조정 제한 흐름

```text
사용자가 하루 10회를 초과해 계획 조정 요청
-> 오늘의 잔여 횟수 0회 확인
-> 추가 조정 요청 차단
-> 다음 날 다시 가능하다는 안내 표시
```

### 조정 제한 규칙

- AI Mate를 통한 일일/주간 계획 조정 요청은 하루에 최대 10번만 가능하다.
- 사용자가 계획 항목의 시간, 날짜, 우선순위, 내용을 직접 손으로 수정하는 기능은 MVP에서 제외한다. 계획의 생성과 수정은 AI Mate로만 처리한다.

## 8. 추천 근거 확인 흐름

### 목적

사용자가 AI가 왜 특정 학습 항목을 추천했는지 이해하도록 한다.

### 흐름

```text
Today의 날짜별 할일 또는 Month의 일정 상세에서 항목 확인
-> 추천 근거 요약 확인
-> 더 자세한 이유가 필요하면 플로팅 AI Mate 열기
-> 추천 이유 질문
-> AI가 근거 설명
```

### 추천 근거 요소

- 마감일까지 남은 기간
- 예상 소요시간
- 중요도
- Google Calendar 개인 일정과의 충돌 여부
- 시험과의 연관성

## 9. MVP 범위 확인

- 하단 네비게이션은 `Today`, `Month`, `Upload` 3개 탭으로 구성한다.
- AI Mate는 개별 탭이 아니라 전역 플로팅 탭으로 제공한다.
- Upload 화면의 업로드 자료와 수정된 추출 정보는 AI Mate의 주간 계획 생성 근거로 사용된다.
- 주간 계획 생성은 주 1회만 가능하다.
- AI Mate 계획 조정 요청은 하루에 최대 10번만 가능하다.
- Google Calendar 외 다른 외부 캘린더는 연결하지 않는다.
- 전체 월간 세부 자동 계획 생성은 제공하지 않는다.
